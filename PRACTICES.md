# 작업 규칙

**이 파일은 커밋된다.** 머신을 넘어야 하는 규칙만 여기 둔다 — `CLAUDE.md` 와 `docs/` 는
`.gitignore` 대상이라 다른 머신의 나는 못 읽는다. 개인 셋업·경로·훅 설치는 `CLAUDE.md` 몫이다.

여기 있는 것은 전부 **실제로 밟아 본 것**이다. 안 밟아 본 규칙은 적지 않는다.

## 신규 기능 조사 — `CHANGELOG` 를 먼저 검색한다

🔴 **웹부터 보면 이미 있는 것을 후보로 올린다.** 두 번 그랬다 — 한 라운드에서 **여섯**,
다른 라운드에서 하나(트랜스클루전). 둘 다 만들기 직전까지 갔다.

무엇으로 확인하나를 재 봤다(2026-09-02):

| 근거 | 잘못 올렸던 후보 6 | 최근 릴리스 기능 4 | 커밋됨 |
|---|---|---|---|
| **`CHANGELOG.ko.md`** | **5 / 6** | **4 / 4** | ✅ |
| 손으로 쓴 기능 지도 | — | **0 / 4** | ❌ |

**CHANGELOG 가 이긴다.** 릴리스마다 쓰이므로 낡을 수가 없고, 커밋돼 있어 어느 머신에서나
읽힌다. 손으로 쓴 지도는 버전 도장이 여섯 릴리스 뒤처져 있었다.

```bash
grep -inE '후보|비슷한 낱말' CHANGELOG.ko.md
```

⚠️ **완벽하지 않다.** 여섯 중 하나(개요)는 CHANGELOG 에 안 잡혔다. 안 잡히면 그때 코드를 본다.

## 측정할 때 — 셋을 조심한다

### ① 예시 문자열을 실사용으로 세지 않는다

계획 문서가 문법을 설명하며 쓴 글자가 코퍼스 grep 에 잡힌다. **두 번 다 그걸 수요로 셌다:**

```
[[노트#헤딩]]  11건  → 전부 문서의 예시. 실사용 0
![[노트]]      11건  → 전부 문서의 예시. 실사용 0
```

세기 전에 **어디에 있는지 눈으로 본다.** 자동으로 거르고 싶어지면 `$lib/vaultAudit` 의
`maskNonProse`(코드 블록·인라인 코드·frontmatter 를 길이 보존하며 덮는다)가 이미 있다 —
다만 지금은 규칙으로 충분하다고 보고 도구를 안 만들었다.

### ② "코드에 없다"는 트리 전체를 보고 말한다

경로 둘만 찍어 grep 하고 "트랜스클루전이 없다"고 읽은 적이 있다. `embed.ts` 는
`src/lib/` 바로 아래 있었다.

### ③ 🔴 `grep -E` 에 `\|` 를 쓰지 않는다

ERE 에서 `|` 가 OR 이고 **`\|` 는 리터럴 파이프**다. 그래서 이렇게 쓰면 조용히 0 이 나온다:

```bash
grep -ciE 'a\|b' FILE   # ← "a|b" 라는 글자를 찾는다. 늘 0
grep -ciE 'a|b' FILE    # ← 이게 맞다
```

2026-09-02 에 이걸로 "CHANGELOG 에 그 기능이 없다"는 **틀린 결론**을 낼 뻔했다.
값이 0 이면 **패턴부터 의심한다.**

## 도구가 빈 결과를 주면

**"없다"와 "도구가 안 돌았다"를 구분하고 나서 결론 낸다.** 이 저장소에서 실제로 겪은 것:

- `find` 가 심링크를 안 따라가 `docs/` 가 비어 보였다
- `grep -E` 의 `\|`(위)
- 게이트가 0초에 "통과" — 안 돈 것이다. 판별법은 소요 시간뿐
- 카나리아가 다른 패턴 덕에 우연히 울어서 반쪽 가드를 못 봤다

## 규칙을 문서에만 두지 않는다

지킬 장치가 없는 규칙은 낡는다. 실제로:

- *"확장자를 벗기는 곳은 여기 하나다"* → 그 사이 여덟 곳이 생겼고 셋이 틀렸다
- *"모달과 팔레트가 공유한다"* → 셋으로 갈렸다(9 · 7 · 5)
- *"다음부터는 이만큼 모으기 전에 낸다"* → 다음 릴리스가 더 커졌다
- `"targets": "all"` → 하나만 만들었다. 13번의 릴리스 동안 아무도 몰랐다

그래서 규칙은 **게이트·타입·테스트**로 옮긴다. `npm run check:arch` 의 내용 규칙과
`scripts/*.test.ts` 가 그 자리다. 옮길 수 없으면 **적어도 낡음이 눈에 보이게** 만든다.

# 작업 규칙

**이 파일은 커밋된다.** 머신을 넘어야 하는 규칙만 여기 둔다 — `CLAUDE.md` 와 `docs/` 는
`.gitignore` 대상이라 다른 머신의 나는 못 읽는다. 개인 셋업·경로·훅 설치는 `CLAUDE.md` 몫이다.

여기 있는 것은 전부 **실제로 밟아 본 것**이다. 안 밟아 본 규칙은 적지 않는다.

## 신규 기능 조사 — `CHANGELOG` 를 먼저 검색한다

🔴 **웹부터 보면 이미 있는 것을 후보로 올린다.** 두 번 그랬다 — 한 라운드에서 **여섯**,
다른 라운드에서 하나(트랜스클루전). 둘 다 만들기 직전까지 갔다.

무엇으로 확인하나를 재 봤다(2026-09-02):

| 근거 | 잘못 올렸던 후보 6 | 최근 릴리스 기능 4 | 커밋됨 |
|---|---|---|---|
| **`CHANGELOG.ko.md`** | **5 / 6** | **4 / 4** | ✅ |
| 손으로 쓴 기능 지도 | — | **0 / 4** | ❌ |

**CHANGELOG 가 이긴다.** 릴리스마다 쓰이므로 낡을 수가 없고, 커밋돼 있어 어느 머신에서나
읽힌다. 손으로 쓴 지도는 버전 도장이 여섯 릴리스 뒤처져 있었다.

```bash
grep -inE '후보|비슷한 낱말' CHANGELOG.ko.md
```

⚠️ **완벽하지 않다.** 여섯 중 하나(개요)는 CHANGELOG 에 안 잡혔다. 안 잡히면 그때 코드를 본다.

## 측정할 때 — 셋을 조심한다

### ① 예시 문자열을 실사용으로 세지 않는다

계획 문서가 문법을 설명하며 쓴 글자가 코퍼스 grep 에 잡힌다. **두 번 다 그걸 수요로 셌다:**

```
[[노트#헤딩]]  11건  → 전부 문서의 예시. 실사용 0
![[노트]]      11건  → 전부 문서의 예시. 실사용 0
```

세기 전에 **어디에 있는지 눈으로 본다.** 자동으로 거르고 싶어지면 `$lib/vaultAudit` 의
`maskNonProse`(코드 블록·인라인 코드·frontmatter 를 길이 보존하며 덮는다)가 이미 있다 —
다만 지금은 규칙으로 충분하다고 보고 도구를 안 만들었다.

### ② "코드에 없다"는 트리 전체를 보고 말한다

경로 둘만 찍어 grep 하고 "트랜스클루전이 없다"고 읽은 적이 있다. `embed.ts` 는
`src/lib/` 바로 아래 있었다.

### ③ 🔴 `grep -E` 에 `\|` 를 쓰지 않는다

ERE 에서 `|` 가 OR 이고 **`\|` 는 리터럴 파이프**다. 그래서 이렇게 쓰면 조용히 0 이 나온다:

```bash
grep -ciE 'a\|b' FILE   # ← "a|b" 라는 글자를 찾는다. 늘 0
grep -ciE 'a|b' FILE    # ← 이게 맞다
```

2026-09-02 에 이걸로 "CHANGELOG 에 그 기능이 없다"는 **틀린 결론**을 낼 뻔했다.
값이 0 이면 **패턴부터 의심한다.**

## 도구가 빈 결과를 주면

**"없다"와 "도구가 안 돌았다"를 구분하고 나서 결론 낸다.** 이 저장소에서 실제로 겪은 것:

- `find` 가 심링크를 안 따라가 `docs/` 가 비어 보였다
- `grep -E` 의 `\|`(위)
- 게이트가 0초에 "통과" — 안 돈 것이다. 판별법은 소요 시간뿐
- 카나리아가 다른 패턴 덕에 우연히 울어서 반쪽 가드를 못 봤다
- **마스킹이 세려는 것을 지웠다.** 코퍼스에서 위키링크를 세는데 `maskNonProse` 를 썼더니
  **0건**이 나왔다. 그 함수는 "안 걸린 언급"을 찾는 도구라 **이미 걸린 링크를 일부러 덮는다.**
  🔴 도구 이름이 그럴듯하면 무엇을 지우는지 안 읽고 쓰게 된다 — 0 을 보고 패턴이 아니라
  **도구를 의심**해야 했던 자리다.

## 🔴 로컬 게이트 목록에 **설치가 없다**

게이트를 전부 돌려 초록이어도 CI 가 빨갈 수 있는 자리가 하나 있다 — `npm ci` 다.
로컬은 이미 설치된 `node_modules` 위에서 돈다.

2026-09-19 에 이걸로 CI 를 **두 번** 빨갛게 냈다.

### 플랫폼별 optional 의존성은 조용히 빠진다

Windows 에서 `npm install` 로 lock 을 다시 쓰면 리눅스·macOS 가 고르는 판이
**에러 없이** 빠진다. `@emnapi/*` 같은 네이티브 shim 이 그렇다.

```
main 의 lock        emnapi 항목  0개   ← CI 통과
Windows 가 쓴 lock  emnapi 항목 13개   ← CI 실패 (버전이 어긋남)
```

**반쯤 적힌 것이 아예 없는 것보다 나빴다.**

⚠️ **로컬 `npm ci --dry-run` 도 통과한다.** 그 판정은 이 머신의 해석 기준이다.
리눅스에서 `npm ci` 를 돌려야만 보인다 — 즉 **여기서는 확인할 방법이 없다.**

`CLAUDE.md` 의 *"Windows 에서 Rust/네이티브 타깃 결과는 macOS 타깃의 근거로 쓰지 않는다"*
와 같은 함정이고, **lock 파일에도 적용된다.**

### `npm i --no-save` 로 조사했으면 트리를 복원한다

버전을 오가며 범인을 가리는 것은 옳다. 끝나고 `npm ci` 로 되돌리지 않으면
**그 오염된 트리 위에서 lock 이 쓰인다.**

### ⚠️ Windows 는 파일을 붙잡는다

복구하려는 `npm ci` 가 `EPERM: unlink` 로 죽는다. 실제로 붙잡고 있던 것:

| 파일 | 범인 |
|---|---|
| `esbuild.exe` | `bundle-run.mjs` 가 남긴 프로세스 셋 |
| `rollup.win32-x64-msvc.node` | `svelte-language-server` |

둘 다 죽이면 풀린다. 그 사이 `npm ci` 가 `node_modules` 를 반쯤 지워 놓으므로,
**실패한 `npm ci` 뒤에는 트리가 깨져 있다고 보고** `npm install` 로 복구한다.

## 🔴 환경이 주는 구현에 양보하는 가드는, 업그레이드 때 하네스를 끈다

테스트 하네스에 이런 가드를 두지 않는다:

```ts
if (Element.prototype.animate) return;   // 있으면 비켜선다
```

`happy-dom` 이 `animate` 를 갖게 되자 스텁이 물러났고, 그 구현은 `onfinish` 를 안 알려
줘서 Svelte 아웃트로가 안 끝났다. 증상은 **"닫아도 모달이 안 사라진다"** 였다 —
앱은 멀쩡한데 **하네스가 꺼진 것**이고, 읽히기로는 앱 결함이다.

머리 주석은 아직 *"happy-dom 에는 Web Animations API 가 없다"* 라고 적고 있었다.
주석이 낡은 것이 문제가 아니라 **그 전제 위에 선 가드**가 문제다.

**테스트가 기대는 것은 환경이 무엇을 주느냐가 아니라 하네스의 계약이다.** 항상 덮어쓴다.

⚠️ 범인은 이분 탐색으로 가른다. `svelte` 를 되돌려도 빨갛고 `happy-dom` 을 되돌리면
초록이었다 — 둘을 같이 올렸으면 엉뚱한 쪽을 의심했다.

## 규칙을 문서에만 두지 않는다

지킬 장치가 없는 규칙은 낡는다. 실제로:

- *"확장자를 벗기는 곳은 여기 하나다"* → 그 사이 여덟 곳이 생겼고 셋이 틀렸다
- *"모달과 팔레트가 공유한다"* → 셋으로 갈렸다(9 · 7 · 5)
- *"다음부터는 이만큼 모으기 전에 낸다"* → 다음 릴리스가 더 커졌다
- `"targets": "all"` → 하나만 만들었다. 13번의 릴리스 동안 아무도 몰랐다

그래서 규칙은 **게이트·타입·테스트**로 옮긴다. `npm run check:arch` 의 내용 규칙과
`scripts/*.test.ts` 가 그 자리다. 옮길 수 없으면 **적어도 낡음이 눈에 보이게** 만든다.

# Changelog

[English](CHANGELOG.md) · **한국어**

> 원본은 영어판 [`CHANGELOG.md`](CHANGELOG.md)입니다. 이 문서는 번역본이라 한 발 늦을 수 있습니다.

Lapis의 버전별 변경 이력. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르되,
1인 프로젝트에 맞게 축약했다. 버전 체계는 [Semantic Versioning](https://semver.org/lang/ko/)을 느슨하게 따른다.

> **바이너리 배포는 하지 않는다.** GitHub Releases에 올려둔 산출물이 없고 태그만 단다.
> 설치는 [README의 설치 절](README.ko.md#설치)대로 소스에서 빌드한다.

> **⚠️ 히스토리 재작성 고지 (2026-08-18)** — 저장소를 개인 계정으로 옮기면서 `git filter-repo`로
> 전체 히스토리를 재작성했다. **v1.10.0 이전 커밋의 SHA는 전부 바뀌었다.** 태그는 새 SHA를 가리키도록
> 갱신됐지만, 외부 문서·PR 페이지에 남은 옛 SHA 참조는 더 이상 해소되지 않는다.

---

## [Unreleased]

### 수정

- **번들 설정이 만들지도 않을 것을 만든다고 적어 두고 있었다.** `"targets": "all"` 인데 실제로는
  **nsis 하나만** 나왔고, 에러도 경고도 없었다 — MSI 가 **13번의 릴리스 동안** 안 나온 것을
  아무도 몰랐다. ⚠️ 문서는 `"all"` 이 지원하는 형식 전부라고 하는데 실측은 달랐고, **왜 갈리는지는
  모른다**(WiX 는 정상이고 `--bundles msi` 로는 잘 나온다). 그래서 `"all"` 을 안 믿고 만들 것을
  배열로 적는다 — `["nsis", "app", "dmg"]`. Windows 에서 macOS 타깃을 같이 적어도 안 깨진다.
  MSI 는 기본에서 뺐다(13번 동안 아무도 안 찾았다). 필요하면
  `npm run tauri build -- --bundles msi`. 이제 `npm run bundle:check` 가 **만들기로 한 것이
  실제로 나왔는지** 버전까지 대조하고, 테스트가 `"all"` 로 되돌아가는 것을 막는다.

## [3.11.0] — 2026-09-01

### 추가
- **한글 IME 를 켠 채 영문을 쳐도 찾는다.** 팔레트가 **0건일 때만** 자판을 되돌려 한 번 더
  찾고, 무엇으로 찾았는지 한 줄로 말한다(*"한글로 찍혀서 0건이었습니다 — \"state\" 로 찾은
  결과입니다"*). 🔴 추측이 아니라 **실사용 기록에서 나왔다** — `lapis usage` 의 "결과가 0건이던
  질의" 아홉 중 **일곱**이 이 모양이었고(`ㄴㄷ셔ㅔㅠㅁㄴㄷ` → `setupbase`), 찾던 노트는 vault
  에 있었는데 키를 칠 때마다 0건을 받았다. ⚠️ 두벌식↔QWERTY 는 **고정 표**라 편집거리 추정이
  아니다 — 태그 감사에서 오타 탐지를 뺀 이유에 걸리지 않는다. ⚠️ **되는 질의는 절대 안
  건드린다.** 0건이라는 막다른 골목에서만 열리는 뒷문이고, 되돌린 것도 0건이면 거기서 끝이다.
  ⚠️ 반대 방향(영문 IME 로 한글 치기)은 **안 만들었다** — 자모를 음절로 조합하는 오토마타가
  필요하고, 이 로그에는 그 모양이 0건이라 근거가 없다.
- **vault 진단에 "끝났다는데" 하나.** 프론트매터는 끝났다고 적혀 있는데 본문에 `- [ ]` 가
  남은 노트를 센다. 앱 진단 화면 · `lapis doctor` · MCP `audit:"decay"` 셋이 **같은 판정
  함수**를 부른다. ⚠️ `status` 가 없거나 규약에 없는 값이면 **안 잡는다** — 점검표는 미체크인
  것이 정상이고, 오탐을 섞으면 목록 자체를 안 믿게 된다. 🔴 이 감사는 **"판단하지 않는다"를
  깨지 않는다**: 동의어를 이어붙이는 추론과 달리 여기는 한 노트 안이라 이어붙일 게 없고,
  둘 다 그 노트가 스스로 적은 것이라 세면 나온다. ⚠️ 실측 **0건**이다 — 이걸 만들게 한 그
  한 건은 같은 날 고쳤으니 지금은 성과가 아니라 보험이다.
- **`status` 를 낱말이 아니라 갈래로 묻는다 — `@done` · `@active` · `@todo`.** 사람은
  같은 자리를 doc_kind 마다 다른 말로 부른다(계획은 하나, 해결 기록은 다른 하나, 할 일은 또
  다른 하나). 코드에게는 전부 다른 문자열이라 리터럴 하나로 물으면 **에러 없이 절반만** 잡혔다 —
  실측 vault 53건에서 `status=완료` 는 15건, `status=@done` 은 41건이다. 갈래는 MCP·CLI
  양쪽에서 쓴다(`{"status":["@done"]}` · `--props status=@done`). **모르는 갈래는 운다** —
  오타가 0건을 정답처럼 내면 안 되고, `@` 를 다른 축에 쓰는 것도 조용한 no-op 이 아니라 오류다.
  ⚠️ 이건 **사람이 선언한 표**이지 기계가 동의어를 추론한 것이 아니다. vault 진단은 지금도
  동의어를 말하지 않는다. 낱말 표는 한 파일에만 두고 `npm run check:arch` 가 사본을 막는다 —
  세우고 보니 이미 세 곳에 사본이 있었고 **셋 다 다섯 중 둘만** 적고 있었다.
- **노트 안의 저장된 질의.** `lapis-query` 코드 펜스를 적어 두면 그 자리에서 돌고 결과가
  그려진다(`doc_kind` · `topic` · 본문 글자). 고르는 일은 **표 화면이 쓰는 그 매처**를
  그대로 부르므로 같은 질의가 표와 노트에서 다른 답을 낼 수 없고, 결과 줄은 평범한 위키링크라
  누르면 다른 링크와 같은 규칙을 탄다. **몇 건인지 먼저** 말하고 목록을 잘랐으면 잘랐다고 다시
  말한다 — 잘린 목록이 전부처럼 보이는 것은 목록이 없느니만 못하다. 못 읽는 질의는 빈 칸이 아니라
  그 자리에 오류로 보여준다(빈 칸은 "그런 노트가 없다"로 읽힌다). 노트에 되쓰지 않고, 태그 축은
  `tag` 축은 **정확 일치 또는 nested 접두사**로 걸린다(`subject` 로 물으면 `subject/ui` 까지).

### 수정
- **진단 화면의 탭 목록이 세 곳에 다르게 있었다.** 모달은 아홉을, "모달과 팔레트가 공유한다"고
  적힌 타입은 일곱을, 팔레트는 다섯을 알고 있었다 — 그래서 **미완 작업 · 끝났다는데 · 최근 변경 ·
  오래된 것 넷은 팔레트에서 직행할 수 없었고**, 타입이 두 탭을 몰라서 명령을 추가하려 해도
  컴파일이 막혔다. 🔴 그 코드의 주석이 이 일을 예언하고 있었다 — *"감사가 다섯이 되고 나서
  나머지 넷에 직행할 길이 없었다."* 같은 일이 또 난 이유도 같다: 손으로 유지하는 목록. 이제
  목록의 주인이 하나고, 라벨 표가 `Record<HygieneTab, …>` 이라 **탭을 늘리고 라벨을 안 채우면
  컴파일이 막힌다.** 반대 방향(명령만 있고 탭이 없는 것)은 테스트가 막는다.
- **도움말이 검사 개수를 손으로 적어 두고 낡았다.** `lapis --help` 의 `doctor` 줄은 *"감사 넷"*,
  `cli/README.md` 는 *"다섯"* 이라 적혀 있었는데 실제로는 일곱을 돈다 — **한 사실에 세 숫자.**
  개수를 안 적는 쪽으로 바꾸고, 다시 적으면 테스트가 막는다.
- **워처가 디렉터리 이벤트를 노트로 취급했다.** 실사용 로그에 `[reindex] scan/update 실패` 가
  **43회** 쌓여 있었고 경로가 전부 디렉터리였다(12개 디렉터리에 흩어져). Rust 쪽은 **일부러**
  디렉터리 이벤트를 흘려보내는데(*"rename 등 인지 위해"*) 프런트가 그걸 노트로 들었다 —
  스캔이 실패하고, 시간축 지도와 "밖에서 바뀜" 표시까지 디렉터리로 더러워졌다. ⚠️ **조용하다** —
  실패해도 루프가 계속 돌아 다른 노트는 정상 반영되고, 그 경고는 사람용 화면이 "경고 50" 이라는
  맨숫자로만 냈다. 이제 노트·무시·전체 다시 읽기 셋으로 가른다: 디렉터리가 **바뀐/생긴** 것은
  안의 파일 이벤트가 따로 오므로 무시하고, **사라지거나 이름이 바뀐** 것은 범위를 알 수 없으니
  통째로 다시 읽는다(같은 500ms 디바운스를 탄다). 🔴 후자는 지금까지 **인덱스가 옛 경로를 그대로
  들고 있던** 자리다.
- **`lapis usage` 가 계산한 것의 절반을 사람에게 안 보여줬다.** `errors[]`(무엇이 몇 번
  실패했나) · `opens[]`(실제로 연 노트) · `unusedCommands`(한 번도 안 쓴 명령)를 `--json` 만
  냈다. ⚠️ **"많이 쓴 명령: open:note 3"** 을 읽고 앱을 거의 안 쓴다고 읽을 수 있었는데 실제
  열기는 **31회**였다 — 그건 팔레트발 명령만 세는 축이다. 이 저장소가 이미 아는 부류다:
  *"맨숫자 하나는 어디에 몰렸는지를 감춘다."* 목록을 자를 때는 **잘랐다고 말한다.**
- **코드 블록 안의 예시를 할 일로 셌다.** 「어느 줄이 코드인가」가 **세 곳에 다르게** 있었다 —
  `linkRewrite` 는 markdown-it 블록 파스로 정확히 잡는데 그 함수가 **비공개**였고, 그래서
  미완 작업 스캐너는 줄 단위 토글(들여쓴 코드블록을 놓침)을, 안 걸린 언급의 마스킹은
  정규식(`~~~` 펜스와 들여쓴 코드블록을 놓침)을 각자 갖고 있었다. ⚠️ **조용하다** — 예시를
  할 일로 세면 숫자가 늘 뿐 에러가 안 나고, 부패 감사는 예시 하나로 멀쩡한 노트를
  "어긋났다"고 부른다. 이제 `src/lib/codeLines.ts` 하나가 주인이고 `check:arch` 가 사본을
  막는다. ⚠️ 정규식으로는 못 고친다 — **중첩 할 일도 네 칸 들여쓰기**라 "네 칸이면 코드"로
  두면 깊이가 통째로 죽는다. 리스트 계속인지 코드인지는 블록 파서만 안다. 전량 파스는 실측
  18배(1.6ms → 29.3ms)라 **체크박스가 있는 노트만**(실측 6%) 파서를 태운다. ⚠️ 이 vault 에서
  실제로 새던 것은 **0건**이다 — 보험이지 성과가 아니다.
- **`lapis tasks audit --json` 과 `lapis stats --json` 이 절대 경로를 냈다.** 다른 CLI JSON
  표면은 전부 vault 상대인데 이 둘만 달랐고, `cli/README.md` 는 *"`lapisQuery()` 의 응답을
  그대로 낸다"* 고 적어 두었다. ⚠️ **조용하다** — 명령 하나만 보면 멀쩡하고, 두 출력을
  교집합하는 순간 에러가 아니라 **0건**이 된다. 원인은 핸들러가 `rel` 을 거르기에만 쓰고
  출력엔 안 태운 것이다(사람용 줄은 이미 태우고 있었다). 이제 **모든 `--json` 표면**을 훑는
  테스트가 막는다 — 한 명령이 아니라 다음 감사도 같이 지킨다.
- **못 읽은 노트가 조용히 빠지고 있었다.** 미완 작업을 세려면 본문이 필요한데, 캐시에는
  있고 디스크에서 사라졌거나 권한이 막힌 노트를 **다섯 곳이 그냥 건너뛰었다.** 그래서
  "미완 12건"이 정말 12건인지, 아니면 12건 + 빠진 것들인지 **구별할 방법이 없었다.**
  🔴 같은 파일이 "맨숫자는 어디에 몰렸는지를 감춘다"며 집중도를 이미 더해 놓았는데,
  정작 그 **분모가 틀릴 수 있다**는 것은 안 봤다.
  이제 읽기와 세기가 한 곳에 있고, `lapis_stats` 와 `tasks audit` 이 빠진 수를 같이 낸다 —
  ⚠️ 0 일 때는 말하지 않는다. 시끄러운 정상은 곧 안 읽힌다.

- **`.mmd` 노트가 반쪽만 지원되고 있었다.** 앱은 `.mmd` 를 1급 노트로 다룬다 —
  Rust 가 색인하고, 감시자가 보고, 새 노트 모달이 안다. 그런데 세 경로가 여전히 `.md` 를
  전제했다:

  - `lapis new diagram.mmd` 가 **`diagram.mmd.md`** 를 만들었다. Rust 의 이름 바꾸기는
    같은 판정을 제대로 하고 있었다.
  - `.mmd` 노트의 이름을 바꾸면 들어오는 마크다운 링크가 **조용히 끊겼다.** 정규식이
    `.md` 만 잡았고, 치환은 잡은 것과 무관하게 `.md` 를 박았다.
  - `.mmd` 링크를 눌렀을 때와 질의에서 해소할 때 `.md` 만 벗겼다.

  🔴 노트 확장자 규칙은 이제 한 모듈에만 있고, **다른 곳에 쓰면 아키텍처 게이트가 빌드를
  깨뜨린다.** 주석은 오래전부터 "여기 하나"라고 적고 있었는데 여덟 곳이 갈라져 있었고
  그중 셋이 틀렸다 — **주석은 규칙을 못 지킨다.**

- **같은 태그 질문에 두 답이 나올 수 있었다.** 질의 엔진은 유니코드 정규화만 하고
  비교했는데 앱의 태그 인덱스는 소문자까지 맞췄다 — 그래서 `subject/UI` 와
  `subject/ui` 를 사이드바는 **한 태그로**, `tag:` 질의는 **두 태그로** 봤다.
  이제 둘 다 한 모듈을 지난다. ⚠️ 이 vault 에서 벌어진 차이는 **0건**이다 — 태그 85개 중
  대소문자만 다른 것이 없다. 보험이지 성과가 아니다.

- **앱이 이유를 말하기 2초 전에 부르는 쪽이 포기할 뻔했다.** 렌더 요청을 아무도 안
  받아가면 앱이 실패 파일로 이유를 적는데, 그건 앱 쪽 15.5초가 지난 뒤의 일이다.
  그런데 부르는 쪽 기본 상한이 20초였다. 실물로 재니 파일은 **약 18초**에 생겼다 —
  여유가 2초뿐이라, 창 띄우기가 조금만 늦으면 사용자는 "아무도 이 요청을 받지 않았다"
  대신 "앱이 제때 결과를 안 냈다"를 본다. **정확한 진단이 필요한 바로 그때 사라진다.**
  25초로 올렸고, 네 곳에 흩어져 있던 그 숫자를 한 곳으로 모았다.

- **두 번째 rename 이 첫 번째를 영원히 기다리게 만들 수 있었다.** 링크 갱신 동의 모달은
  호출부의 `resolve` 콜백을 들고 있는 슬롯에 담기는데, 그 슬롯을 그냥 덮어쓸 수 있었다.
  덮이면 첫 요청에 아무도 답하지 않아 rename 이 끝나지 않은 약속 위에 멈춘다 — 에러도
  타임아웃도 없이, 이름은 바뀌고 들어오는 인용은 안 바뀐 채로 끝난다. 모달이 떠 있어도
  전역 단축키가 안 막히므로 두 번째 rename 은 실제로 닿을 수 있었다.

  이제 스토어가 짝을 갖는다 — 새 요청은 앞의 것을 취소로 닫고, 매듭짓기는 답하기와
  비우기를 한 번에 한다. 밖에서 슬롯을 직접 건드릴 수 없다.

  모달 자체는 **테스트가 하나도 없었다.** 여러 파일을 한꺼번에 고치는 쓰기의 동의
  게이트인데도 그랬다. 닫는 길 넷이 각각 무엇으로 매듭짓는지, 그리고 **기본 초점이
  취소인지**를 못 박았다 — 잘못 친 Enter 하나가 되돌릴 수 없는 쓰기로 가면 안 된다.

## [3.10.0] — 2026-08-29

> MCP 가 앱을 조작하고, CLI 가 파이프 중간에 낀다.
> 도구 하나가 여덟이 됐고, 명령 열넷이 스물하나가 됐다.

### 추가
- **MCP 도구 일곱.** `lapis_query` 하나뿐이었고 읽기만 됐다. 이제:
  `lapis_stats`(vault 통계) · `lapis_usage`(사용 기록 집계) · `lapis_index`(헤드리스 재색인) ·
  `lapis_export_html` · `lapis_open`(앱에서 노트 열기) · `lapis_reveal`(폴더 열기) ·
  `lapis_render`(앱 품질 HTML, mermaid 다이어그램 PNG).

- **`lapis_render` 는 앱 옆이 아니라 앱을 통해 그린다.** mermaid 도 수식도 사용자 CSS 도
  브라우저에서만 존재한다. MCP 안에서 한 번 더 그리면 **렌더러가 둘**이 되고, 둘은 반드시
  갈린다 — 이 저장소에서 가장 자주 나온 결함이다. 대신 이미 있는 argv 통로로 요청을 넘기고,
  앱이 그린 것을 원하는 자리에 떨어뜨린다. 느리지만 화면과 어긋날 수가 없다.

- **`lapis_export_html` 은 앱 없이 돌고, 어디가 다른지 말한다.** 응답의 `differs_from_app`
  이 자체 변환기가 재현 못 하는 것을 적는다(mermaid · 수식 · 사용자 CSS · 위키링크 해소).
  다른 것은 **오류가 아니다.** 다른데 말 안 하는 것이 오류다.

- **CLI 명령 일곱.** `read`(본문. `-` 면 표준입력에서 경로) · `stats` · `usage` · `new` ·
  `config` · `diff` · `completion`(bash · zsh · pwsh).

- **CLI 가 파이프에 낀다.** `--quiet` 가 사람용 줄을 빼고 `read -` 가 표준입력에서 경로를
  받아, 이렇게 된다:
  `lapis search q --json | jq -r '.results[0].path' | lapis read -`

- **`config` 가 설정에 두 번째 눈을 준다.** `mcp_enabled` 를 앱에서만 켤 수 있었다. 그 토글이
  조용히 안 먹었을 때 확인할 다른 경로가 없어서 원인을 엉뚱한 데서 찾았다. 읽는 쪽이 둘이면
  어긋난 순간 소리가 난다.

### 추가
- **`lapis render`** 가 떠 있는 앱에게 노트를 넘겨 화면에 보이는 것을 그대로 받는다 —
  앱 품질 HTML, 또는 본문 첫 mermaid 다이어그램을 PNG 로. `export` 는 여전히 앱 없이
  돌지만 브라우저가 없어 mermaid 가 코드 펜스로 남는다. 요청 조립·대기·실패 판정을
  한 모듈에 모아 CLI 와 `lapis_render` 가 같이 부른다 — 옮긴 뒤 둘이 같은 노트에서
  **정확히 같은 바이트 수**를 냈다.

- **`lapis props rename`** 이 `props audit` 이 찾은 것을 고친다. 감사는 `topic` 이
  `feature`(11개)와 `feature-selection`(2개)로 갈렸다고 알려 주는데 **손쓸 방법이
  없었다** — 태그에는 `tag rename` 이 있고 frontmatter 값에는 없었다. 규율은 같다:
  기본 dry-run, `--apply` 로만 쓰기, 같은 백업·교체 트랜잭션.
  다만 태그와 달리 **접두 계층이 없다** — `cross-platform` 은 `platform` 이 아니므로
  정확히 일치할 때만 바꾼다.

- **`lapis tasks audit` 을 좁힐 수 있다** — `--under` · `--exclude` · `--top` ·
  `--count`. 이 vault 는 미완 89건 중 67건이 수동 테스트 체크리스트 하나인데 99줄이
  매번 그대로 나왔다. 걸러낸 건수를 **항상 적는다** — 안 적으면 좁힌 목록이 전부로 읽힌다.

- **작업 합계가 어디에 몰렸는지 말한다.** `lapis_stats` 가 `{ open: 90, done: 30 }` 을
  냈는데, 맞는 값이면서 오해를 부른다 — 4분의 3이 체크리스트 하나였다. 이제 가장 많은
  노트와 그 비중을 같이 낸다. **설정을 새로 만들지 않았다** — 고아 노트 때와 같은 판단으로,
  무엇을 숨길지 앱이 정하는 대신 사람이 판단할 두 번째 숫자를 준다.

- **나란히 보기.** 트리에서 노트를 우클릭해 읽던 것 옆에 띄운다 — 지금까지는 창을 따로
  띄워야 했고, 모니터가 하나면 불편했다. 옆칸은 **읽기 전용**이다: 탭도 편집기도 문서 내
  검색도 없다. 본문 상태가 전부 싱글턴이라 그걸 pane 별로 쪼개는 것은 2,400줄짜리 파일의
  구조 개편이고, 값은 "A 를 읽으면서 B 를 띄워 두기"에 있다. 옆칸의 링크는 **본문**을
  움직이고, 본문이 옆칸과 같은 노트로 가면 옆칸이 스스로 닫힌다 — 안 그러면 같은 문서가
  나란히 두 벌 뜬다.

- **명령 팔레트가 초성을 알아듣는다.** `ㅅㅌ` 은 파일(`새 탭`)은 이미 찾아냈다.
  파일 인덱스가 초성을 미리 계산해 두기 때문이다. 그런데 명령은 다른 경로를 타서 라벨
  원문과 비교했고, 낱자만 있는 질의는 **영영 안 맞았다.** 이제 라벨도 같은 방식으로 접고,
  낱말을 가로지르는 것(`ㅅㄴㅌ` → `새 노트`)까지 잡는다. 그렇게 맞은 것은 다른 강한
  매치와 똑같이 맨 위 그룹으로 올라간다.

- **최근 연 노트가 읽던 자리를 말한다.** 목록은 이미 있었고, 없던 것은 **그중 어느 것을
  실제로 읽었는지**였다. 기억된 자리가 있으면 점이 붙고, 편집기에서 보던 자리면 줄 번호가
  뜬다. **퍼센트는 안 쓴다** — 저장된 자리는 스크롤과 줄만 알지 문서 길이를 모르고,
  어림한 진도는 **틀려도 틀려 보이지 않는다.**

### 수정
- **앱은 그리는데 감사는 안 세던 작업이 있었다.** `+ [ ] 할 일` 과 `1. [ ] 할 일` 은
  체크박스로 그려지는데 `tasks audit` · `stats` · MCP 는 안 셌다. 화면에는 남은 일이
  보이는데 도구는 "미완 작업이 없다"고 답하는 상태다. 작업 규칙이 두 곳에 적혀 있었고,
  렌더러는 markdown-it 위에 있어 CommonMark 불릿 셋과 번호 목록을 다 받는 반면 감사는
  줄 정규식으로 `-` 와 `*` 만 받았다.

  둘은 합칠 수 없다 — 하나는 파서 뒤를 보고 하나는 인덱스 없이 본문만 훑는다. 그래서
  같은 입력을 두 규칙에 먹여 **답이 갈리면 실패하는** 테스트를 뒀다.

  ⚠️ 이 vault 에서 새던 것은 **0건**이다 — `+` 로 쓴 것이 없다. 보험이지 성과가 아니다.

- **나란히 보기 옆칸의 링크가 통째로 죽어 있었다.** 위키링크도, 마크다운 링크도, mermaid
  내보내기 버튼도 눌러서 아무 일이 안 났고 **에러도 안 났다.** 클릭 규칙이 `+page.svelte`
  안에 있어 아무도 못 부르니 옆칸에 사본을 뒀는데, 그 사본이 틀렸다. `a.wikilink` 를
  찾았지만 플러그인이 내는 것은 `span.wikilink` 였고(앵커면 웹뷰가 떠나 버리므로 일부러
  그렇게 낸다), 읽은 속성 `data-target-path` 는 **저장소 어디서도 만들지 않는 이름**이었다.
  일반 `<a>` 는 아예 안 받아서 기본 동작이 살아 있었다 — 안쪽 `.md` 링크는 SPA 라우팅을,
  바깥 URL 은 웹뷰 이동을 일으킬 수 있었다.

  사본을 고치는 대신 **없앴다.** 두 칸이 같은 함수를 부른다. 같은 부품으로 그리니 나오는
  HTML 이 같고, 그러면 그것을 읽는 규칙도 하나여야 한다.

  🔴 옛 테스트 둘이 이 버그를 못 박고 있었다 — 렌더러가 내는 DOM 이 아니라 **망가진
  컴포넌트가 기대하던 DOM** 을 손으로 지어 놓고 쟀다. 그래서 기능이 완전히 죽은 채로 초록이었다.

- **`props rename` 이 안 건드려야 할 값을 바꿀 수 있었다.** YAML 에서 `#` 은 앞에 공백이
  있어야 주석이라 `C#` 은 스칼라 `C#` 하나인데, 주석 인식이 공백 없는 것까지 받아서
  `topic: C#` 이 값 `C` 로 읽혔다. `C` 를 바꾸면 `topic: D#` 이 됐고, 반대로 진짜 값
  `C#` 은 **영영 안 맞았다.** dry-run 미리보기도 "바뀐다"고 정상으로 보여줘서 사람이
  검토해도 알 수 없었다. 따옴표 안의 값(`topic: "a # b"`)도 같은 자리에서 잘렸다.

- **렌더 요청이 다른 vault 를 연 창을 가로챌 수 있었다.** 차가운 기동은 `main` 을 지목해
  vault 와 무관하게 받아가게 하는데, 그 표식을 아무도 지우지 않아 **그 뒤 모든 요청**을
  같은 창이 가져갔고, 그 창은 요청받은 vault 로 갈아탔다. 노트 열기는 새 요청마다 표식을
  지운다 — 렌더만 빠져 있었다.

- **옆칸이 잠깐 이전 노트를 보여줬다.** 읽기가 비동기라 경로가 바뀐 뒤에도 머리글은 B 를
  말하는데 본문은 A 였고, 그걸 알릴 신호가 없었다. 잠깐 비어 있는 편이 틀린 것보다 낫다.

- **"안 쓴 명령 없음"이 거짓말이었다.** 실제 MCP 클라이언트로 물으니 `lapis_usage` 가
  명령 0건, 안 쓴 명령 0건을 냈다. 진실은 분모가 없었다는 것이다 — 명령 목록이 Svelte
  스토어를 끌어오는 모듈에 있어 헤드리스 소비자가 못 읽었고, 그래서 아무것도 안 넘겼다.
  이제 id 는 import 가 없는 모듈에 있고, 실제 명령과 어긋나면 **컴파일이 안 되며**,
  분모가 없으면 빈 배열이 아니라 `null` 을 낸다.

- **어느 창도 안 연 vault 의 렌더 요청이 아무 데도 안 갔다.** 두 번째 프로세스는 인자를
  넘겼다는 이유로 **코드 0(성공)** 을 내고 끝나는데, 결과 파일도 실패 파일도 안 생겨서
  셸에서 `&&` 로 이으면 그대로 진행됐다. 노트 열기는 이미 이 경우를 풀고 있었다 —
  아무도 안 받아가면 창을 하나 띄우고 그 라벨을 기억해, 그 창이 vault 를 안 따지고
  받아가게 한다. 렌더도 같은 방법을 쓰고, 그 창에서 요청받은 vault 를 열고, 그러고도
  안 받아가면 실패 파일을 쓴다 — 타임아웃이 유일한 신호인 상황을 남기지 않는다.

- **앱이 꺼져 있을 때 온 렌더 요청이 조용히 사라졌다.** 앱은 argv 에서 요청을 받아 담아
  뒀는데, 프런트는 기동 때 **한 번만** 물었다 — vault 가 열리기 전에. 요청은 vault 가 맞는
  창에만 내주므로 못 가져갔고, 다시 물을 계기가 될 이벤트는 구독보다 먼저 지나간 뒤였다.
  Rust 로그에 "담음"은 찍히고 "가져감"은 안 찍혔다. 결과 파일도, 실패 파일도 없었다 —
  실패한 적이 없으니까. 부른 쪽은 타임아웃으로만 알았다. 이제 vault 가 열린 뒤 다시 묻는다.

- **`data-rendered` 를 "끝났다"로 읽었다.** mermaid 는 그리기를 **시작**하는 순간 그 속성을
  `pending` 으로 놓는데, 기다리는 쪽이 속성이 붙은 것을 다 끝난 것으로 셌다. 그래서 다음
  줄이 아직 없는 `<svg>` 를 찾다 "다이어그램이 아직 안 그려졌거나 문법이 틀렸다"를 냈다 —
  **둘 다 아니었다.** 이제 속성이 아니라 결과물을 보고, "느리다" · "문법이 틀렸다" ·
  "됐다"를 따로 말한다. 조치가 다르기 때문이다.

- **응답에 담기는 경로가 이제 한 모양이다.** `lapis_render` 의 `out` 을 고쳤더니 같은 누수가
  이웃에 그대로 있었다 — `lapis_usage` 는 로그 폴더를, `lapis_open` 은 실행파일 경로를
  역슬래시로 냈다. 소비자가 `/` 로 쪼개므로 혼자 다른 것은 파일명과 부모 디렉터리를 조용히
  잃는다. 테스트 하네스가 아니라 **실제 MCP 클라이언트로 불러 보고** 나왔다.

- **내보내기가 더는 vault 안에 쓰지 않는다.** `out` 을 안 주면 `lapis_export_html` 이 노트
  옆에 — 즉 vault 안에 — HTML 을 뒀다. 모듈은 "vault 밖으로만 쓴다"고 선언해 놓고서. 앱이
  감시 중이라 그 쓰기가 재색인을 불렀고, 사용자가 안 만든 파일이 노트 사이에 쌓였다.
  이제 `~/Downloads`(없으면 임시 폴더)로 나가고, 응답이 어디인지 말한다.

- **타임아웃이 엉뚱한 곳을 짚었다.** 앱이 버젓이 떠 있는데도 `lapis_render` 가 "앱이 떠
  있는지 확인할 것"이라고 답했다. 진짜 원인은 떠 있는 앱이 이 기능 이전 버전이라는 것이었다 —
  옛 빌드는 모르는 인자를 무시하고, 두 번째 프로세스는 argv 를 넘긴 뒤 끝나며, 아무도 실패를
  쓰지 않는다. 이제 조치문이 두 원인을 버전부터 말한다. 탐지는 못 한다 — 네트워킹 코드가
  없고 argv 는 한 방향이라 **떠 있는 앱에게 버전을 물을 통로가 없다.** 있을 수 없는 탐지보다
  정직한 조치문이 낫다.

- **`lapis_render` 응답에 경로 모양이 둘 섞였다.** `path` · `vault` 는 `/`, `out` 만
  역슬래시였다. 소비자가 경로를 `/` 로 쪼개므로 혼자 다른 것은 파일명과 부모 디렉터리를
  조용히 잃는다.

- **설정을 원자적으로 쓴다.** CLI 가 설정 파일을 그 자리에서 덮었다. 파일 하나에 앱 설정
  *전부* 가 들어 있어서, 쓰다 끊기면 키 하나가 아니라 전부 날아가고 앱은 그걸 "설정이 없다"로
  읽는다 — MCP 게이트가 닫힌다. 이제 임시 파일 → 같은 디렉터리에서 rename.

- **표준입력 경로에 `\r` 이 남았다.** Windows 파이프는 CRLF 를 준다. `\n` 만 자르면 경로 끝에
  캐리지 리턴이 붙고, 화면으로는 멀쩡한 채로 파일이 **없다고** 나온다.

### 변경
- MCP 실패 목록을 표로 바꾸고 종류마다 조치를 적었다. `app_not_found` 와 `app_timeout` 을
  일부러 나눈다 — 하나는 설치 문제, 하나는 앱이 안 떠 있다는 뜻이고, 잘못 들은 쪽은 엉뚱한
  데를 판다.

## [3.9.0] — 2026-08-29

> 5차 분석의 나머지 전부와 CLI 명령 다섯. **네 번으로 나누지 않고 한 번에** 냈다 —
> 릴리스 의식이 일보다 더 들고 있었다.

### Added
- **인쇄.** 내보낸 HTML 이 `@media print` 를 싣는다. 브라우저의 "PDF 로 저장"이 읽을 만한
  것을 낸다. PDF 라이브러리는 안 들였다 — 스타일시트 한 장이 같은 결과를 내고, 라이브러리를
  들이면 글꼴·CJK·수식이 전부 우리 문제가 된다.

  코드 블록을 접는다(가로로 스크롤하던 것이 종이에서는 **그냥 사라진다**), 바깥 링크는
  주소를 뒤에 붙이고, 제목이 페이지 맨 아래에 혼자 남지 않는다.

- **오류가 상태바에 뜬다.** 릴리스 빌드에는 devtools 가 없어서, 실패는 로그에만 남고 **그
  파일을 열기 전에는 뭔가 깨졌다는 사실 자체를** 몰랐다. 이제 숫자 한 칸이 그 자리에서
  말한다. 끼어들지는 않는다 — 배너는 여전히 되돌릴 수 없는 쓰기가 깨졌을 때만 뜬다.

- **vault 진단에 "오래된 것" 탭** — 반년 넘게 그대로인 노트. 고치라는 목록이 아니라 **다시
  볼 때가 됐는지 묻는 목록**이다. 파일 수정 시각과 frontmatter `date` 중 더 최근을 보고,
  **둘 다 모르는 노트는 뺀다** — "모른다"를 "아주 오래됐다"로 세면 목록이 가득 차고 아무도
  안 믿는다.

- **모호한 링크를 읽는 자리에서 표시한다.** 이 vault 에는 같은 이름의 노트가 일곱 쌍 있다
  (두 프로젝트). 진단은 목록을 냈지만 정작 `[[open-items]]` 를 **읽고 있을 때는** 아무
  표시가 없었다. 이제 물결선이 "확인해 보라"고 말하고 후보를 도움말에 적는다.
  **막지 않는다** — 링크는 그대로 동작한다.

- **팔레트 미리보기.** 고른 노트의 앞부분을, 디바운스해서, 경로가 있는 항목에만.
  옆이 아니라 **아래**에 둔다 — 팔레트 폭은 계측으로 맞춰져 있고, 옆으로 넓히면 결과 글자가
  줄어든다.

- **리사이저를 키보드로.** 사이드바·컨텍스트 리사이저에 `role="separator"` 와 이름은 있는데
  `tabindex` 도 키 처리도 없었다 — **보조기술은 컨트롤이 있다고 말하는데 닿을 수가 없었다.**
  화살표로 움직이고, `Shift` 면 크게, `Home` 이면 기본값.

- **CLI 명령 다섯.** `read`(이름으로 찾아 본문을 낸다 — `cat` 은 경로를 알아야 한다) ·
  `stats`(vault 에 무엇이 있나. `status` 는 캐시 상태라 다른 질문이다) · `usage`(앱이
  `analysis.md` 를 만들 때 쓰는 **같은 analyzer**) · `new`(템플릿으로 노트를 만들되 덮어쓰기와
  vault 이탈을 거부한다) · `completion`(bash·zsh·pwsh. 명령 목록을 **명세에서 뽑는다** —
  손으로 적으면 명령을 추가할 때마다 잊는다).

### Fixed
- **명령 사용량이 여전히 덜 세어지고 있었다.** 타이틀바와 탭바가 스토어를 직접 불러서,
  vault 열기 · 타이틀바에서 팔레트 열기 · 탭 닫기·고정이 전부 안 보였다. 이제 기록한다.

## [3.8.0] — 2026-08-29

> 5차 분석의 첫 묶음 — **찾아 놓고 다시 찾게 만들던** 자리들.

### Added
- 🔴 **읽던 자리에서 다시 열린다.** 노트마다 위치를 기억했다가 본문이 그려진 뒤 되돌린다.
  근거는 실측이다 — 사용 로그의 네 세션이 **전부 같은 노트를 `via: tab` 으로** 열었고,
  매번 맨 위였다.

  ⚠️ 자리 적기는 `requestAnimationFrame` **밖**에서 한다. 창이 가려지면 rAF 가 한 번도
  안 오는데(v3.5.1), 마지막으로 읽던 자리가 정확히 그때 필요하다. 되돌릴 때는 적용
  직전에 경로를 다시 확인한다 — 빠르게 노트를 바꾸면 **이전 노트의 자리가 새 본문에**
  적용될 수 있다. `[[노트#헤딩]]` 으로 지목한 자리가 있으면 그쪽이 이긴다.

- **미완 작업이 그 줄로 데려다준다.** `OpenTask` 는 `line` 을 이미 담고 있었는데 화면은
  파일까지만 갔다 — 90건짜리 노트에서 그 항목을 다시 손으로 찾아야 했다. 이제 작업 글자를
  문서 내 검색으로 넘긴다(`⌘⇧G` 결과가 쓰는 것과 **같은 기계**). **리터럴**로 넘긴다 —
  할 일 문장에 괄호·별표가 흔해서 정규식으로는 안 맞거나 죽는다.

- **끊긴 링크에서 그 노트를 만든다.** 이름을 새 노트 대화상자로 넘기고, 폴더는 그 링크를
  거는 노트의 폴더로 둔다. 확정하기 전에는 아무것도 안 쓴다.

- **아웃라인에서 헤딩 링크를 복사한다.** `[[노트#헤딩]]` 앵커는 이미 동작했는데, 그 링크를
  만들려면 헤딩을 옮겨 적어야 했고 **한 글자만 달라도 조용히 문서 맨 위로** 간다.
  `]]` · `|` · `#` 가 든 헤딩은 **버튼을 아예 안 낸다** — 깨진 링크는 없는 것보다 나쁘다.

- **닫은 탭 되살리기** — 명령 팔레트에서. 단축키는 안 준다. `⌘⇧T` 는 새 창이 쓰고,
  매일 쓰는 조작이 아니라 키를 뺏으면 자주 쓰는 것이 밀린다.

### Fixed
- 🔴 **다른 화면에서 넘겨준 검색이 하이라이트되지 않았다.** 미리보기 하이라이트는
  프리뷰 HTML 이 바뀔 때만 다시 걸리고, 질의는 **일부러** 비반응으로 읽는다(반응으로
  만들면 다음 일치로 넘어갈 때마다 현재 위치가 0 으로 되돌아간다). 그런데 인계는 순서가
  반대다 — **본문이 먼저, 질의가 나중.** 그래서 `⌘⇧G` → 노트가 검색바에 글자만 채운 채
  문서 맨 위에 떨어지고 있었다.

  인계 전용 번호를 두고 그것만 하이라이트를 깨우게 했다. `applySearch` 에서만 늘어나므로
  일치 사이를 오가는 동작은 그대로다.

## [3.7.2] — 2026-08-29

> 둘 다 **앱이 스스로 쓴 첫 분석 문서를 읽다가** 나왔다.

### Fixed
- 🔴 **분석 문서가 여전히 가리고 있었고, 없어진 내보내기를 안내했다.** 경로를 `…/노트.md` 로
  적고 "원본이 필요하면 `raw` 로 다시 내보낸다"고 했는데, v3.7.1 이 내보내기 UI 를 없앴다.
  가림은 리포트가 **밖으로 나가는 순간**을 막으려던 것이고 이제 그런 순간이 없다 — 게다가
  어느 폴더에서 일하는지를 가리면 이 로그가 답하려던 첫 질문이 사라진다.

  이제 경로와 질의문이 그대로 나오고, **문서 첫 줄이 그 사실을 밝힌다** — 모르고 공개된 곳에
  붙여넣는 일이 없도록.

- 🔴 **앱을 쓰고 있는데 명령 사용량이 0 이었다.** 설정 톱니가 레일의 `activate()` 를 안 거치고
  `openSettings` 를 직접 불렀고, 기록은 `activate()` 안에 있었다. 그래서 문서가 "많이 쓰는
  명령: 아직 기록이 없다"라고 하면서 스무 개를 "한 번도 안 쓴 명령"으로 올렸다 —
  **틀린 게 아니라 거꾸로였다.**

  톱니가 기록하게 했고, 창 메뉴는 **항목을 고르는 한 곳**에서 남긴다(새 항목을 넣을 때
  빼먹지 않게). 진입점마다 기록이 붙어 있는지 가드가 못 박는다.

### Added
- **창 메뉴(`⋯`)가 어느 항목을 골랐는지 남긴다** — `via: "menu"`. 스키마가 자리를 잡아 뒀는데
  아무도 안 쓰고 있던 입구다.

## [3.7.1] — 2026-08-29

### Fixed
- 🔴 **창이 안 닫혔다.** v3.7.0 이 세션 길이를 남기려고 닫기 훅을 걸었다. Tauri 는 그
  핸들러가 끝날 때까지 창을 붙잡으므로, 그 안에서 느려지거나 던지면 **앱을 끌 방법이
  없어진다.** X 버튼이 고장 난 것으로 보였다.

  훅을 없앴다. 세션 길이는 이미 로그에 있는 타임스탬프에서 계산한다 — `session start` 부터
  그 뒤 마지막 이벤트까지. **창이 열려 있던 시간**이 아니라 **실제로 쓴 시간**을 재는 셈이라
  오히려 쓸모 있고, 닫을 때 아무 비용도 안 든다.

  ⚠️ 기능보다 교훈이 크다: **관찰 장치가 앱을 인질로 잡을 수 있으면 안 된다.** 세션 길이
  숫자 하나가 끌 수 없는 프로그램보다 값질 수는 없다.

### Changed
- **로그 파일이 `YYYY-MM.log` 다** — 내용은 그대로 JSONL 이고 확장자만 바꿨다. 폴더에서 꺼낼
  때 로그로 보이는 편이 헷갈리지 않는다. 이미 있는 `.jsonl` 은 기동할 때 옮긴다 — 안 옮기면
  그 달들이 **에러 없이** 통계에서 사라진다.

- **내보내기 UI 를 없애고, 분석 문서가 알아서 쌓인다.** 설정에 저장 버튼과 형식 선택과 가림
  체크박스가 있었는데, 그건 **로그를 관리하는 일**을 읽는 사람에게 떠넘긴 것이다. 이제 앱이
  기동할 때 로그 옆에 `analysis.md` 를 써 두고, 필요한 것을 꺼내 가면 된다. "기록 지우기"는
  그대로 둔다.

  ⚠️ 닫을 때가 아니라 **기동할 때** 쓴다 — 위와 같은 이유다. 그래서 문서는 지난 세션까지의
  이야기이고, 꺼내 볼 때 필요한 것이 정확히 그것이다.

### Removed
- 수동 내보내기(`usageExport.ts`)와 리포트 가림. 문서가 이제 앱 데이터 폴더를 안 벗어나므로
  가릴 경계가 없다 — 자기가 읽을 것을 가리면 쓸모만 준다.

## [3.7.0] — 2026-08-29

> 사용 로그는 "다음에 무엇을 고칠까"에 답하려고 넣었는데, 내보낼 수 있는 것이 마크다운 요약뿐
> 이었다 — 표를 다시 파싱해 분석하는 사람은 없다. 그 목적에 맞게 다시 세웠다.

### Added
- **원본을 JSONL 로 내보낸다.** 디스크에 이미 그 형태로 있고, 분석이 원하는 형태도 그것이다.
  월 파일을 **그대로 잇는다** — 파싱해서 다시 쓰면 파서가 못 읽은 줄이 조용히 사라지고, 그러면
  내보낸 파일이 디스크와 다른데 그 사실을 알 방법이 없다.

- **이벤트 종류 넷을 더했다.** 검색(무엇을 찾았고 결과가 몇이며 하나라도 열었나) · 노트 열람
  (무엇을, 어디로 닿아) · 성능(인덱스 빌드 · 캐시 적중 · vault 열기 — **노트 수와 함께**, 시간만
  으로는 vault 가 큰 건지 코드가 느린 건지 모른다) · 세션 끝, 그래서 "한 번에 얼마나 쓰나"에
  답이 나온다.

  ⚠️ 열람은 **어디로 들어왔는지를 호출부가 준다.** `selectNote` 안에서 추측하면 틀린다 —
  트리·팔레트·백링크·위키링크가 다 같은 함수를 부른다. 필수 인자로 두어 타입 검사가 25개
  호출부를 전부 짚게 했다. 주의력에 맡기지 않는다.

- **`UsageAnalyzer` 가 흘려보낸다.** 설정 화면이 모든 달을 한 배열로 모은 뒤 집계했는데, 월
  파일 상한이 16 MB 라 열두 달이면 최악 192 MB 의 문자열이다. 종류가 늘수록 나빠진다. 이제
  달을 하나씩 먹이고 버린다.

### Fixed
- 🔴 **가림이 홈 경로의 사용자 이름을 남겼다.** 일반 경로 규칙이 먼저 돌아 `/Users/이름` 을
  `…/이름` 으로 줄였고, 그 아래 사용자 이름 규칙 둘은 **한 번도 안 걸렸다** — 가림이 막으려던
  바로 그것이다. 이제 이름 규칙이 먼저 돈다.

- 🔴 **가림이 검색 질의를 안 덮었다.** 이제 질의를 기록하는데, 경로 규칙으로는 질의가 안
  걸린다. 그대로 뒀으면 가린 리포트가 **가렸다고 말하면서 질의문을 그대로 인쇄**했을 것이다.
  가리면 길이만 남는다.

- **경고가 오류로 기록됐다.** 심각도가 `msg` 앞의 `warn: ` 접두사에 숨어 있어 분석할 때마다
  다시 파싱해야 했다. 이제 별도 필드이고, 파서가 이미 쌓인 줄을 옮겨 읽어 몇 달치 경고가
  오류로 세어지지 않는다.

- **못 읽은 줄과 더 새 버전이 쓴 줄을 같이 셌다.** 한 버전만 되돌려도 멀쩡한 로그가 "손상된
  줄 수천 개"로 보인다. 이제 따로 세고 리포트가 어느 쪽인지 말한다.

### Changed
- **저장 버튼이 둘에서 하나로.** "리포트 저장"과 "가리지 않고 저장"이 나란히 있어서 눌러 보기
  전엔 뭐가 다른지 몰랐다. 이제 형식(원본 / 요약)을 고르고, 요약일 때만 가림 체크박스가 뜨며
  **기본은 꺼짐**이다 — 파일은 사용자가 고른 위치로 나가고, 위험은 저장이 아니라 나중에 그것을
  어딘가에 붙여넣는 순간에 있다.

## [3.6.0] — 2026-08-28

> 열린 목록에서 여기서 닫을 수 있는 것 전부. 넷 중 둘은 **픽스처가 못 닿던 화면을 프리뷰에
> 올려** 나왔다.

### Fixed
- **한국어 화면에 영어가 세 군데 섞여 있었다.** `NewNoteModal` 의 주 버튼이 *Create & Open*
  이었다 — 바로 옆 "취소"는 번역돼 있었다. 팔레트 그룹 헤더 아홉 중 하나가 하드코딩된
  `COMMANDS` 였고, 형제들은 "최근" · "노트" · "본문" 으로 나온다. `commandsHeaderLabel` 은
  코드에서 `"COMMANDS"` / `"QUICK ACTIONS"` 를 들고 있었다.

  타입 검사가 못 본다 — 문자열이 **문법적으로 멀쩡**하기 때문이다. 이제 마크업과 코드에서
  번역 안 된 UI 문자열을 훑는 가드가 있다. 예외 목록은 고유명사·약어로만 제한한다.

- **로케일 파일이 갈릴 수 없다.** 한쪽에만 있는 키는 다른 로케일에서 빈 문자열이나 키 이름으로
  조용히 새어 나온다.

### Added
- **dev 픽스처가 화면 넷에 더 닿는다.** git 이력 · `.lapis/templates/` 의 템플릿 둘 ·
  일부러 갈라 둔 `status` 값 · 쓰기가 실패하는 경로를 담았다. 전부 **조건이 맞아야만 뜨는
  화면**을 실제로 보기 위한 것이다: 최근 변경 탭 · 템플릿 고르기 · frontmatter 갈림 감사 ·
  실패 배너.

  ⚠️ 갈라 둔 값은 `완료됨` 이 아니라 `구현 완료` 다. 감사는 공통 부분 뒤에 **낱말 경계**를
  요구한다 — `완료됨` 은 한 낱말이라 일부러 보고하지 않는다(안 그러면 `완료`/`미완료` 를 같은
  값이라고 한다). 픽스처 주석에 적어 뒀다. "오타를 고치면" 그 화면이 조용히 비기 때문이다.

- **실패 배너에 컴포넌트 테스트가 생겼다.** 스토어는 전부 덮여 있었는데 **그리는 쪽**은
  아무도 안 봤다 — 이 저장소에서 반복된 실패다. 열 건이 못 박는다: 비었을 때 자리를 안 차지할
  것 · 자세한 내용은 접혀 있을 것 · 닫기가 그 건만 지울 것 · 읽을 수 있는 이름을 가질 것.

- **내보내기의 캔버스 없는 알맹이를 테스트할 수 있다.** 배율 클램프는 `exportGeometry.ts` 로,
  앱 전용 노드 제거는 `previewExportDoc.ts` 로 갈랐다. 둘 다 캔버스가 붙은 채로는 happy-dom
  에서 **"안 돌았는데 통과"** 가 돼 손댈 수 없었다.

  노드 제거는 보기보다 중요하다 — 내보내기 전에 문서 내 검색 하이라이트를 벗긴다. 빠뜨리면
  **내보낸 순간 무엇을 찾고 있었는지가 파일에 남는다.** 에러는 나지 않는다.

## [3.5.2] — 2026-08-28

> 테스트가 아니라 **앱을 보다가** 나온 것들.

### Fixed
- 🔴 **내림차순에서 빈 칸이 맨 위로 왔다.** `compareCells` 의 주석은 *"빈 칸은 항상 뒤로"* 라고
  못 박는데, `sortedOrder` 가 비교 결과 **전체**에 방향 부호를 곱하면서 그 규칙까지 뒤집었다.
  값이 듬성한 열을 내림차순으로 정렬하면 표 위쪽이 빈 행으로 찼다.

  `tableView.ts` 는 같은 규칙을 근거까지 달아 제대로 지키고 있었다 — 실제 vault 에서 `related`
  는 3%, `description` 은 1% 만 값이 있어 대부분의 열이 뒤집으면 그 꼴이 된다. **같은 규칙이 두
  군데 적혀 있고 한쪽만 맞았다.** 이 저장소가 반복해서 만드는 결함 모양이라, 두 정렬기를 같은
  질문 앞에 세우는 테스트를 새로 넣었다.

- **표 머리글을 키보드로 정렬할 수 없었다.** 클릭 리스너만 있고 `tabindex` 도 컨트롤도 없어서,
  마우스가 없으면 이 기능에 **닿을 방법이 아예 없었다** — 나머지가 전부 단축키로 도는 앱에서.
  이제 머리글 안에 초점 받는 버튼이 있다. 핸들러는 칸에 그대로 둬서 머리글 전체가 여전히
  누를 수 있는 자리다.

- **정렬 상태가 보조기술에 안 보였다.** 이미 그리고 있던 화살표와 짝이 되도록 머리글이
  `aria-sort`(`none` → `ascending` → `descending`)를 낸다.

- **모달 셋이 닫기 버튼을 "✕" 라고 읽었다.** `aria-label="✕"` 는 라벨을 붙인 것처럼 보이고
  자동 검사도 통과시키지만, 화면 낭독기는 그 기호를 그대로 읽는다. 찾기·태그 이름 변경·vault
  진단이 이제 번역된 "닫기" 를 쓴다 — 나머지 모달은 이미 그러고 있었다.

## [3.5.1] — 2026-08-28

> **에러 메시지가 전혀 없던** 결함 셋과, 앱을 실제로 눈으로 볼 방법.

### Fixed
- 🔴 **창이 안 보이면 인덱스 빌드가 영원히 멈췄다** — `requestAnimationFrame` 은 창이 가려지거나
  최소화되면 한 번도 오지 않는데, 같은 청크 양보 헬퍼의 사본 셋(`linkIndex` · `relations` ·
  `stores/vault`)이 그것을 그냥 기다렸다. 백그라운드 탭에서 실측: 1.5초 동안 rAF 콜백 0회,
  빌드는 첫 청크 경계에서 멈춘 채 에러도 타임아웃도 로그도 없이 "인덱스 만드는 중…" 오버레이만
  남았다.

  헬퍼를 한 곳(`yieldToPaint.ts`)으로 모으고 **rAF 를 타이머와 경주**시킨다. 보이는 창은 그대로
  paint 를 기다리므로(rAF 는 ~16ms, 상한 100ms 안쪽) 스피너가 계속 갱신되고, 가려진 창은 곧장
  넘어간다.

- 🔴 **폴더 축과 팔레트 스코프 칩이 한 번도 안 떴다** — `scopeOptions` 는 깊이 2까지의 디렉터리
  접두사를 내는데 호출부 둘이 **절대경로**를 넘긴다. 드라이브와 홈이 깊이 예산을 다 먹어 모든
  후보가 전체 노트를 덮었고, "하나라도 걸러야 후보다" 규칙에 걸려 전부 사라졌다. 단위 테스트는
  vault 상대경로를 먹였기 때문에 **함수는 통과하고 기능만 비어 있었다.**

  이제 모든 경로가 공유하는 뿌리를 걷어내고, 매칭용 `prefix` 옆에 화면용 `label`(vault 아래)을
  같이 낸다 — 한 함수가 둘 다 가지므로 갈릴 수 없다.

- **필터 칩에 선택 표시가 없었다** — 활성 스타일이 칩 종류마다 따로 적혀 있어서 나중에 붙은
  축(3.1.0 폴더, 3.3.0 임의 frontmatter)에는 조용히 없었다. 이제 `.facet-chip.active` 기본
  규칙 위에 종류별 강조를 얹고, 모든 칩 변종에 활성 표시가 있는지 테스트가 요구한다.

- **dev 가짜 백엔드가 테스트에서 잡히지 않는다** — vitest 도 `DEV` 이고 Tauri 밖이라, Tauri
  코어를 목킹한 테스트가 목 대신 픽스처를 물고 있었다. 픽스처는 그럴듯한 값을 주므로
  **아무것도 관찰하지 않은 채 단언이 통과했다.**

### Added
- **개발 중 앱이 보통 브라우저에서 돈다**(`npm run dev`). 메모리 안의 픽스처 vault 를 상대로
  돌기 때문에 필터 패널·표·진단을 직접 눈으로 볼 수 있다. 상태바에 `FIXTURE` 배지가 뜨고,
  모르는 명령은 `undefined` 대신 던지며, 프로덕션 빌드 산출물을 훑어 이것이 배포되지 않음을
  테스트가 증명한다.

## [3.5.0] — 2026-08-28

> 4차 분석의 **마지막** 묶음(8 · 9 · 10 · 11 · 12 · 13 · 17). 이로써 21 후보가 다 끝났다.

### Added
- 🔴 **실패를 화면에 띄운다** ([#278]). v3.2.0 이 96곳을 `logError` 로 구조화했지만 그건
  **기록**이다 — 릴리스에는 devtools 가 없어 사용자는 여전히 아무것도 못 봤다.

  `stores/vault.ts` 가 이렇게 적고 있었다: *"이 경로에는 아직 화면 오류 표면이 없다."*

  ⚠️ **되돌릴 수 없는 쓰기의 실패만** 온다 — 인용 갱신 · 충돌 해결 · 자동 커밋.
  96곳을 전부 띄우면 배너가 상시가 되고, 상시가 된 경고는 아무도 안 읽는다.

  ⚠️ **토스트가 아니라 배너**다. 토스트는 사라지는데, 이름은 바뀌고 인용은 안 바뀐 상태를
  자리 비운 사이에 놓치면 영영 모른다. 사용자가 **직접 닫아야** 사라진다.

  ⚠️ 같은 키는 하나만 남는다. 자동 커밋이 계속 실패하면 같은 줄이 쌓이는데 그건 정보가
  아니라 소음이다.

- **새 노트 템플릿** ([#278]). `NewNoteModal` 이 "Phase 4.2에서 확장 예정"이라고 적어
  두고 있던 것. 실측이 값어치를 말한다 — 이 vault 는 프론트매터 규약이 **100%**
  (`title`·`doc_kind`·`topic`·`tags` 가 112/112)라 매번 손으로 적고 있었다.

  ⚠️ 자리는 **vault 안**(`.lapis/templates/`)이다. 앱 설정에 담으면 vault 를 옮길 때
  안 따라간다 — 노트가 파일시스템에 그대로 있는 것이 이 앱의 전제다.

  ⚠️ 치환은 **날짜와 제목뿐**이다. 표현식·조건을 넣으면 그건 템플릿 엔진이고 엔진은
  조용히 틀린다. **모르는 이름은 그대로 둔다** — 오타(`{{titel}}`)가 사라지지 않고 눈에 보인다.

- **커밋 시점의 노트 내용** ([#278]). 이력에서 "이 시점 내용"을 누르면 그때의 본문을
  보여주고 복사할 수 있다.

  ⚠️ **되돌리기가 아니다.** `git checkout` 은 작업 트리를 바꾸는 되돌릴 수 없는 쓰기다.
  옛 내용을 보여주고 복사까지만 한다 — 다른 감사들과 같은 태도다.

  ⚠️ 커밋 해시만 받는다. 임의 문자열을 `git show` 에 넘기면 `HEAD~3:../..` 같은 것으로
  **vault 밖을 읽는다** — 경로는 막혀 있지만 revision 쪽은 안 막힌다.

- **vault 전체 최근 변경** ([#278]). 진단 모달의 일곱째 탭. "오늘 뭐가 바뀌었나"에
  답한다 — 노트별 이력(컨텍스트 패널)과 **다른 질문**이다.

- **자동 커밋의 흔적** ([#278]). 상태바에 마지막 커밋. 4초 뒤 조용히 도는 것이라,
  흔적이 없으면 **잘 도는 것과 안 도는 것이 화면에서 같아 보인다.**

  ⚠️ **실제로 커밋됐을 때만** 남긴다. 변경이 없으면 backend 가 no-op 인데 그때도 갱신하면
  "방금 커밋했다"는 거짓말이 된다.

### Changed
- 🔴 **한글에서 낱말 단위 검색이 제대로 갈린다** ([#278]). `고양이` 가 `검은고양이`
  안에서는 안 잡힌다.

  세 단계를 거쳤다: v3.1.1 이전엔 **0건**(ASCII `\b` 라 한글에는 경계가 아예 없다) →
  v3.1.1 에서 0건은 면했지만 경계가 없어 부분 문자열로 잡힘 → 지금은 유니코드 낱말
  문자 lookbehind.

  ⚠️ **기능 검사로 감쌌다.** lookbehind 를 못 파싱하는 엔진에서는 `new RegExp` 가 던지고,
  그러면 정규식이 `null` 이 되어 **검색이 통째로 죽는다**(v3.1.1 에서 고친 증상과 같아진다).
  못 파싱하면 2단계로 떨어지고, **0건이 되는 1단계로는 절대 안 돌아간다.**

  ⚠️ `\p{...}` 는 `u` 플래그가 있어야 뜻이 있다. 없으면 `p` 한 글자로 읽혀 조용히 다른
  것을 찾는다. 진짜 경계를 쓸 때만 켠다 — regex 모드에 켜면 예전에 되던 패턴이 깨진다.

### 17 · 안 만들기로 한 것
`lapis doctor` 를 앱에 옮기는 것은 **안 한다.** doctor 가 답하는 것(캐시 버전 · 경로 ·
스키마 편차)은 **앱이 못 뜰 때** 물어야 하는 질문이다. 앱 안에 두면 정작 필요할 때 못 쓴다 —
CLI 에 있는 것이 맞는 자리다. 근거를 계획서에 적었다.

## [3.4.0] — 2026-08-28

> 4차 분석의 **본문** 묶음(2 · 3 · 4 · 5 · 7). 코퍼스가 가장 크게 가리킨 것들이다.

### Added
- **작업 목록이 체크박스로 보인다** ([#277]). markdown-it 코어에는 작업 목록이 없어서
  `- [ ] 할 일` 이 **글자 그대로** `[ ] 할 일` 로 보였다.

  실측: 미완 **90** · 완료 **30** 이 5노트에 있다 — 적지 않은데 전부 대괄호였다.

  ⚠️ **읽기 전용**이다. 클릭으로 파일을 고치는 것은 되돌릴 수 없는 쓰기이고
  `README` 가 "쓰기 도구가 아니다"라고 못 박았다. 눌러도 아무 일이 없으면 고장처럼
  보이므로 `title` 로 이유를 남긴다.

  ⚠️ 인라인 토큰을 **다시 만들지 않는다.** 새로 파싱하면 위키링크·강조가 한 번 더
  처리되거나 아예 안 된다 — 둘 다 조용히 틀린다.

- **미완 작업 감사** ([#277]). vault 진단의 **여섯째**. 표면 셋 전부
  (앱 · `lapis tasks audit` · MCP `audit: "tasks"`).

  실측 vault 에서 **88건 미완 · 23건 완료**, 그중 67건이 한 노트에 몰려 있었다 —
  그 노트를 열기 전에는 알 방법이 없었다.

  ⚠️ **코드 블록 안은 안 센다.** 이 vault 는 코드블록이 63노트에 있고 셸 예시에
  `- [ ]` 가 들어간다. 세면 **있지도 않은 할 일**이 목록에 뜨고, 그러면 목록을 못 믿게 된다.

  ⚠️ `unlinked` 와 같이 **본문을 전부 읽는다.** 그래서 탭을 열 때만 돈다.

- **표를 눌러 정렬하고 복사한다** ([#277]). 머리글 클릭으로 원문 → 오름 → 내림,
  마크다운·CSV 복사.

  실측: 표가 **95/112 노트(85%)** 에 있다 — 이 vault 의 지배적 구조인데 스타일만 있고
  아무 기능이 없었다.

  ⚠️ **원문을 안 고친다.** 정렬은 보기만 바꾼다 — 새로고침하면 원래 순서로 돌아오는데
  그게 맞는 동작이다.

  ⚠️ 숫자처럼 보이는 칸은 **숫자로** 센다. 문자열로 비교하면 `10` 이 `9` 앞에 오고,
  그건 표를 정렬한 사람이 바로 알아채는 오답이다.

  ⚠️ 정렬한 **뒤에** 복사하면 보이는 순서로 나온다. 화면과 다른 것을 복사하면 거짓말이다.

- **코드블록 복사 버튼** ([#277]). 실측 언어 분포 `bash:33 · ts:12 · json:5`.

### 이미 돼 있던 것
7번(넓은 표 가로 처리)은 `rendered.css` 가 이미 `overflow-x: auto` 로 처리하고 있었다 —
분석이 코드를 안 보고 넣은 항목이었다.

## [3.3.0] — 2026-08-28

> 4차 분석의 **축과 검색** 묶음(1 · 6 · 14 · 15 · 16). 첫 항목은 **3차에서 계획해 놓고
> 빠뜨린 것**이다.

### Added
- 🔴 **임의 frontmatter 축으로 거른다** ([#276]). `status` 같은 축이 필터 뷰의 칩이 된다.

  3차 계획서가 `PR-C` 로 적어 뒀는데 폴더 축만 넣은 채 끝났다. 측정은 그대로였다 —
  `status` 가 **44노트(41%)** 에 있고, 한눈에 보기에는 **컬럼으로 추가되는데 거를 수는
  없었다.** 보이는데 못 거르는 것은 반쪽이다.

  ⚠️ **아무 필드나 축이 되지 않는다.** `date` 는 96% 노트에 있지만 값이 전부 달라 칩이
  노트 수만큼 뜬다 — 하나를 고르면 노트 한 개가 남으니 그건 필터가 아니라 파일 열기다.
  감사(`vaultAudit`)가 "이건 열거형인가"에 쓰는 **같은 문턱**을 쓴다.

  ⚠️ 그 필드가 **없는** 노트는 빠진다 — `status` 없는 노트가 "완료"일 리 없다.

- **MCP·CLI 도 같은 축을 안다** ([#276]). `props: {status: ["완료"]}` · `--props status=완료`.
  `list: "fields"` 가 어떤 축이 있는지, `list: "<필드>"` 가 그 값을 낸다.

  ⚠️ MCP 가 앱의 `applyFilters` 를 **그대로 부른다.** 따로 구현하면 같은 질문에 두 표면이
  다른 답을 낸다 — 태그 접두사에서 실제로 그랬다(v3.1.2).

- **저장된 검색** ([#276]). `⌘S` 로 지금 검색을 저장하고, 질의가 빈 팔레트에서 고른다.

  ⚠️ **스코프를 같이 저장한다.** 질의만 담으면 다른 프로젝트를 보던 중에 불렀을 때
  엉뚱한 결과가 나오고, 그건 저장된 검색이 고장 난 것처럼 읽힌다.

- **"최근 바뀐"의 기준을 고른다** ([#276]). 설정 → vault. 파일 수정(mtime) 또는
  frontmatter `date`.

  CLI·MCP 는 `--by mtime|date` 로 **둘 다** 받는데 앱은 mtime 고정이었다.
  실측: `date` 가 있는 107노트 중 **42건이 mtime 과 날짜가 다르다** — 두 축은 이 vault
  에서 실제로 갈린다.

  ⚠️ `date` 축은 그 필드를 적은 노트만 대상이다. 없는 노트를 0으로 두면 목록 맨 뒤에
  몰려 뜻이 없어진다. 파싱은 CLI·MCP 가 쓰는 `parseFrontmatterDate` **같은 함수**다.

### Changed
- **팔레트 폴더 칩이 영속 스코프가 된다** ([#276]). 닫아도 남고, **모든 모드**에 걸리고,
  경로 있는 결과 전부에서 후보를 뽑는다.

  🔴 예전 칩은 **정확 일치**로 걸렀다 — 다른 표면은 전부 문자열 접두사인데(`inScope` ·
  `under` · `exclude`) 팔레트만 달랐다. `knowledge/lapis` 로 좁히면
  `knowledge/lapis/plans/a.md` 가 **빠졌다.** 결과는 나오고 에러는 없다.

  ⚠️ 남는 만큼 **항상 보인다.** 조용히 좁혀진 결과는 "왜 안 나오지"가 되고, 그건 검색이
  고장 난 것과 구별이 안 된다. 걸린 스코프를 한 줄로 그리고 한 번에 푼다.

  ⚠️ **경로가 없는 항목(명령·태그·facet)은 스코프가 안 지운다.** 폴더를 좁혔다고 `>`
  명령이 사라지면 스코프를 켠 사용자는 명령 팔레트를 못 쓴다.

### 안 만든 것 — 4차 분석의 18·19·20
외부 링크 미리보기(URL 있는 노트 **4%**) · 아웃라인 깊이 제어(h4 이상 **4노트**) ·
제목 충돌 해소 UI(겹치는 제목 **1건**, 파일명 동명 7쌍은 경로 표시로 이미 해결).

## [3.2.0] — 2026-08-28

> 4차 분석의 21번. 시작은 "메뉴 사용 통계"였는데, 재 보니 무게중심이 다른 데 있었다 —
> 이 앱은 **96곳에서 조용히 실패**하고 있었다.

### Added
- **사용 기록** ([#275]). 어떤 명령을 **어느 입구로** 쓰는지, 어디서 오류가 나는지를
  이 컴퓨터에만 남긴다. 설정 → 고급에서 켜고 끄고, 리포트로 내보내고, 지운다.

  ⚠️ **텔레메트리가 아니다.** `README` 가 "네트워크 코드는 존재하지 않는다"고 못 박았고
  이 기능도 예외가 아니다 — 쓰는 것은 디스크뿐이다.

  ⚠️ **vault 에 안 쓴다.** 로그를 vault 안에 두면 감시자가 돌아 **매 이벤트마다 재색인**
  하고 로그가 노트로 색인된다. `app_data_root/usage/YYYY-MM.jsonl` 에 쌓이고 dev/release
  분리를 그대로 따른다.

  **입구를 같이 남기는 것이 요점이다.** "파일 열기를 400번 했다"는 쓸모가 적고,
  "400번 중 380번이 `⌘P`, 레일은 3번"은 레일을 고칠지 말지를 정해 준다.
  한 번도 안 쓴 명령도 낸다 — 그건 로그에 **없는** 것이라 아는 명령 전체를 분모로 받는다.

- **오류 96곳이 구조화됐다** ([#275]). `console.error`/`console.warn` 이 전부
  `logError`/`logWarn` 이 된다(콘솔 출력은 겸한다 — dev 에서 여전히 제일 빠른 경로다).

  ⚠️ **릴리스 빌드에는 devtools 가 없다.** 지금까지 그 96곳은 아무도 안 읽는 곳에 쓰고
  있었다. `usageWiring.test.ts` 가 `console.error` 가 다시 생기는 것을 막는다.

- **리포트 내보내기** ([#275]). 마크다운으로. 기간 · 많이 쓰는 명령(입구 분포 포함) ·
  한 번도 안 쓴 명령 · 오류 빈도.

  ⚠️ **기본이 가림**이다. 경로는 마지막 조각만 남긴다 — 사고는 로그 원본이 아니라
  **문서를 붙여넣는 순간** 나고, 이 저장소는 공개다. 원본이 필요하면 "가리지 않고 저장"을
  **명시적으로** 눌러야 하고, 그 문서는 첫 줄에 경고를 달고 나온다.

### Fixed
- **vault 가 없으면 설정에 닿을 길이 없던 것** ([#275]). 설정은 레일 버튼으로만 열리는데
  레일은 vault 가 있어야 뜨고, 팔레트에도 설정 명령이 없다 — 언어·테마를 vault 를 열기
  전에는 못 바꿨다. 빈 상태에 설정 항목을 뒀다.

  (사용 기록을 화면에서 확인하다 발견했다.)

### 담는 것과 안 담는 것

| 담는다 | 안 담는다 |
|---|---|
| 명령 id · 입구 · 시각 | 노트 **본문** |
| 오류 자리 · 메시지 · 예외 · 경로 | 어디로도 **전송** |
| 세션 시작 · 버전 · 플랫폼 | |

## [3.1.2] — 2026-08-28

> 안 닿던 모듈 **나머지 전부**. 결함 둘이 더 나왔고 둘 다 **에러가 없다.**

### Fixed
- **앱과 MCP 가 같은 태그에 다른 답을 내던 것** ([#274]).

  MCP 의 태그 질의는 `n === t || n.startsWith(t + "/")` — **정확 일치 ∪ 하위**다.
  그런데 앱의 접두사 색인은 **자기 자신을 안 넣었다.** `feature` 를 고르면 앱은
  `feature/*` 만 주고 정확히 `feature` 로 태그된 노트는 **빠졌다.**

  ⚠️ 겸사로 딸려 있던 문제 — 태그가 **정확 태그이면서 동시에 상위 접두사**이면
  (`a/b` 가 그 자체로 붙어 있고 `a/b/c` 도 있는 경우) `TagPanel` 의 `isSubPrefix` 가
  false 가 되어 leaf 로 골랐고, 3단계 칩은 애초에 없어 **깊은 노트에 트리로 닿을 수
  없었다.** 이제 `prefixChildren` 이 모든 단계를 담고, 판정이 "자식이 있는가"다.

  `tagIndex.test.ts` 가 MCP 규칙을 그대로 재현해 **두 구현이 같은 집합을 내는지** 본다.
- **동명 노트가 백링크 발췌를 나눠 쓰던 것** ([#274]).

  캐시 키가 `${source}::${target의 stem}` 이었다. 발췌를 만드는 항목은 target 의
  **stem · title · aliases** 셋이라 stem 만으로는 target 을 구별 못 한다.

  이름이 같은 노트가 둘이면(이 vault 는 `audit: tags` 기준 **7쌍**) 같은 source 에서 두
  target 이 **같은 키**를 쓰고, 먼저 계산된 쪽이 이겨 두 번째 노트가 **남의 발췌**를
  보여줬다. 키를 target **경로**로 바꿨다.

### Added
- **남은 모듈 전부에 테스트** ([#274]) — 65건.
  `backlinks`(캐시 키) · `unread`(변경 판정 경계) · `inDocSearch`(인계 옵션을 영속화하지
  않는가) · `cssFormat`(빈 입력 · 멱등 · 파싱 실패) · `cli/appLaunch`(detached·unref·stdio) ·
  `cli/io`(**vault 이탈 차단 · 확장자 화이트리스트 · 원자적 쓰기**) ·
  `watcher`(외부 변경 충돌 해결 두 갈래).

  ⚠️ `cli/io` 의 이탈 차단은 **심링크로 밖을 가리키는 경우**까지 본다. 심링크를 못 만드는
  환경에서는 그 단언을 건너뛴다 — **못 만든 것을 통과로 세지 않는다.**

  ⚠️ `watcher` 의 "외부 변경 사용"이 **읽기에 실패하면 충돌을 안 닫는지**를 못 박았다.
  닫으면 사용자는 해결된 줄 알고 계속 편집하다가 다음 저장에서 남의 변경을 덮어쓴다.

### 테스트하지 않은 것 — 이유를 적는다
- `mermaidExport` · `previewExport` — 캔버스 렌더와 이미지 인라인이라 **실제 캔버스 없이는
  뜻 있는 단언을 못 만든다.** `previewExport` 의 순수 절반(`previewExportDoc`)은 이미
  덮여 있다.

## [3.1.1] — 2026-08-28

> 유닛 테스트 전수 점검. **테스트가 하나도 안 닿는 모듈이 열둘**이었고, 그중 셋에서
> 실제 결함이 나왔다. 셋 다 **에러 없이** 틀린다.

### Fixed
- **`.mmd` 노트 이름을 바꾸면 링크가 조용히 끊기던 것** ([#273]).

  `renamePath` 가 옛/새 stem 을 뽑아 링크 갱신에 넘기는데, stem 을 뽑는 코드가 **`.md` 만**
  벗겼다. `.mmd` 는 확장자가 남아 `"diagram.mmd"` 로 찾게 되고, 본문 위키링크는
  `[[diagram]]` 이라 **매칭 0건** → 미리보기 모달도 안 뜨고 **조용히 종료**한다.
  이름은 바뀌고 인용은 옛 이름을 가리킨 채 남는다.

  ⚠️ `vault.rs` 는 `.md` 와 `.mmd` 를 **둘 다** 노트로 다루고 이름 변경도 `.mmd` 를
  보존한다. 확장자를 벗기는 규칙만 코드베이스에 **세 가지**로 갈려 있었다:
  `previewExportDoc`(md·mmd·markdown) · `relations`(md·mmd) · 나머지 넷(**md 만**).

  이제 `notePath.ts` 의 `noteStem` 하나가 정의하고, `noteStem.test.ts` 가 `.md` 만 보는
  정규식이 다시 생기는 것을 막는다.
- **트리 필터에서 폴더 이름으로 찾으면 빈 폴더 한 줄이 뜨던 것** ([#273]).

  폴더 이름이 맞아도 자식을 필터 결과(=빈 배열)로 갈아 끼웠다. `plans` 를 치면
  **빈 `plans/`** 가 뜨고, `countMatches` 는 잎만 세므로 화면에는 **"0 matches"** 라고
  적혔다 — 줄은 보이는데 개수는 0이라 둘 중 하나가 거짓말이었다.

  폴더가 맞으면 **그 안이 곧 찾던 것**이므로 하위를 통째로 남긴다.
- **문서 내 검색의 "낱말 단위"가 한글에서 결과를 0건으로 만들던 것** ([#273]).

  `\b` 는 **ASCII 낱말 문자**와의 경계다. 한글은 낱말 문자가 아니라서 앞뒤가 둘 다
  비-낱말 → 경계가 **아예 없다**. `\b고양이\b` 는 `"고양이"` 에도 안 맞았다.

  "덜 걸린다"가 아니라 **검색이 죽었다.** 한글이 주 용도인 앱에서 그 옵션을 켜면 결과가
  통째로 비고, 에러도 안내도 없다.

  이제 literal 모드에서는 질의의 그 끝이 ASCII 낱말 문자일 때만 경계를 붙인다.
  ⚠️ 진짜 CJK 낱말 경계는 lookbehind 가 필요한데, 옛 WKWebView 가 그걸 못 파싱하면
  **정규식이 통째로 null 이 되어 지금 고친 증상과 똑같아진다.** 거기서 멈췄다.

### Added
- **테스트가 안 닿던 모듈에 테스트** ([#273]) — 60건. `layout`(마이그레이션 분기 셋) ·
  `treeFilter` · `previewHighlight` 의 정규식 조립 · `windowScope`(창별 키) ·
  `tagIndex`(중첩 접두사) · `noteStem`.
- **셸 치수 드리프트 가드** ([#273]). `--collapsed-strip-w`(CSS)와 `COLLAPSED_STRIP`(TS)이
  같은 숫자인지, 셸 높이가 밀도 블록에 안 들어갔는지 본다. 갈리면 접힌 폭과 스트립 버튼
  폭이 어긋나는데 둘 다 34 근처라 눈에 잘 안 띈다.

### 고치지 않은 것 — 잠재 결함으로 적어 둔다
- **태그가 정확 태그이면서 동시에 상위 접두사이면 깊은 노트에 못 닿는다.**
  `TagPanel` 의 `isSubPrefix` 가 그때 false 가 되어 leaf 로 고르고, 3단계 칩은 애초에
  없다. ⚠️ 2026-08-28 실측으로 이 vault 는 **깊이 3 태그가 0개**이고 이 형태도 0건이라
  아직 안 터진다. `tagIndex.test.ts` 가 조건이 만들어지는 것을 고정해 뒀다 — 고칠 때
  재현 지점이 된다.

## [3.1.0] — 2026-08-28

> 3차 코퍼스 분석(107노트)에서 나온 여섯. **감사를 실제로 돌려서** 무엇이 안 잡히는지부터 봤다.

### Added
- **폴더 스코프 — "이 아래에서만"** ([#272]). 표면 셋 전부: 앱 필터 패널의 **세 번째 축**,
  `lapis ... --under`, MCP `under`.

  측정: `audit: tags` 가 낸 이름 충돌 **7건이 전부** 한 vault 안의 두 프로젝트 사이였다
  (`state` · `feature-map` · `open-items` · `verification` · `shortcut-map` ·
  `autonomous-loop` · `열린 항목`). 검색 결과의 절반이 남의 프로젝트인데 **에러가 없다.**

  ⚠️ 두 프로젝트가 **같은 `doc_kind` 를 쓴다** — 그래서 기존 두 축(doc_kind·topic)으로는
  경계를 못 긋는다. 경로가 유일한 구분자다.

  ⚠️ **`exclude` 와 같은 문자열 접두사**다. 디렉터리 경계(`x + "/"`)로 맞추면
  `lapis/plans/lapis-cli-` 처럼 세그먼트 중간에서 끊는 접두사가 조용히 no-op 이 된다 —
  `exclude` 가 그 이유로 이미 문자열 접두사이고, 둘이 갈리면 같은 문자열이 한쪽에서만 먹는다.

  ⚠️ 겹치면 **`exclude` 가 이긴다.** 아카이브를 빼 두고 그 안을 스코프로 잡았을 때
  딸려 나오면 뺀 뜻이 사라진다.
- **`stale` 이 "이 답이 틀리나"를 말한다** ([#272]). `stale.affects_results` — 결과에 든
  경로 중 캐시보다 새로운 것이 있는가.

  측정: 실제로 11시간(39,541초) 뒤처져 19건이 새로웠다. 그런데 `newer_count` 는 **vault
  전체**를 말하지 이 질의를 말하지 않는다. 그 19건이 결과에 없으면 이 답은 멀쩡한데,
  개수만 보면 매번 의심하게 된다 — 의심이 상시가 되면 아무도 안 본다.

### Changed
- **프론트매터 감사가 뒤에서 겹치는 값을 잡는다** ([#272]). 새 종류 `suffix`.

  측정: `audit: props` 가 **0건**이었는데 `status` 는 이렇게 갈려 있었다 —
  `반영됨(19)` · `완료(10)` · `진행 중(10)` · `구현 완료(1)` · `해결됨(1)` · `닫힘(1)` ·
  `이전됨(1)` · `미착수(1)`. 여덟 중 다섯이 "끝났다"를 뜻한다.

  `prefix` 규칙이 **접두사만** 봤다 — `완료` 는 `구현 완료` 의 **접미사**다. 한정어는
  앞에 붙는 일이 많아서(구현 완료 · 부분 완료) 그 방향이 빠지면 절반을 놓친다.

  실제 vault 에서 이제 `구현 완료 ⊂ 완료` 와 `cross-platform ⊂ platform` 이 잡힌다.
- **축이 안 굳었다는 신호** ([#272]). 새 종류 `sparse` — 1회만 쓰인 값이 **셋 이상이고
  전체의 절반 이상**일 때.

  ⚠️ **동의어라고 말하지 않는다.** `반영됨`·`해결됨`·`닫힘`·`이전됨` 사이에는 공통 문자열이
  없고, 같은 뜻인지는 **기계가 정할 수 없다.** 이 기능의 원칙이 "판단하지 않는다"다.
  셀 수 있는 것만 말한다 — "여덟 종인데 다섯이 1회다".

  ⚠️ 건강한 축에는 안 울린다. 이 vault 의 `doc_kind` 는 여섯 종에 1회 값이 하나도 없다.
  거기서 울리면 감사 전체를 안 믿게 된다.

  ⚠️ 값 목록은 **여덟에서 자른다.** 안 자르면 `topic` 이 17줄 벽이 되고, 그 아래 진짜
  갈림(`suffix`)까지 같이 안 보인다. 자른 것은 "외 N개 더"로 **말한다.**
- **"백링크 없음" 목록에서 진입점이 뒤로 간다** ([#272]). 나가는 링크가 적은 것부터,
  같으면 경로순.

  측정: 이 vault 의 `HOME.md` 는 나가는 링크가 **19개**인데 아무도 안 가리킨다 —
  진입점이니 당연하다. 그게 경로순 정렬 때문에 목록 **맨 위**에 왔다. 첫 줄이 매번
  "고칠 것 아님"이면 목록 전체를 덜 보게 된다.

### 안 만든 것 — 코퍼스가 말했다

| | 측정 |
|---|---|
| 시간축 기능 더 | 107노트 **전부 1주 내**. 물 것이 없다 |
| 자산·첨부 | md 아닌 파일 **0개** |
| 접기·긴 문서 최적화 | 1,000줄 넘는 노트 **0개** |
| 이미지 · mermaid · 수식 | 여전히 **0** |
| 태그에 `sparse` | 74종 중 35종이 1회지만 **비율이 0.47** — 태그는 열린 축이라 설계상 롱테일이다. 울리면 노이즈다 |

### 틀렸던 가설 하나

`related:` 가 91% 노트에 있는데 `buildBacklinks` 는 본문 링크만 본다길래 "고아 오탐이
많겠다"고 봤다. 실제로 돌려 보니 **7건뿐**이었고 고아 판정은 프론트매터 관계도 세고
있었다. 주석 한 줄만 읽고 결론 낼 뻔했다.

## [3.0.2] — 2026-08-28

### Changed
- **용어 셋을 바꿨다** ([#270]). 화면에 보이는 말만 바뀐다 — **저장된 것도, 명령도,
  CLI 플래그도 그대로다.**

  | 전 | 후 |
  |---|---|
  | vault 위생 | **vault 진단** |
  | 고아 노트 | **백링크 없음** |
  | 테이블 뷰 | **한눈에 보기** |

  "위생"과 "정리"는 좋고 나쁨을 함의하는데, 이 화면은 **판단하지 않는다** — 찾아서
  보여줄 뿐 고치지 않는다. "고아"는 감정이 실린 비유였고, 실제로 재는 것은
  **들어오는 링크가 하나도 없다**는 사실 하나다. "테이블 뷰"는 번역투였다.

  ⚠️ **코드와 CLI 는 옛 이름을 쓴다.** `lapis links --orphans` 가 **API** 라서다 —
  플래그를 바꾸면 남의 스크립트가 깨진다. 코드만 UI 를 따라가면 CLI 만 옛 말을 써서
  셋이 갈리므로, 화면 하나만 바꾸고 대응표를 `vaultAudit.ts` 머리에 적었다.

### Fixed
- **한눈에 보기에서 "컬럼" 메뉴가 표 머리글에 가려지던 것** ([#270]).
  컬럼 메뉴와 sticky 헤더가 **둘 다 `z-index: 1`** 이었고, 같은 값이면 **DOM 순서가
  이긴다** — 표가 툴바보다 뒤에 있어서 헤더가 메뉴를 덮었다.

  메뉴는 열리고 항목도 다 있는데 첫 줄들만 헤더 뒤로 사라진다. 에러도 없고 콘솔도
  조용하다. `tableStacking.test.ts` 가 두 값의 순서를 못 박는다.

## [3.0.1] — 2026-08-28

### Fixed
- **커스텀 타이틀바에서 창이 안 움직이고 캡션 버튼이 안 눌리던 것** ([#269]).
  실물 Windows 창에서 잡혔다. 원인이 둘이고 **둘 다 에러 없이 같은 증상**을 만든다.

  1. **드래그 영역이 사실상 없었다.** 값 없는 `data-tauri-drag-region` 은 **그 요소를
     직접 클릭했을 때만** 먹는다 — 자식이 덮은 자리는 안 잡히는데, 40px 짜리 줄에서
     자식이 안 덮은 자리는 거의 없다. 게다가 그 속성이 가운데 트랙에만 붙어 있었다.
     이제 상단바 전체가 `deep` 이다.

     ⚠️ 컨트롤마다 no-drag 예외를 손으로 적지 않는다 — Tauri 가 button·link 같은
     **상호작용 role 을 기본으로 막는다.** 손으로 적으면 새 버튼마다 빼먹고, 빼먹은
     버튼만 안 눌린다.

  2. **캡션 버튼이 절반 높이였다.** `.titlebar` 가 `align-items: center` 라 세 섹션이
     내용 높이(~28px)만 차지했고, `height: 100%` 인 버튼이 그것을 기준으로 풀렸다.
     Windows 관례는 46×40 인데 46×28 이 나오고 있었다. 이제 46×40 이고 닫기 버튼이
     창 오른쪽 끝에 붙는다.

  더블클릭 최대화도 1번 때문에 안 됐다 — 잡을 면이 없으면 더블클릭도 그 면에 안 닿는다.
- **상단바에서 vault 메뉴를 열면 메뉴 여백을 눌러도 창이 끌리던 것** ([#269]).
  `deep` 은 하위 전체를 잡으므로 팝업이 자기 자리를 `data-tauri-drag-region="false"` 로
  막는다. 항목은 버튼이라 Tauri 가 알아서 막지만 `<ul>`·`<li>` 의 빈 자리는 아니다.

`titlebarChrome.test.ts` 가 넷을 못 박는다: `deep` 인지, 값 없는 속성이 남았는지,
섹션이 늘어나는지, 버튼 폭이 46px 리터럴인지(밀도 토큰으로 바꾸면 조밀 모드에서 좁아진다).

## [3.0.0] — 2026-08-28

> 셸 재설계(상단바 · 상태바 · 단일 뷰 사이드바 · 세그먼트 탭 · 밑줄 탭)와 색 토대는
> 아래 **[3.0.0-beta]** 절에 있다. 이 절은 그 위에 올라간 것들이다.

### Added
- **팔레트가 모드 넷을 든다** ([#268]). `⌘P`(파일) · `⌘⇧F`(본문) · `⌘K`가 **같은
  팔레트의 다른 모드**를 연다. **단축키는 그대로다.**

  `⌘K`는 **마지막으로 쓰던 모드**로 열린다. `⇥`로 전체 · 파일 · 본문 · 명령을 돈다.

  ⚠️ **`⇥`가 목록 이동에서 모드 순환으로 바뀐다.** v2까지 `Tab`은 `↓`의 별칭이었다.
  화살표가 그 일을 그대로 하므로 잃는 것은 없지만, 손이 기억하는 키다.

  ⚠️ `tag`(`#`)·`facet`(`:`)은 순환에 **없다** — 접두사로만 들어간다. `⌘K`도 그 둘은
  기억하지 않는다. 기억하면 다음에 입력창은 빈데 태그만 나오는 화면이 열린다.
- **전문 모드에 폴더 칩과 `rel` 점수** ([#268]). CLI·MCP가 이미 내던 값이다 —
  질의 안에서의 상대 점수로, 1위가 `1.00`.

  ⚠️ raw BM25를 그대로 내지 않는다. 질의마다 스케일이 달라(63 vs 1,494) 읽는 사람이
  질의를 가로질러 비교하게 된다.

  ⚠️ 칩은 **거르기 전** 결과에서 뽑는다. 거른 뒤에서 뽑으면 하나를 고르는 순간 나머지
  칩이 사라져 다른 폴더로 옮겨갈 수가 없다.
- **애니메이션 설정 3단** ([#268]). 설정 → 모양 → 시스템 / 최소 / 전체.

  ⚠️ **설정이 시스템보다 세다.** 시스템의 "동작 줄이기"만 보면 "이 앱에서만 줄이고
  싶다"와 "시스템에서는 줄여 뒀지만 여기서는 보고 싶다"를 둘 다 표현할 수 없다.

  ⚠️ **"최소"에서도 스피너와 진행 바는 돈다.** 작업 중을 알리는 기능 요소라, 멈추면
  "작업이 끝났다"로 읽힌다.
- **밀도가 3단** ([#268]). 여유 / 기본 / 조밀. v2.0.0의 2단에서 늘었다.

  ⚠️ **글자 크기는 세 단계에서 같다.** 가독성은 밀도의 대상이 아니다. 셸 치수와 창
  버튼도 밀도를 안 따른다 — OS 관례를 지켜야 하는 자리다.
- **빈 상태가 화면 전체로** ([#268]). vault 미선택 화면이 사이드바 안의 문구에서 화면
  전체가 됐다. **최근 vault 목록**(최대 5)이 생겨 창마다 다른 vault를 여는 경로가
  짧아진다.

  ⚠️ v2는 사이드바 안에만 문구를 두고 나머지(탭 스트립 · 노트 헤더 · 컨텍스트 패널)를
  평소대로 그렸다. 빈 사이드바 하나만 다른 화면은 "아직 안 골랐다"가 아니라 **"뭔가
  안 불러와졌다"** 로 읽힌다.
- **창 크롬 스위치 — Windows** ([#268]). 설정 → 모양 → 타이틀바. 켜면 상단바가
  타이틀바를 겸하고 캡션 버튼 셋(46×40, 닫기 hover `#c42b1c`)을 직접 그린다.

  ⚠️ **`tauri.conf.json`에 박지 않고 런타임에 끈다.** 설정 파일에 `decorations: false`를
  쓰면 프런트가 못 뜨는 상황에서도 장식 없는 창이 뜨는데, 그러면 창을 **옮길 수도 닫을
  수도 없다** — 캡션 버튼을 그릴 주체가 죽어 있기 때문이다.

  ⚠️ **macOS에서는 이 스위치가 없다.** 거기서 `setDecorations(false)`는 신호등까지
  없애 닫을 방법이 ⌘Q밖에 안 남는다. macOS가 원하는 것은 `titleBarStyle: "Overlay"`인데
  그건 빌드 시점 설정이라 런타임으로 켜고 끌 수 없다. 하지 않는 것을 할 수 있는 것처럼
  보이게 두지 않는다.

### Changed
- **모달 등장이 220ms, 퇴장이 140ms** ([#268]). 닫는 동작은 결과를 기다리는 동작이
  아니라 물러나는 동작이라, 같은 길이면 느리게 **느껴진다**. 카드가 `scale .97→1`에
  `y −8→0`을 더한다.
- **탭 밑줄과 읽기↔편집 세그먼트가 미끄러진다** ([#268]). 180ms. 밑줄은 탭마다 하나씩이
  아니라 **전체에 하나**이고 `transform`으로 옮긴다 — 본문 렌더를 기다리지 않는다.

  ⚠️ 자리는 측정해서 잡는다(탭은 드래그로 재정렬되고 가로로 스크롤된다). 측정에 실패하면
  **아무것도 안 그린다** — 틀린 자리에 줄이 남는 것보다 낫다.
- **테마 전환이 스냅샷 크로스페이드** ([#268]). 토큰 151곳에 `transition`을 걸지 않는다 —
  그러면 hover 하나에도 색이 늦게 따라온다. 대신 브라우저의 View Transition이 뜬 스냅샷
  한 장을 320ms 겹친다. 지원하지 않는 환경에서는 색이 즉시 바뀐다.
- **레일 툴팁에 250ms 지연** ([#268]). 없으면 레일을 스쳐 지나가는 것만으로 말풍선이
  여섯 개 연달아 튄다. **퇴장에는 지연이 없고**, 키보드 포커스에도 없다.
- **인덱싱 pill에 shimmer** ([#268]). 폭은 "얼마나 왔나"이고 반짝임은 "아직 돌고 있나"다.
  인덱스 빌드가 메인 스레드를 잡으면 폭이 한동안 멈추는데, 그때 멈춘 것과 끝난 것을
  가르는 게 이것이다.
- **vault 위생 모달이 좌측 목록으로** ([#268]). 가로 탭 다섯이 두 그룹으로 갈린다 —
  **그래프에서**(끊긴 링크 · 고아 · 태그 · 안 걸린 언급)와 **거를 수 있는
  축**(프론트매터). 다섯이 나란히 있으면 다 같은 종류로 읽히는데 실제로는 두 종류다.
- **사용자 정의 CSS 예시가 새 토큰으로** ([#268]). 액센트 예시가 **셋을 같이** 바꾸도록
  고쳤다 — 하나만 덮으면 나머지 둘이 옛 색으로 남아 링크만 다른 색이 된다.

### Fixed
- **"애니메이션 최소"에서 스피너가 멈추던 것** ([#268]). `[data-motion="minimal"]` 쪽
  리셋에 기능 요소 복원이 통째로 빠져 있었다.

  같이 재 보니 복원 목록 다섯 중 **넷이 앱 어디에도 없는 이름**이었다 — 옛 무한 슬라이딩
  바가 사라지면서 죽은 이름들인데 목록만 남아 있었다. 죽은 이름은 다음 사람에게 "이건
  지켜지고 있다"고 말하면서 아무것도 안 지킨다. `motionReset.test.ts`가 이제 두 목록의
  동기화와 **이름의 실재**를 둘 다 본다.

- **Windows 빌드가 프리릴리스 버전에서 죽던 것.** MSI 번들러는 프리릴리스 식별자가
  **숫자여야** 한다 — `3.0.0-beta` 에서 `beta` 를 거부하고 번들 전체가 실패했다.
  Windows 는 이제 NSIS(`.exe`) 만 만든다.

  ⚠️ 타깃을 `tauri.windows.conf.json` 으로 **Windows 에만** 건다. 공용
  `tauri.conf.json` 의 `"all"` 을 손대면 macOS 산출물까지 같이 바뀌는데, MSI 는
  Windows 에만 있는 타깃이라 거기서 뺄 이유가 없다.

### 안 한 것
- **패널 접힘/펼침 폭 애니메이션.** 두 가지로 시도했고 둘 다 실측으로 실패했다 —
  `transition: grid-template-columns`는 **최종값에 도달하지 못하고 멈췄고**(접혔는데 폭은
  그대로), `@property`로 등록한 길이는 값이 즉시 튀었다. 갓 만든 빈 요소에서도 같았다.

  폭은 즉시 바뀌고, 대신 **내용 페이드**(120ms, 등장 40ms 지연)를 넣었다 — 폭
  애니메이션으로 풀려던 문제("글자가 찌그러진다")를 실제로 푸는 쪽은 그것이다.
  자세한 것은 `app.css`의 "패널 폭" 블록.

### 토큰 이름 변경 (사용자 정의 CSS 영향)

| 옛 이름 | 새 이름 | 비고 |
|---|---|---|
| `--dur-fast` | `--dur-1` | 90ms. 옛 이름은 **한 릴리스만** 별칭으로 남는다 |
| `--dur-base` | `--dur-2` | 140ms |
| `--dur-slow` | `--dur-3` | 220ms |
| — | `--dur-4` | 320ms 신설 |
| `--border-*` | 이름 유지 | 값이 불투명 회색 → rgba 헤어라인 |
| — | `--accent-solid` · `--accent-text` | 신설 |
| — | `--n-250` · `--n-350` | 램프 단계 신설 |
| — | `--titlebar-h` · `--statusbar-h` · `--collapsed-strip-w` | 셸 치수 신설 |

`--accent`를 글자색으로 쓰던 사용자 CSS는 `--accent-text`로 옮긴다.
`data-lapis` 훅이 열다섯에서 **열일곱**이 됐다(`titlebar` · `vault-empty` 추가).

### 바뀌지 않은 것

단축키 표, 다크 단일 테마, "vault 위생은 판단하지 않는다", 치환의 파일 단위 적용,
프론트매터 `tags:`만 인덱싱, `mcp_enabled` 기본 꺼짐, 인덱스 생산자는 Rust 하나.

⚠️ **`CACHE_VERSION`은 그대로다(7). 이 릴리스는 재색인을 유발하지 않는다.**
UI 개편은 캐시를 무효화할 이유가 없다.

## [3.0.0-beta] — 2026-08-28

> **프리릴리스.** 3.0 개편의 **셸**까지다 — 색 토대와 공간 골격. 팔레트 검색 모드,
> 모달 세 종의 레이아웃, 모션 마감, 빈 상태는 v3.0.0 에 들어간다.
>
> ⚠️ 창 크롬(타이틀바 네이티브 장식 제거)은 **아직 안 들어갔다.** 네이티브 창을
> 띄워보지 않고는 검증할 수 없는 변경이라 v3.0.0 에서 설정 스위치 뒤에 둔다.

### Changed
- **사이드바가 한 번에 한 뷰를 보여준다** ([#265]). 세로로 쌓인 아코디언 넷이 레일이 고르는
  **단일 뷰**가 된다(VS Code 액티비티 바).

  v2.0.0 주석은 이미 "VS Code 액티비티바 관용구"라고 적고 있었는데 실제 동작은 섹션
  토글이었다. 3.0 은 적혀 있던 대로 만든다 — 다른 아이콘은 **전환**, 활성 아이콘은
  **접기**. 레일의 접기 버튼이 사라진다(재클릭이 그 일을 한다). ⌘B 는 그대로다.

  레일에 뷰 둘이 는다: 테이블과 vault 위생. ⚠️ 둘은 사이드바 뷰가 아니라 **모달**이라,
  레일이 모달을 열고 사이드바는 건드리지 않는다 — 뷰 상태로 넘기면 **빈 사이드바**가 남는다.

  ⚠️ 섹션 높이가 사라지고 저장돼 있던 값도 같이 간다. 260px 를 넷이 나눠 쓰니 **어느
  것도 제 높이를 못 가졌고**, 높이 드래그는 그 문제를 푸는 대신 사용자에게 떠넘긴 장치였다.

  ⚠️ **접어도 사이드바를 언마운트하지 않는다.** 폭 0 + 언마운트는 펼칠 때마다 트리를 새로
  그렸다. 이제 34px 스트립이고 무거운 내용만 조건 렌더다.
- **컨텍스트 패널이 세그먼트 탭이 된다** ([#265]). 300px 에서 같은 이유다. 증상은 목차가
  "문서마다 길이 편차가 커서" 기본 접힘이었던 것 — 그건 목차의 성질이 아니라 **넷이 한
  칸을 두고 다투는 구조**의 성질이다.

  탭 내용은 손대지 않았다. 컨테이너만 바뀐다.
- **탭이 폴더형에서 밑줄형으로** ([#265]). 칩은 활성 탭이 본문 면으로 갈아타면서 아래와
  이어지는 효과였는데, 3.0 은 면끼리 명암차가 작아 그 이음매가 안 읽힌다. 밑줄은 면에
  기대지 않는다.

  드래그 재정렬 · 컨텍스트 메뉴 · 핀 · dirty 점은 그대로다.
- **셸에 상단바와 상태바가 생긴다** ([#264]). 창 폭을 가로지르는 두 줄이고, **노트가 아니라
  창이 소유한다.**

  v2.0.0 은 디스코드에 전역 상단바가 없다는 이유로 그것을 노트 헤더에 합쳤다. 그런데
  디스코드에 없는 것이 lapis 에는 있다 — vault, 인덱싱, 창 하나에 vault 하나. 셋 다
  **창에 딸린 것**이고, 노트 헤더는 노트를 안 열었을 때 그것들을 둘 자리가 없다.

  vault 메뉴 · ⌘K 커맨드바 · 인덱스 상태가 위로, 감시 상태 · 노트 수 · 문서 통계 ·
  현재 경로가 아래로 간다. 노트 헤더에는 **지금 보는 것의 이름**만 남는다.

  ⚠️ **인덱싱이 실측 진행을 보여준다.** 예전 strip 은 무한 슬라이딩 바였다 — "돌고 있다"만
  말하고 "얼마나 남았나"는 말해 주지 않는데, 12,000 노트에서는 그 차이가 전부다.
  `buildProgress` 가 done/total 을 줄곧 들고 있었고 화면이 안 읽고 있었다.

  ⚠️ 감시 상태와 노트 수가 사이드바 푸터에 있어서 **사이드바를 접으면 같이 사라졌다** —
  패널을 접었다고 감시가 멈춘 것도 아닌데.
- **`data-lapis="titlebar"` 가 사용자 CSS 계약에 들어간다** ([#264]).
- **액센트가 셋으로 나뉜다** ([#263]). 한 값으로 채움·글자·보더를 다 감당하면 반드시 한쪽이
  기준 아래로 떨어진다. 옛 Blurple 은 채움에 맞춰 고른 값인데 **글자로 3.7:1** 이었고,
  링크가 그걸 쓰고 있었다.

  `--accent-solid` 가 채우고, `--accent` 가 포커스 링·보더를 그리고, `--accent-text` 가
  글자다. `--danger`/`--danger-text` 와 같은 계약이고 가드도 같다 — `accentText.test.ts` 가
  `color: var(--accent)` 를 어디서든 막는다.

  ⚠️ 프리셋 26종은 나머지 둘을 **파생한다.** 테마마다 손으로 적으면 스물일곱 번째에서
  빼먹고, 빼먹으면 그 테마만 링크가 기본 파랑으로 남는다 — 화면은 멀쩡하고 한 요소만
  틀린다. 파생은 **밝기 한 축**만 움직인다. 색상은 테마의 정체성이라 그걸 흔들면 26종이
  서로 닮아 간다.

  ⚠️ 고정 −12% 로는 `teal` 이 **4.4994:1** 이었다 — 천분의 육으로 미달, 2.1.0 의
  `--n-700` 과 같은 모양이다. 이제 여유가 생길 때까지 어둡게 한다. 26종 전부를 **그 테마의
  물든 면 위에서** 잰다.
- **새 색 토대 — Ink & Pyrite** ([#263]). 중립 램프가 무채색 9단에서 **아주 옅게 파랑으로
  기운 11단**이 되고, 셸 3계층과 카드와 hover 가 한 단계를 나눠 쓰던 자리에 두 단계를 더
  넣었다. 다크 단일 테마는 그대로다.

  ⚠️ **보더가 불투명 회색에서 rgba 헤어라인으로 바뀐다.** 면끼리의 명암차가 이미 셸 영역을
  가르므로, 보더는 **같은 면 안의** 카드·입력 구분에만 남는다. `--border-*` 위에 자기 색을
  얹던 사용자 CSS 는 결과가 달라진다.

  ⚠️ **오버레이 계층이 반전됐다.** 팝오버·모달이 배경보다 어두웠는데(디스코드 관용구),
  스크림을 쓰는 이상 카드는 배경보다 **밝아야** 뜬다.

  새 본문 면에서 잰 값: 본문 11.93:1 · 보조 8.53 · 흐림 5.74 · 액센트 글자 5.72 ·
  위험 글자 6.29.
- **모션 예산을 넓혔다** ([#263]). `--dur-fast/base/slow`(100/150/200) 가
  `--dur-1..4`(90/140/220/320) 가 된다. 옛 이름은 **한 릴리스만** 별칭으로 남는다.

  ⚠️ `motion.ts` 가 Svelte transition 용으로 같은 숫자를 든다. CSS 의
  `prefers-reduced-motion` 이 거기까지 못 닿기 때문이다 — 한쪽만 바꾸면 같은 동작이
  CSS 로 그려질 때와 JS 로 그려질 때 속도가 어긋난다.

## [2.4.1] — 2026-08-27

### Fixed
- **MCP 질의를 켜도 조용히 아무 일도 안 할 수 있었다** ([#261]). 결함이 둘이고, 어느 쪽이든
  "켰는데 안 된다"를 만든다:

  `setMcp` 이 새 값과 store 가 같으면 조기 반환했다 — store 와 파일이 어긋나면 **되돌릴
  유일한 조작이 쓰지도, 말하지도 않았다.** 화면은 이미 그 항목이 활성으로 보인다.

  그리고 쓰기가 닿았는지 아무도 확인하지 않았다. `settingsWrite` 가 던지지 않으면 성공으로
  봤는데, 쓰기는 **다른 파일에 성공**할 수 있다(dev/릴리즈 분기) — 그건 예외가 아니라 정상
  종료다. 다시 읽어 보는 것으로만 잡히므로 `patchSettings` 가 그렇게 하고, 값이 안 남았으면
  던진다. ⚠️ 그때 store 는 그대로 둔다 — 갱신하면 지금 진단하던 바로 그 상태, **화면만
  바뀌고 디스크는 그대로**가 된다.
- **설정 화면이 MCP 가 실제로 읽는 파일을 보여준다** ([#261]). dev 빌드는 `-dev` 디렉터리에
  쓰는데 MCP 게이트는 릴리즈 파일을 먼저 본다. 그래서 dev 빌드에서 MCP 를 켜면 MCP 에
  닿지 않는다 — **정상 동작인데 진짜 결함과 증상이 똑같고**, 둘을 구분할 방법이 없었다.
  이제 MCP 항목이 두 경로를 같이 내고, 갈렸으면 경고색으로 말한다.
- **속성 감사가 개수를 두 배로 셌다** ([#261]). Rust 는 frontmatter 의 모든 키를 `props` 에
  담는다 — 타입 있는 `doc_kind`·`topic` 도 거기 들어간다. 감사가 양쪽을 다 세서 `todo 6 ·
  todos 4` 라고 했는데 진짜는 `3 · 2` 였다. 어느 값이 갈렸는지는 맞았고 **숫자만**
  거짓말이었다 — 그래서 아무도 안 의심했다.
- **노트 자기 h1 이 남의 언급으로 잡혔다** ([#261]). 두 프로젝트가 같은 개념을 각자 문서로
  두면 제목이 겹치고, 한쪽의 h1 이 다른 쪽 frontmatter title 과 같아진다. 감사는 그걸
  언급이라고 했다. 본문 첫 h1 은 그 노트 **자신의 이름**이라 이제 덮는다 — `title:` 을
  덮는 것과 같은 이유다.

  이걸로 이 감사가 **실측으로** 걷어낸 오탐 종류가 셋이 됐다. 셋 다 책상에서 안 나왔다.

## [2.4.0] — 2026-08-27

### Added
- **MCP 가 vault 위생을 물을 수 있다** ([#259]). 감사가 앱에 다섯 · CLI에 다섯 ·
  **MCP에 0개**였다 — 도우미가 vault를 검색은 하는데 **무엇이 깨졌는지**는 못 물었다.
  `lapis_query` 가 `audit: "broken" | "orphans" | "unlinked" | "tags" | "props"` 를 받는다.

  앱·CLI가 부르는 **같은 순수 함수**를 부른다. 여기에 판정을 따로 두면 도우미가 보는 상태와
  사람이 보는 상태가 갈리고, 그러면 누가 맞는지 알 방법이 없다.

  ⚠️ `all` 은 없다. `unlinked` 만 본문을 전부 읽어서, `all` 에 넣으면 값싼 넷을 물을 때마다
  그 비용을 문다 — 넣지 않으면서 `all` 이라 부르면 거짓말이다.
- **`lapis export --all --out-dir <디렉터리>`** ([#259]). vault 전체를 **구조 그대로** 낸다.
  평평하게 펴면 `a/노트.md` 와 `b/노트.md` 가 한 디렉터리에서 같은 이름을 놓고 다투고,
  진 쪽은 말없이 사라진다.

  `--out-dir` 없는 `--all` 은 거절한다. 수백 장을 표준출력으로 이어 붙인 것은 문서가 아니고,
  그걸 조용히 하느니 멈추는 편이 낫다.
- **팔레트가 감사 다섯에 전부 닿는다** ([#259]). 명령 하나가 위생 화면을 첫 탭으로 열어서,
  "속성"까지 가려면 클릭이 둘 더 들었다. 이제 감사마다 항목이 있다.
- **위키링크 헤딩 자동완성** ([#258]). `[[노트#` 를 치면 그 노트의 헤딩이 뜬다.

  ⚠️ 그 전에는 아무것도 안 하는 정도가 아니라 **반대로** 동작했다 — 트리거가 `#` 을 이름의
  일부로 봐서 `[[boundary-contracts#` 이 그 이름의 노트를 찾았고 **목록이 비었다.** 2.2.0이
  문법을 넣고 **치는 방법**을 안 넣었다. 헤딩 이름을 정확히 외워야 했다.

  `[[#헤딩]]` 은 **편집 중인 버퍼**에서 찾는다. 앵커를 다는 가장 흔한 순간이 "헤딩을 쓰고
  바로 가리키는" 때인데, 디스크를 읽으면 방금 친 헤딩이 후보에 없다.

  노트가 해소되지 않으면 이름 완성으로 떨어진다 — `[[C#` 은 여전히 `C#.md` 를 낸다.
  해소 규칙과 같은 우선순위다.
- **당겨온 조각이 어디서 왔는지 말한다** ([#258]). 테두리가 "남의 글"임은 알려줬지만
  **누구 것**인지는 말해 주지 않았다. 조각 위에 원본을 한 줄 놓았고, 그건 평범한
  위키링크다 — 클릭 경로를 하나 더 만들지 않고 있는 것을 탄다.

  실패한 임베드에는 안 붙인다. 갈 곳이 없다.
- **편집기가 새 문법을 안다** ([#258]). `> [!WARNING]` 과 `![[노트]]` 가 편집 중에는 평범한
  글자이고 미리보기에서만 색이 붙어서, `[!WARN]` 같은 오타를 모드를 바꾸고서야 알았다.

  ⚠️ **모르는 종류를 다르게 표시하는 것이 요점이다.** 아는 것만 칠하면 오타는 안 칠해진
  글자라 정상으로 읽힌다. 모르는 종류는 위험색 **에 더해** 물결 밑줄이 붙는다 — 색만
  다르면 "다른 색인가 보다" 하고 넘어간다.

## [2.3.1] — 2026-08-27

### Fixed
- **글자색 `--danger` 가 열다섯 곳에 남아 있었다** ([#256]). 2.3.0에서 문제를 재고 콜아웃만
  옮겼고, 나머지는 눈으로 확인하며 갈 몫으로 남겼다. 이번이 그 차례다.

  앱이 글자를 얹는 **네 면 전부**에서 재 보면 `--danger` 는 3.35:1 ~ 4.37:1 로 **넷 다 AA
  미달**이다. `--danger-text` 는 5.54 ~ 7.22 다. 테두리는 `--danger` 를 그대로 쓴다 —
  비문자 대비 기준은 3:1 이고 통과한다.

  ⚠️ 가드는 값을 옮겨 적지 않고 **`app.css` 를 읽어 실제 토큰을 푼다.** 손으로 적었다가
  `--surface-content` 를 엉뚱한 램프 단계로 알고 **통과가 아닌 것을 통과라고** 재고 있었다.
- **코드 블록에 배경이 없었다** ([#256]). `.rendered pre` 가 `--surface-sunken` 을 주는데,
  바로 다음 규칙 `.rendered .hljs { background: transparent }` 가 특이도로 이겼다 — 모든
  코드 펜스가 `<pre class="hljs">` 로 나오기 때문이다. 바로 위 주석은 "배경은 `pre` 규칙이
  담당"이라고 적고 있었다. 주석과 코드가 다른 말을 했고, 화면에는 테두리만 남았다.

  `transparent` 는 highlight.js **테마 스타일시트**가 얹는 배경을 지우는 관용구인데 이
  저장소는 그 테마를 안 쓴다. 없는 것을 막고 있었다.

## [2.3.0] — 2026-08-27

### Added
- **트랜스클루전** ([#254]). `![[노트]]` 는 그 노트 전체를 쓴 자리에 펼치고,
  `![[노트#헤딩]]` 은 그 절만 펼친다 — 헤딩 줄을 포함해서, 같거나 더 높은 레벨의 다음
  헤딩 앞까지.

  [#246]이 깔아 둔 것 위에 얹힌다 — 앵커가 해소돼야 절을 가리킬 수 있다.

  ⚠️ **실패는 자리에 남는다.** 임베드는 문장의 앞뒤와 이어져 있어서 빈 자리가 되면
  눈치채기 어렵다 — 원래 거기 뭐가 있었는지 말해 주는 게 없다. 없는 노트 · 없는 헤딩 ·
  순환 · 너무 깊음 각각이 **무엇을 못 가져왔는지** 한 줄로 남긴다.

  ⚠️ **순환과 깊이를 둘 다 막는다.** 하나만 있어도 다른 하나가 결국 멈추게 하지만, 둘은
  **다른 말을 한다.** 카나리아로 하나씩 꺼 봤더니 둘 다 멈추기는 했다 — 대신 틀린 문구가
  나왔다.

  앱은 렌더 후 DOM 을 훑어 채우고(노트 읽기가 IPC 인데 markdown-it 은 동기다), CLI 는
  문자열을 훑는다. **규칙은 공유한다** — 깊이 · 순환 · 실패 문구가 한 모듈에 있다. 거기가
  갈리면 같은 문서가 두 곳에서 다르게 읽힌다.

  ⚠️ **임베드는 제 줄에 두는 것을 전제한다.** 자리표시자가 인라인 룰에서 나오는 블록
  요소라, 같은 줄에 있는 글자는 문단 밖으로 밀린다.
- **`lapis export <노트>`** ([#253]). 자립 HTML 한 장을 터미널에서. 앱에는 2.0부터 있었는데,
  헤드리스 소비자가 사는 CLI에는 없었다.

  앱과 **같은 조립기**를 쓴다(`previewExportDoc.ts`). 다른 것은 재료를 어디서 얻느냐이고,
  그 차이는 숨기지 않고 적었다 — 앱은 mermaid 때문에 **라이브 DOM을 복제**한다(마운트 후
  런타임에 `<svg>`가 된다). CLI에는 브라우저가 없다. 그래서 mermaid는 코드 펜스로 남고,
  토큰 값은 `getComputedStyle` 대신 `app.css` + 설정 파일이 말하는 색 테마에서 온다.
  사용자 정의 CSS는 안 따라간다.

  ⚠️ 못 넣은 이미지는 **세어서 말한다.** 조용히 원본 경로를 남기면 "자립"이 거짓이 된다.
  원격 이미지는 일부러 그대로 둔다 — 받아오려다 실패하면 그림이 통째로 사라지는데 URL을
  남기면 온라인에서는 보인다.
- **설정 검색** ([#252]). 설정 헤더의 입력칸이 **카테고리를 가로질러** 찾는다 — 어느 카테고리에
  있는지 아는 것이야말로 찾을 때 없는 정보다. 결과마다 카테고리 이름을 같이 내고, 누르면
  그리로 간다.

  2.0.0에서 설정을 카테고리로 쪼개면서 잃은 것이 있다 — 한 줄로 나열하던 때는 브라우저의
  찾기로 됐다. 그걸 돌려준다.

  ⚠️ 카테고리 내용을 그 자리에서 거르지 않는다. 안 보는 카테고리는 **DOM에 없기** 때문이고,
  그 덕에 무거운 CodeMirror 편집기가 고급 탭을 열 때만 뜬다. 대신 결과 목록으로 갈아 끼운다.

  ⚠️ 그래서 설정 목록의 진실이 **둘**이 된다 — 마크업과 검색 색인. 항목을 더하고 색인에 안
  넣으면 **있는데 안 찾히는** 항목이 생긴다. 조용하고, 검색해 본 사람은 "그런 설정이 없나"로
  읽는다. 가드가 마크업을 읽어 제목이 전부 색인에 **맞는 카테고리로** 있는지 본다.

  띄어쓰기는 무시한다("사용자정의css"로 "사용자 정의 CSS"를 찾는다). 설명도 같이 본다 —
  사람은 이름을 정확히 기억하지 않는다.
- **콜아웃** ([#251]). `> [!NOTE]` · `[!TIP]` · `[!IMPORTANT]` · `[!WARNING]` · `[!CAUTION]`.

  먼저 쟀다: 이 vault의 41파일에 `⚠️ **…**` 로 **손으로 만든 콜아웃이 218곳** 있다. 문법이
  없어서 굵은 글씨와 이모지가 대신하고 있었다.

  ⚠️ **GitHub이 받는 다섯 종만 받는다.** Obsidian은 열둘 넘게 받지만 이 문서들은 GitHub에서도
  읽힌다 — 저장소가 공개고 README와 변경 기록이 거기서 렌더된다. 교집합 밖은 한쪽에서만 뜬다.
  모르는 종류를 평범한 인용문으로 남기는 것도 일부러다: `[!QUESTION]` 글자가 그대로 보여서
  쓴 사람이 왜 안 먹었는지 바로 안다. 조용히 삼키면 모른다.
- **frontmatter 위생 — 감사 계열의 다섯째** ([#250]). **거를 수 있는 축**의 값이 갈린 곳을
  찾는다. `lapis props audit` · vault 위생의 다섯째 탭 · `lapis doctor` 행. 앞의 넷은 링크
  그래프를 보고, 이건 축을 본다.

  ⚠️ **조용하다.** 질의가 에러를 내는 게 아니라 **절반만 찾는다.** 지금 실제 vault가 그렇다 —
  `doc_kind`에 `todo`와 `todos`가 같이 있고, `status`는 열두 종으로 갈렸다(`반영됨` 18 ·
  `완료` 2 · `해결됨` 1 · 그리고 `완료 — #232` 같은 자유 서술 여섯).

  판정 셋은 태그 감사가 태그에 하던 것과 같은 모양이다: 대소문자만 다름 · 단수/복수 ·
  앞부분이 같음.

  ⚠️ **무엇을 안 보는지가 요점이다.** 이름으로 빼는 필드는 둘뿐이다 — `tags`(태그 감사가
  본다)와 `aliases`(값이 이름이라 갈리는 게 정상이다). 나머지는 **모양으로** 거른다: 값이
  거의 반복되지 않는 필드는 자유 서술이지 열거형이 아니고, 값이 **노트로 해소되는** 필드는
  값이 아니라 상호참조다.

  마지막 규칙은 실측이 만들었다. 넣기 전 실제 vault 목록의 절반이 `related`였다 — `feeds` ·
  `feeds-excerpt-only` · `feeds-settings-hardening`. 셋은 **서로 다른 문서**고, 이름이 비슷한
  것은 문서 제목의 자연스러운 성질이다. 필드 이름 목록을 손으로 두었다면 새 필드가 생길
  때마다 고쳐야 했겠지만, **인덱스에 "이 값이 노트로 해소되나"를 묻는 것**은 그럴 필요가 없다.

  자유 서술을 오류라 부르지 않는다. `status: 완료 — #232`는 사람에게 유용하다. 감사가
  말하는 것은 **같은 접두사로 시작하는 값이 여럿**이라는 사실뿐이다.

### Fixed
- **CLI와 MCP 실행 래퍼가 Windows에서 안 돌았다** ([#249]). `cli/lapis`와 `mcp/lapis-*`는
  `#!/bin/sh` 스크립트다. PowerShell에서 `cli/lapis …`를 치면 **"이 파일을 열 앱을 고르라"는
  창**이 뜬다 — 에러도 아니고 지원 안 한다는 말도 없다. 편집기를 고르면 셸 스크립트 내용이
  보인다.

  `cli/README.md`는 Windows shim을 두지 않기로 적어 뒀었다 — *"유지할 진실이 하나 더 늘고,
  주 개발 환경은 macOS다"*. 그 논리가 현실에서 버티지 못했다. Windows는 이 저장소의 1급
  타깃이고(CI의 Rust 잡이 양쪽에서 돈다), 대안으로 제시한 "Git Bash를 쓰면 된다"도 기본값이
  아닌 것을 전제한다 — 기본 PowerShell `Path`에는 `sh`도 `bash`도 없다.

  이제 셸 래퍼마다 `.cmd` 짝이 있다. "유지할 진실이 하나 더"는 규칙이 아니라 **가드로**
  갚는다: `scripts/launchers.test.ts`가 sh 래퍼마다 `.cmd`가 있는지, **둘이 같은 진입점을**
  부르는지, 번들 러너 두 벌이 공유 계약 둘(`$lib` 별칭 · `LAPIS_REPO` 전달)을 지키는지 본다.
  다섯 번째 래퍼를 한쪽만 만들면 빨개진다.

  ⚠️ `.gitattributes`에 `*.cmd`를 CRLF로 못박았다. 저장소는 그 밖에서 전부 LF를 강제하는데,
  cmd.exe는 LF만 있는 배치 파일에서 **티 안 나게** 이상하게 군다.
- **네이티브 경로를 주면 내보낸 문서 제목에 전체 경로가 박혔다** ([#253]). `$lib`은 `/`
  구분자를 전제한다(`to_ui` 계약). CLI는 그 파이프라인 밖이다. 실제 호출은 캐시 경로라 이미
  정규화돼 있지만, Windows 경로가 들어오면 `<title>C:\\Users\\…\\note</title>` 가 나왔다 —
  문서는 멀쩡히 그려지고 탭 이름만 이상하다. 경계에서 한 번 정규화한다.
- **렌더 본문 스타일시트가 한 라우트에서만 들어왔다** ([#251]). 메인 페이지 컴포넌트에 있어서,
  컴포넌트 미리보기 라우트에서 마크다운을 그리면 **스타일이 하나도 안 붙었다** — 콜아웃 다섯
  종이 전부 똑같이 나왔고 에러는 어디에도 없었다. [#243]에서 고친 사용자 CSS 버그와 같은
  모양인데, 스타일시트 import는 그때 같이 안 옮겼다. 이제 루트 레이아웃에 있고 가드가 붙었다.

  브라우저에서 계산된 색을 읽어 보고 알았다. 깨진 동안 테스트는 전부 통과했다 — 구조를 보고
  있었고, happy-dom에는 레이아웃 엔진이 없다.
- **`--danger`는 글자색으로 쓰면 안 읽힌다** ([#251]). 본문 배경에서 3.35:1로 기준 4.5에
  못 미친다. 이 채도는 채움과 테두리에는 맞고 글자에는 안 맞는다 — 2.1.0의 `--n-700`과 같은
  종류다. `--danger-text`(5.54:1)를 팔레트에 넣고 caution 콜아웃이 그걸 쓴다.

  ⚠️ `color: var(--danger)`를 쓰는 곳이 여덟 군데 더 있다. 그쪽은 눈으로 확인하며 따로 옮긴다 —
  한 번에 바꾸면 무엇이 달라졌는지 아무도 못 본다.
- **액센트로 제목을 칠했다면 대부분의 테마에서 안 읽혔을 것이다** ([#251]). 기본 액센트에서
  자기 틴트 위 2.37:1이었고, 색 테마 26종이 각각 `--accent`를 딴 데로 옮기므로 그 숫자는
  복권이다. 그래서 `note`는 중립으로 두고 나머지 넷은 테마가 안 건드리는 상태색을 쓴다.
  실측 4.65~11.48:1로 전부 AA 위다.

## [2.2.0] — 2026-08-27

### Added
- **위키링크가 헤딩을 가리킬 수 있다** ([#246]). `[[노트#헤딩]]`은 그 노트를 열고 거기로
  스크롤한다. `[[#헤딩]]`은 지금 문서 안에서 움직인다. 마크다운 링크는 처음부터 앵커를 떼고
  해소했는데(Rust가 추출 단계에서 뗀다) 위키링크만 안 뗐다. 그래서 `[[노트#헤딩]]`은 아무
  데도 해소되지 않았다.

  그 고장은 **두 곳에서 동시에 조용했다.** 링크는 노트가 없는 것처럼 회색으로 보였고, 끊긴
  링크로 보고됐고, **백링크 간선이 사라졌다** — 대상 노트의 백링크 목록이 그냥 한 줄 짧고,
  왜 짧은지는 어디에도 안 나온다.

  ⚠️ **통째로 먼저 찾고, 없을 때만 앵커로 읽는다.** 파일 이름에 `#`이 들어갈 수 있어서다
  (`C#.md`). 반대 순서면 지금 잘 가던 `[[C#]]`이 조용히 `C.md`로 간다. 이렇게 써야 이 폴백이
  **오늘 해소되는 링크를 하나도 안 건드린다.**

  없는 헤딩을 가리키면 아무 데로도 스크롤하지 않는다. 맞는 노트의 맨 위에 있는 편이 엉뚱한
  헤딩에 도착하는 것보다 낫다 — 후자는 잘못된 곳을 읽고 있는 줄 모른다.
- **링크 안 걸린 언급 — 감사 계열의 네 번째** ([#245]). 다른 노트의 이름을 말했는데 링크는 안 건
  자리를 찾는다. `lapis links --unlinked` · vault 위생의 넷째 탭 · `lapis doctor`의 검사 항목.
  끊긴 링크가 "가리켰는데 없다", 고아가 "아무도 안 가리킨다"라면 이건 **"말했는데 안 가리킨다"**
  — 같은 그래프를 세 번째 각도에서 보는 것이다.

  목록만 내고 멈춘다. "링크로 바꾸기" 버튼은 없다 — 감사 셋이 전부 그렇고, 오탐이 남아 있는
  목록에 일괄 적용을 붙이면 한 번의 실수가 vault 전체에 퍼진다.

  ⚠️ **오탐을 막는 것이 이 기능의 전부다.** 조사한 사례들의 알려진 약점이 정확히 오탐이었고,
  시끄러운 목록은 아무도 안 연다. 거르는 규칙을 하나씩 꺼서 실제 vault(81노트)에서 쟀다:

  | 끄면 | 보고되는 언급 |
  |---|---|
  | 아무것도 안 끔(지금 나가는 것) | **2** |
  | 코드 제외 | 63 |
  | 이미 그 노트로 연결된 출처 제외 | 5 |
  | frontmatter 제외 | 5 |
  | 모호한 이름 제외 | 3 |

  2건짜리 목록은 아무것도 못 찾은 기능처럼 보인다. 그게 아니라 **vault가 실제로 잘 엮여 있다는
  뜻**임을 아는 방법이 위 표다 — 규칙을 하나만 풀어도 목록이 최대 서른 배가 된다.

  감사 넷 중 이것만 본문을 읽는다(나머지 셋은 인덱스로 된다). 그래서 `doctor`도 본문을 읽게
  됐다 — 같은 vault에서 0.73 → 0.79초(+54 ms). 큰 vault에서는 이 항목이 비용을 지배한다.

### Fixed
- **`lapis index`가 같은 CLI도 못 읽는 캐시를 커밋했다** ([#247]). 이 명령은 **설치된 앱**을
  시켜 캐시를 쓴다. 그 앱이 한 버전 낮으면 낡은 형식으로 쓰고, 그러고도
  `커밋했다. 앱을 켜면 이 인덱스를 그대로 읽는다(재색인 없음)`이라고 출력했다. 같은 vault에
  `lapis doctor`를 돌리면 `version_skew`로 거부한다. **방금 만든 것을 방금 만든 도구가 못
  읽는다.** 그러고도 성공이라고 말했다.

  다른 작업 중 임시 vault에 돌려 보다가 걸렸다. 기존 확인은 인자를 **무시하는** 구버전 앱을
  막는다. 이건 인자는 받으면서 다른 버전을 쓰는 경우다.

  이제 shard를 만들기 전에 멈춘다. 하드 실패는 아니다 — **그 캐시가 쓸모없는 건 아니라서**
  그걸 쓴 앱은 잘 읽는다. 못 읽는 것은 CLI와 MCP다. 그래서 `--allow-version-skew`로 길을
  남기되, 통과하면 마지막 줄이 누가 읽을 수 있는지를 말한다. 진행 중 경고만으로는 부족하다 —
  그건 위로 스크롤돼 사라지고 **사람이 읽는 것은 마지막 줄**이다.
- **이름을 바꾸면 위키링크의 앵커가 날아갔다** ([#246]). `[[old#헤딩]]`이 `[[new]]`가 됐다.
  마크다운 링크는 처음부터 앵커를 보존했는데, 위키링크 패턴은 `[[stem]]`과 `[[stem|alias]]`만
  잡았다. 앵커가 해소되지 않던 동안에는 티가 안 났지만, 이제는 이름 바꾸기가 링크를 조용히
  깨는 경로가 된다.
- **처음 나온 5건 중 3건이 이미 그 노트로 링크를 건 곳이었다** ([#245]). 내보내기 전에 실제
  vault에 돌려 보고 찾았다. 셋 다 같은 모양이었다 — 링크는 파일 이름으로 걸고 설명은 제목으로
  쓴 줄:

  ```md
  - [[STATE]] — Lapis 진행 상태
  ```

  링크된 자리는 덮이지만 바로 옆의 제목은 안 덮여서, 이미 가리키고 있는 노트를 "안 가리킨다"고
  보고했다. 이제 대상과 **이미 연결된 출처**는 통째로 뺀다(본문 링크든 frontmatter 관계든).
  줄이 아니라 노트 단위다 — 이 감사가 찾는 것은 **없는 간선**이고, 간선이 이미 있으면 어느
  줄에서 다시 말하든 그래프는 이어져 있다.

### Internal
- **마스킹에 직접 겨냥한 테스트가 생겼다** ([#245]). 줄 번호를 원본 오프셋으로 세기 때문에,
  마스킹은 코드와 frontmatter를 지우는 대신 **같은 길이의 공백으로** 덮는다. 그 길이 보존이
  깨져도 예외는 안 난다 — 보고되는 줄 번호가 전부 조용히 어긋나고, 그러고도 그럴듯하다.

## [2.1.0] — 2026-08-27

### Added
- **색 테마 스물여섯 가지** ([#243]). 테마는 여전히 다크 하나이고, 이건 그 위에 얹는다 —
  열넷은 강조색만 바꾸고 열둘은 배경까지 물들인다. 설정 → 모양. 어느 것을 골라도 사용자
  정의 CSS로 개별 토큰을 다시 덮어쓸 수 있다.

  이건 `app.css`의 두 번째 팔레트가 아니라 **데이터**다. 그 중복은 2.0.0에서 지웠고 가드가
  다시 생기는 것을 막는다. 프리셋은 스타일시트와 사용자 CSS 사이에 런타임으로 주입된다.

  ⚠️ **어느 것도 눈으로 색을 맞추지 않았다.** 스물여섯을 손으로 맞추면 안 읽히는 것이 몇
  개 나오게 돼 있고, 안 읽히는 테마는 오류가 아니라 그냥 나쁜 화면이라 **그걸 고른 사람만**
  겪는다. 대신 색조가 건드리는 모든 단계에서 **WCAG 상대휘도를 보존한다** — 대비비는 휘도
  만으로 정해지므로, 휘도를 붙들면 가독성이 붙들린다. 테마별로 재 보면 본문 글자가 9.29에서
  9.41 사이에 들어온다(기본값 9.36).

  액센트는 그걸 물려받을 수 없다 — 자기 색이 존재 이유다. 그래서 그 위에 올릴 글자를
  **계산한다**: 검정과 흰색 중 대비가 큰 쪽. Amber와 Ember는 아무도 정해 주지 않아도
  검은 글자를 받는다.
- **사용자 정의 CSS 편집기가 예시와 함께 열린다** ([#243]). 규칙은 전부 주석 처리돼 있고,
  하나를 풀어 저장하면 앱이 바뀐다. 훅을 설명만 하는 예시는 **무엇을 붙잡을 수 있는지**
  보여 주지 못한다.

  저장하는 게 아니라 보여 주기만 한다. 저장된 기본값으로 심으면 "아무것도 설정 안 함"과
  "아무것도 아닌 것으로 설정함"의 차이가 사라지고, 그러면 지웠을 때 다시 나타나거나 영영
  없어진다.

### Fixed
- **text-muted 토큰에 대비 여유가 없었다** ([#243]). `#949ba4`는 본문 배경에서 정확히
  4.505:1 — 기준 4.5를 천분의 다섯으로 넘고 있었다. 색조를 얹은 값을 8비트로 반올림하는
  것만으로 그 아래로 내려갔다. 지금은 `#989fa8`(4.73:1)이고, 눈에 띄게 다르지 않으면서
  움직일 자리를 남긴다. **새 테마만의 문제가 아니라 기본 테마에 있던 취약점**이었다.
- **사용자 정의 CSS가 한 라우트에서만 주입됐다** ([#243]). effect가 메인 페이지 컴포넌트에
  있어서, 그 밖에서 그려지는 것 — 이를테면 컴포넌트 미리보기 라우트 — 은 저장된 CSS와
  테마를 받아 놓고도 적용하지 않았다. 이제 루트 레이아웃에서 돈다.

### Internal
- **CSS 토큰 가드가 컴포넌트 지역 프로퍼티를 트집 잡지 않는다** ([#243]). 요소에 인라인으로
  넣고 같은 컴포넌트의 스타일시트가 읽는 커스텀 프로퍼티는 `app.css`에 있을 이유가 없는데,
  가드가 그걸 미정의로 셌다. 예전엔 그런 것을 손으로 관리하는 허용 목록에 넣었다. 이제
  **같은 파일이 그 프로퍼티를 정의했는지**를 묻는다. 틀릴 때마다 자라는 목록은 결국 자기가
  잡으려던 오타를 봐주게 된다.

## [2.0.0] — 2026-08-27

> **⚠️ 파괴적 변경 — 라이트와 시스템 테마가 없어졌다.** 이제 테마는 다크 하나다.
> 색을 바꾸는 길은 사용자 정의 CSS다. 이전 버전에서 저장한 테마 설정은 무시된다.

### Changed
- **화면이 디스코드의 시각 문법을 따른다** ([#235], [#236], [#237]). 중립 램프가 파란기
  도는 슬레이트였는데, 디스코드는 채도가 거의 없는 회색이고 인상 차이의 대부분이 거기서
  온다. 셸 세 면이 디스코드의 세 면에 그대로 앉았다 — 레일 `#1e1f22`, 사이드바 `#2b2d31`,
  본문 `#313338`.

  액센트는 v1.x부터 이미 Blurple이었다. 그래서 이번 작업은 전환이 아니라 연장이다.

  라운드는 값을 하는 곳에서 올렸다 — 43곳이 쓰는 `--r-sm`이 4px에서 6px로. `--r-lg`는
  16px로 올렸다가 되돌렸다. 모달이 그걸 쓰는데, 16px은 디스코드 모달보다 **오히려 더
  둥글었다.** 디스코드처럼 보이고 싶은 것과 더 둥글게 하고 싶은 것은 다르다.

  호버·활성 전환에 등장과는 다른 곡선을 준다. 기존 곡선은 오버슈트가 있어서 모달이
  튀어나올 때는 맞지만, 커서가 지나가며 색이 바뀔 때는 **되튀어 보인다.**
- **전역 상단바가 없어지고 노트에 헤더가 생겼다** ([#237]). 디스코드에는 전역 상단바가
  없다 — 지금 보고 있는 것의 이름은 그 내용 바로 위에 붙는다. 방문 이력, 노트 경로,
  저장 배지가 그 헤더로 들어갔고 "Lapis"라는 글자는 버렸다. 앱이 자기 이름을 상시
  표시할 이유가 없다.
- **설정이 카테고리로 나뉜다** ([#239]). 모양 · 언어 · vault · 고급이 왼쪽 목록에 있고
  고른 카테고리의 내용이 오른쪽에 온다. 일곱 섹션이 한 줄로 있으면 뭘 찾든 스크롤하며
  다시 읽어야 한다. 버전은 창 크롬이 아니라 카테고리 목록 하단에 있다.

### Added
- **사용자 정의 CSS** ([#238], [#239]). `app.css`의 디자인 토큰과 `data-lapis` 훅 열다섯이
  문서화된 계약이고, 내부 클래스 이름은 **명시적으로 아니다.** 선을 좁게 긋는 것은
  의도다 — 넓게 그으면 내부 구조가 공개 API가 되어 그대로 굳는다.

  편집기는 고급 아래에 있다. 문법 컬러링, 괄호 매칭, 진짜 포매팅이 되고, 포매팅 실패가
  곧 문법 검사 역할을 한다. 저장은 명시적인 동작이다 — 타이핑마다 저장하면
  `[data-lapis="app"] { display: none` 을 치는 도중, **닫는 중괄호를 치기 전에** 앱이
  사라진다.

  ⚠️ **되돌리는 길이 셋이다.** CSS 한 줄로 창과 설정이 같이 사라질 수 있다.
  `⌘⇧⌥C`(`Ctrl+Shift+Alt+C`)가 적용을 끈다 — 키 핸들러는 스타일이 무엇을 하든 상관없이
  돌기 때문에 새까만 화면에서도 듣는다. 앱이 아예 안 뜨면 터미널에서 `lapis css --off`가
  설정 파일을 고친다(**내용은 지우지 않고 끄기만** 한다). 그것도 안 되면 설정 파일을
  지우면 초기화된다.

  미리보기는 그 길에 **들어가지 않는다.** `display: none`은 미리보기에서도 똑같아 보인다.
  **이미 실수를 알아챈 사람에게만 듣는 장치는 안전장치가 아니다.**

### Fixed
- **같은 색이 세 곳에 정의돼 있었다** ([#235]). `app.css`가 팔레트를 `:root`에 한 번,
  `[data-theme="light"]`에 또 한 번, `prefers-color-scheme` 블록 안에 세 번째로 갖고
  있었다. 하나를 고치면 나머지도 기억하라는 주석과 함께. 강제하는 것은 없었고, 거기서
  어긋나면 다크만 쓰는 사람 눈에는 **영원히 안 보인다.** 라이트와 시스템을 없애면서
  파일에서 135줄이 빠졌고, 이제 가드가 두 번째 팔레트를 거절한다 — 의도된 조밀 밀도
  변형은 그대로 통과시키면서.
- **편집기와 미리보기가 검색 매치를 다르게 칠하고 있었다** ([#236]). 양쪽이 각자 색을
  하드코딩했고 이미 갈려 있었다 — 불투명도 30%와 35%, 현재 매치의 주황이 서로 다른 색.
  **편집과 읽기를 오가면 같은 매치가 달라 보였다는 뜻이다.** 이제 토큰 넷을 공유한다.

### Internal
- **이번에 생긴 계약들에 가드를 붙였다** ([#235], [#238], [#239]). 팔레트가 하나인지,
  계약의 모든 `data-lapis`가 마크업에 있고 마크업의 모든 것이 계약에 있는지, 패닉
  단축키가 다른 키 처리보다 먼저 검사되는지, 모든 설정 행이 카테고리 안에 있는지 —
  밖에 있으면 **컴파일되고 테스트도 통과하는데 화면에만 안 보인다.**

  그중 둘은 첫 시도에 틀렸고 카나리아로만 드러났다. 하나는 `{#if}` 깊이를 단순 카운터로
  셌는데 섹션 안의 중첩 조건문이 그걸 음수로 끌어내려 **아무것도 못 잡았다.** 다른
  하나는 설명 주석을 진짜 속성으로 읽었다.
- **포매터가 필요할 때만 로드된다** ([#239]). 정적으로 넣으니 prettier가 852 KB 청크에
  들어가 진입점이 즉시 끌어왔다 — **설정을 한 번도 안 여는 사람이 매 기동마다** 그걸
  받고 있었다. 동적 import 뒤로 보내니 156 KB와 82 KB로 갈라지고, "정돈"을 처음 누를
  때 받는다. 한 번은 느려지고, 매 기동은 아니다.
- **열린 측정 질문 셋을 아무것도 안 만들고 닫았다** ([#240]). `aliases`를 색인할지,
  `name` 필드가 제 값을 하는지, 한글 bigram 토크나이저가 과하게 매칭하는지. **이 코퍼스로는
  셋 다 답할 수 없다** — alias가 있는 노트가 하나도 없고, 파일명은 로마자인데 제목은
  아니며, 과매칭은 19,225 노트에서 관측된 것인데 이 vault는 71개다. **매칭 수는 노트
  수를 넘을 수 없다.**

  그 한계를 하네스 자체에 적어 뒀다. 다음 시도가 도구가 아니라 **코퍼스부터 보게** 하려는
  것이다.

## [1.20.0] — 2026-08-27

> **⚠️ 이 버전은 처음 켤 때 재색인을 한 번 한다.** 풀텍스트 인덱스에 필드가 하나 늘어
> `CACHE_VERSION`이 8 → 9이 되고, 기존 샤드를 재사용할 수 없다. 파일 이름만 옮기던
> v1.17.0 캐시 이주와 달리 **다시 만든다.** 큰 vault는 잠깐 걸린다.

### Added
- **노트의 제목이 자기 검색 필드를 갖는다** ([#233]). 풀텍스트 인덱스에는 `name`과 `body`
  뿐이었다. `name`은 파일명이고 이 계열 vault에서는 영문 kebab-case라, 한글 제목 질의에
  `boost: { name: 3 }`이 **아무 일도 안 했다.** frontmatter `title`은 자기 필드가 없어
  그 텍스트가 `body` 안에서 다른 산문과 같은 무게로 취급됐다.

  인덱스 모양만 바꿔 같은 vault에서 잰 결과(69 노트 · 204 케이스):

  | fields | 전체 | title | **제목 2어절** | body | MRR |
  |---|---:|---:|---:|---:|---:|
  | `name`, `body` | 79.9% | 91.2% | **64.7%** | 83.8% | 0.870 |
  | `name`, `title`, `body` | 89.7% | 98.5% | **85.3%** | 85.3% | 0.943 |

  같은 자리를 두 번 시도해 두 번 실패했었다 — 토크나이저 교체는 1포인트, 결합 4단계화는
  변화 없음.

  ⚠️ **이 숫자는 조심해서 읽어야 한다.** 하네스가 질의를 frontmatter `title`에서 뽑기
  때문에, 그 필드를 따로 색인하는 것은 **부분적으로 시험 문제를 가르치는 셈**이다. 제목에
  빚진 것이 없는 body 질의는 83.8% → 85.3%였다. 정직한 주장은 "검색이 10포인트 좋아졌다"가
  아니라 **"어렴풋이 기억하는 제목으로 노트를 찾는 일이 크게 좋아졌다"** 이고, 그게 이
  하네스가 재려고 만들어진 상황이다.

  이 필드에 boost는 주지 않았다. 0.01에서 100까지 바꿔도 결과가 변하지 않는다 — 깨끗한
  질의는 전부 `AND` 단계에서 끝나고 그 단계는 검색 옵션을 명시적으로 넘겨서, 인스턴스에
  건 boost가 도달하지 않는다. 개선은 BM25 길이 정규화에서 온다: 짧은 제목 필드의 term이
  긴 본문에 묻힌 같은 term보다 무겁다. 아무 일도 안 하는 숫자를 남기면 다음 사람은 그걸
  튜닝된 값으로 읽는다.

### Fixed
- **검색 품질 하네스가 아무것도 안 재고 통과라고 보고했다** ([#232]).
  `./mcp/lapis-eval --vault <경로>`를 돌리면 `케이스 0건`이 나오고, 품질 수치가 전부
  `NaN%`인데, 마지막 줄은 통과라고 적히고 종료 코드가 0이었다.

  세 가지가 겹쳐야 나오는 상태였다. `Number("--vault")`는 `NaN`이고 `slice(0, NaN)`은 빈
  배열이다 — 예외도 경고도 없다. `--vault`는 애초에 지원되는 옵션이 아니었는데 아무도
  거절하지 않아, **표본 수 자리인 위치 인자를 조용히 먹었다.** 그리고 마지막 판정은 지연
  예산만 보고 있었다.

  측정 도구는 다른 판단이 딛고 서는 바닥이다. 토크나이저를 바꾸고 이걸 돌려 "R@1 그대로"를
  읽었다면, 실제로는 아무것도 비교하지 않은 것이다. `mcp/benchRun.ts`에도 같은 줄이
  있었다 — 거기서는 `Math.max(200, NaN)`도 `NaN`이다.

  두 도구 모두 인자를 제대로 파고, `--vault`와 `--help`를 지원하고, 모르는 것은 거절한다 —
  `cli/README.md`가 이미 적어 뒀지만 형제 도구가 안 지키던 규율이다. 케이스 0건은 이제
  통과가 아니라 오류다.

### Internal
- **인덱스 모양을 `CACHE_VERSION`에 묶는 가드** ([#233]). `CLAUDE.md`가 `fullTextOptions.ts`는
  그 버전의 보호 밖이라고 경고하고 있었지만 강제하는 것이 없었다. 잊으면 낡은 샤드를 새
  질의 코드가 읽고, 오류 없이 조용히 틀린 결과가 나온다. 가드는 저장되는 인덱스를 실제로
  결정하는 것들(`fields` · `tokenize` · `processTerm`)만 지문으로 만들어 버전 옆에 못 박는다.
  질의 시점 값(`boost` · `bm25` · `fuzzy`)은 **일부러 뺐다** — 넣으면 랭킹을 손볼 때마다
  전체 재빌드가 걸리고, 그만큼 시끄러운 가드는 꺼진다.
- **중복된 `FullTextDoc`을 지웠다** ([#233]). `searchIndex.ts`에 인터페이스 사본이 하나 더
  있었고, 앱은 그쪽으로 문서를 만드는데 인덱스는 다른 쪽으로 설정되고 있었다. 둘이 우연히
  같아서 아무 문제도 없었다 — 한쪽에 필드를 더하기 전까지는. 각 파일은 스스로 일관되므로
  타입 검사가 걸리지 않고, 틀리는 것은 검색 결과뿐이다. 사본을 지우자마자 문서를 만드는
  지점 둘이 더 드러났고, 둘 다 증분 갱신 경로라 **고친 노트만** 다른 모양으로 색인될
  자리였다.

## [1.19.0] — 2026-08-27

### Fixed
- **낡은 인덱스가 자신 있게 답하는데, 그렇다고 말하는 명령이 하나뿐이었다** ([#228]). CLI는
  인덱스를 읽기만 한다. 그래서 vault가 인덱스보다 새로우면 답이 낡을 수 있다. `mcp/README.md`에
  낡음은 보고하되 차단하지 않는다는 계약이 적혀 있었는데 **`lapis search`에만 구현돼 있었다.**

  `backlinks` · `list` · `links --broken` · `links --orphans` · `tag audit` · `replace`는 전부
  조용했다. 노트를 추가한 직후 감사를 돌리면 끊긴 링크 없음 · 고아 1개가 나왔고, 재색인 후 같은
  vault에서 고아가 3개였다. 답이 바뀐 게 아니라 **처음 답이 틀렸던 것**인데, 그걸 짐작할 단서가
  없었다.

  이제 모든 읽기가 한 줄로 말하고, `--json`에는 `stale` 필드로 실린다. 여전히 막지는 않는다 —
  살아 있는 vault는 편집 몇 초 만에 낡으므로 하드 실패시키면 도구를 못 쓴다.

  **쓰기는 다르게 대하고, 이제 멈춘다.** `tag rename --apply`와 `replace --apply`는 내용은
  디스크에서 새로 읽으면서 **노트 목록은 인덱스에서** 가져온다. 그래서 목록이 낡으면 마지막
  인덱싱 뒤에 만든 노트가 조용히 빠지는데, 보고는 노트 2개를 갱신했다고 한다. 임시 vault에서
  재현했다 — 인덱싱 뒤에 만든 노트가 옛 내용 그대로 남았고 아무 말도 없었다. 두 명령은 이제
  낡은 인덱스에서 거절하고(종료 코드 2) `lapis index`를 가리킨다. `--allow-stale`로 강행할 수
  있다. 낡은 읽기는 다시 읽으면 되지만, 일부만 쓰인 vault는 아무도 검사할 수 없다.
- **명령 이름을 쳐도 그 명령이 위로 오지 않았다** ([#228]). `⌘K`에서 `위생`을 치면 본문 검색
  결과가 먼저 나오고 명령은 다섯 줄 아래 있었다. 그 명령의 라벨이 바로 그 단어로 시작하는데도.

  점수 문제가 아니었다. 명령에는 1.2배 우대가 걸려 있었지만 팔레트는 **고정된 그룹 순서**로
  그리고 명령 그룹이 항상 마지막이라, 우대가 아무것도 움직일 수 없었다. 우대 코드 자체는
  멀쩡히 읽힌다 — 자리를 정하는 것은 다른 파일이다.

  이제 라벨(또는 라벨 안 어느 단어)이 입력으로 시작하는 명령은 결과 위로 올라온다. 퍼지로만
  맞는 것은 그대로 아래다. 조건이 헐거우면 노트를 찾는 모든 흐름에 명령이 끼어들어 원래
  문제보다 나빠진다. 나머지 그룹 순서는 그대로다 — 자리가 매번 바뀌면 근육 기억이 죽고,
  팔레트에서 잘못 고르면 노트가 열리거나 창이 뜬다.
- **README의 단축키 셋이 실제로는 없는 것이었다** ([#226]). `⌥B`는 실제로 `⌘⌥B`, `⌘←`/`⌘→`는
  실제로 `⌘⌃←`/`⌘⌃→`였고, `⌘⇧B`(표 보기)는 표에 아예 없었다. 적힌 대로 눌러도 아무 일이
  없으니 낡은 문서가 아니라 **고장난 앱**으로 읽힌다. 코드가 맞고 README가 틀렸다.
  Development 절도 CI와 맞췄다.

### Added
- **`lapis doctor`** ([#228]). 감사를 한 번에 돌린다 — 끊긴 링크 · 고아 · 태그 중복 · 모호한
  이름, 그리고 따로 치지 않으면 놓치는 인덱스 낡음까지.

  훅이나 CI에서 쓸 수 있게 종료 코드에 뜻을 준다: `0` 깨끗함, `1` 문제 있음, `2` 못 돌림.
  **`doctor`만 `1`에 오류 이상의 뜻을 주므로**, 못 쓰는 vault를 `2`로 내는 것도 이 명령뿐이다 —
  안 그러면 경로 오타가 위생 문제로 보고된다.

  낡음은 맨 위에 낸다. 아래 숫자를 얼마나 믿을지가 거기 달렸기 때문이다. 다만 문제로 세지는
  않는다 — 살아 있는 vault는 거의 항상 조금씩 낡아 있고, 늘 실패하는 검사는 결국 지워진다.
  감싸고 있는 감사들과 마찬가지로 `doctor`도 고치지 않는다.

### Internal
- **위 두 어긋남을 막는 가드.** 하나는 모든 핸들러의 소스를 읽어, 인덱스를 읽는 명령이
  `--json`과 사람용 **양쪽에** 낡음을 내는지 본다. 처음엔 둘 중 하나만 있으면 통과시켰는데,
  카나리아를 돌리니 사람용 한 줄을 지워도 JSON 필드가 남아 통과했다. 그게 가장 나쁜 모양이다 —
  스크립트는 필드를 받는데 터미널 앞의 사람은 경고 없이 낡은 숫자를 본다. 다른 하나는 팔레트
  컴포넌트를 읽어 렌더 순서가 선언된 `GROUP_ORDER`와 같은지 본다. 이 검사가 없어서 점수와
  자리가 갈라진 채로 있었다.
- **손으로 열기 번거로운 화면을 바로 띄울 수 있다** ([#229], [#230]). vault 위생 모달과 치환
  패널은 실제 vault 상태가 있어야 채워진다. `npm run dev` 후 `/dev/preview`가 픽스처로 그려주고
  Tauri 없이 돌며 화면·테마를 고를 수 있다 — DOM 테스트가 못 보는 색·간격·정렬을 보기 위해서다.
- **그 화면들에 테스트가 붙었다** ([#227]). 감사와 치환 로직은 순수 함수로 이미 고정돼 있었고
  CLI도 같은 것을 쓰므로 **데이터는 검증돼 있었다.** 안 된 것은 마크업이 그걸 어떻게 그리는가다.
  조건 하나가 뒤집히면 경고가 아무 에러 없이 사라지는데, 치환 패널에서 그 경고는 되돌릴 수 없는
  쓰기 직전에 보는 마지막 정보다. 아이콘 컨테이너(`.icns` · `.ico`)도 원본 PNG와 바이트로
  대조한다.

## [1.18.0] — 2026-08-26

### Fixed
- **같은 이름의 노트 둘 중 walk가 먼저 닿은 쪽으로 해소되던 것** ([#220]). 해소기가 소문자
  이름 → 경로 **하나**인 평평한 전역 Map이었고 먼저 넣은 것이 이겼다. walk가 알파벳순이라,
  한 프로젝트에서 쓴 링크가 다른 프로젝트의 동명 노트를 **조용히** 가리켰다. 링크가 깨진 게
  아니라 엉뚱한 곳으로 갔다.

  이제 후보를 전부 들고 **링크한 노트와 가장 가까운 것**을 고른다. 프론트매터 교차참조도
  같은 규칙을 타는데, 새는 곳의 대부분이 거기였다. 사람이 준 이름(`lapis open` · `backlinks`)
  에는 그런 맥락이 없으므로, 모호하면 추측하지 않고 **후보 경로와 함께 거부**한다.

  두 프로젝트가 든 vault에서 실측: 고아 노트가 **8건 → 4건**. 사라진 넷은 전부 오탐이었다 —
  들어오는 링크를 다른 프로젝트의 동명 노트가 가로채고 있었다.
- **구조 질의 결과의 순서가 정해져 있지 않던 것** ([#219]). `doc_kind` · `topic` · `tag` ·
  `backlinks_of`로 나온 행에는 점수가 없고, 순서는 캐시가 `link_infos`를 담은 순서였다 —
  전체 빌드면 vault walk 순서, 앱의 증분 재색인 뒤면 패치된 순서다. 그래서 같은 질의가
  재색인 전후로 다른 순서를 냈다.

  순서만 문제가 아니었다. 그 뒤에 `limit`으로 자르므로 **어느 행이 남는지도 달라졌다.**
  이제 vault 상대 경로 오름차순으로 정렬하고, 비교는 `localeCompare`가 아니라 **UTF-16
  코드 단위**로 한다 — 로케일에 따라 순서가 갈리지 않게.

---

### Added
- **`lapis replace` · `⌘⇧G` 안에서 바꾸기** ([#224]). vault 전체 찾아 바꾸기. 찾는 것은 이미
  됐는데 **손을 쓸 수가 없었다.** 기본은 **dry-run**이고 `--apply`가 있어야 쓴다. 쓰기는 태그
  이름 바꾸기와 **같은** `$lib/safeWrite` 트랜잭션을 탄다(백업 → 순차 쓰기 → 실패 시 롤백).

  ⚠️ 찾기와 바꾸기는 **정규식 엔진이 다르다.** `⌘⇧G`는 Rust `regex`, 치환은 JS `RegExp`이고
  매치 지점이 다를 수 있다. 그래서 적용 전에 보여주는 건수는 **항상 치환 엔진이 낸 것**이며,
  앱에서는 **검색이 안 찾은 노트를 절대 쓰지 않는다** — 놓침은 되돌릴 수 있고 잘못된 쓰기는
  아니다. 둘이 갈리면 그 사실을 말해준다.

  경고는 목록보다 **먼저** 낸다: 바꿀 내용이 찾을 내용에 다시 걸리는 경우(`a` → `aa`는 실행할
  때마다 두 배가 된다), 그리고 frontmatter 안의 매치가 몇 건인지(YAML이 깨질 수 있다).
- **시간축 — `--since` · `--sort` · `--by`** ([#223]). vault는 모든 노트가 언제 바뀌었는지
  알고 있는데 물어볼 방법이 없었다. `checkStale`이 이미 매 질의마다 vault 전체를 훑고 그
  타임스탬프를 **버리고** 있었고, 프론트매터 `date`도 이미 인덱싱돼 있었다. 둘 다 질의
  차원이 된다 — `search` · `backlinks` · `links --orphans`, 그리고 MCP 도구에서.

  축이 둘인 이유는 **서로 다른 질문에 답하고, 하나는 거짓말을 하기** 때문이다. `mtime`은
  내가 실제로 만진 것이지만 `git pull`·`checkout`이 덮어쓴다 — 새로 클론하면 전부 같은
  값이라, pull 직후 "최근 바뀐 것"은 "pull이 건드린 것"이 된다. 프론트매터 `date`는 git에
  안 흔들리지만 사람이 적은 곳에만 있다.

  고른 축에 값이 없는 노트는 `--since`에서 빠지고 정렬에서는 맨 뒤로 간다 — **빠진 건수는
  항상 보고한다.**

  앱에서는 명령 팔레트 빈 입력에 **최근 변경** 그룹이 생겼다. *최근 연* 노트와 **따로** 낸다
  — 편집기·git·다른 도구가 쓴 변경은 열람 이력에 절대 안 남는다.
- **새 앱 아이콘.** 그동안 Tauri 기본 로고로 배포되고 있었다 — 인디고 대괄호와 금색 보석으로
  바뀐다. 벡터 원본(`src-tauri/icons/lapis-light.svg` · `lapis-dark.svg`)을 함께 두어 언제든
  다시 생성할 수 있게 했다.

  데스크톱 아이콘은 **단일 애셋**이라 light 쪽이 나간다. dark는 배경이 거의 검정이라 어두운
  작업 표시줄·독에서 실루엣이 사라진다.
- **`lapis links --orphans` · `lapis tag audit`** ([#221]). 인덱스에 이미 있는 것만 읽는 감사 둘.
  고아는 **아무도 안 가리키는 노트**로, 끊긴 링크 감사의 거울상이다 — 백링크가 주된 이동
  수단이므로 사실상 닿을 수 없는 문서라는 뜻이다. 태그 감사는 중복 후보를 낸다: 같은 잎을
  다른 부모에 단 것, 대소문자만 다른 것, 그리고 노트 여럿으로 해소되는 이름.

  둘 다 **무엇을 하라고 말하지 않는다.** 고아 행에는 나가는 링크 수가 함께 붙어 진입점
  (나감 많음 · 들어옴 없음)과 떨어진 섬이 구분되고, 합치는 일은 미리보기 → 백업 → 롤백을
  거치는 `tag rename`이 맡는다.

  앱에서는 끊긴 링크 화면이 **vault 위생**으로 넓어져 탭 셋이 됐다. 팔레트 항목 이름도
  따라 바뀐다.

---

## [1.17.0] — 2026-08-26

### Added
- **`lapis open <노트>`** ([#216]). 실행 중인 앱에서 노트를 연다. 앱이 꺼져 있으면 켠다.
  리스닝 포트는 쓰지 않는다 — 앱 실행파일을 다시 실행하면 argv가 실행 중인 인스턴스로
  전달된다.

  **어느 창이 여는지는 창들이 정한다.** Rust는 어느 창이 어느 vault를 열었는지 모르므로,
  열 것을 담아두기만 하고 각 창이 "내 것이냐"를 묻는다. 그 vault를 연 창이 하나도 없으면
  그때 새 창을 띄운다.
- **`lapis index` — 앱 없이 인덱스를 다시 만든다** ([#215]). 검색 인덱스를 다시 만들려면
  앱을 띄워야 했다. 이제 터미널에서 되고, 앱은 다음 기동에 그 결과를 **재색인 없이** 그대로
  읽는다.

  앱이 IPC 경계를 두고 하는 분업을 CLI는 **프로세스 경계**로 한다 — Rust가 vault를 훑고
  (인덱스 생산자는 하나뿐이다), Node가 MiniSearch shard를 만들고(옵션은 한 곳에 있다),
  Rust가 캐시 계약이 요구하는 **순서대로** 커밋한다.

  플래그를 모르는 구버전 앱을 만나면 매달리지 않고 그 사실을 말한다. 옛 빌드는 모르는 인자를
  무시하고 조용히 창을 띄운 뒤 돌아오지 않는데, 그러다 발견했다.
- **`lapis tag rename` — CLI 3층** ([#213]). vault 전체 태그 이름 바꾸기·병합을 터미널에서 한다.
  하위 태그가 부모를 따라가고, 경계는 `/`에서만 인정하며, 이미 있는 태그로 바꾸면 **쓰기 전에**
  병합이라고 알린다.

  **기본이 dry-run이다.** `--apply` 없이는 아무것도 쓰지 않는다 — 되돌릴 수 없는 작업이 인자 하나
  빠뜨렸다고 실행되면 안 된다. 쓰기는 앱과 **같은** `$lib/safeWrite` 트랜잭션(백업 → 순차 쓰기 →
  실패 시 롤백)을 타고, `cli/io.ts`가 Rust 커맨드가 주던 보장을 다시 세운다 — 원자적 쓰기,
  심링크까지 풀어서 하는 vault 이탈 차단, 확장자 화이트리스트. CLI만 느슨하면 공유하는 안전 규칙이
  갈린다.

### Fixed
- **옛 이름 캐시가 고아로 남을 수 있던 것** ([#217]). [#214]가 넣은 일회성 rename이 **새 이름
  파일이 없을 때만** 돌았다. 그래서 `lapis index`(CLI)가 앱보다 먼저 캐시를 쓰면 옛 파일이
  디스크에 영영 남는다 — [#214]가 없애려던 바로 그 상태다. CLI를 검증하다 실제 캐시에서 봤다.

  이제 새 이름 파일이 있어도 정리하고, 그 경우엔 덮어쓰지 않고 **밀려난 쪽을 지운다** —
  덮어쓰면 방금 만든 인덱스를 옛 스냅샷으로 되돌린다. 어느 쪽을 남길지는 meta 파일의 mtime
  하나로 **키별 한 번** 정한다. 파일마다 따로 재면 meta는 새 것, shard는 옛 것으로
  **스냅샷이 찢어진다**(그렇게 짰다가 테스트가 잡았다).
- **같은 vault가 캐시를 둘 가질 수 있던 것** ([#214]). 캐시 파일 이름의 해시를 **호출부가 준 문자열
  그대로** 계산했다. 그래서 `C:\Projects\x` 와 `C:/Projects/x`, 후행 슬래시 유무, 심링크를 거친
  경로가 각각 다른 이름을 만들었다. 증상은 "왜 또 전체 재인덱싱이지"이고, 이전 캐시는 아무도 안 읽는
  고아로 남는다. 이 머신에서 실측했다 — 앱이 만든 캐시 파일이 역슬래시 철자의 해시였고, 같은 vault를
  `/` 형태로 적으면 다른 이름이 나왔다. 이제 해싱 전에 경로를 canonicalize 한다.

  기존 캐시는 다시 만들지 않고 **이름만 옮긴다.** [#207]이 이전 해시 변경을 다룬 방식 그대로이고,
  이주가 이제 옛 세대 **둘**을 모두 시도한다.
- **실패한 쓰기가 성공처럼 보이던 것** ([#212]). 백업 → 순차 쓰기 → 롤백 트랜잭션이 아무것도
  반환하지 않았다. 백업이 실패하면 그냥 `return` 해서, 호출부는 중단된 쓰기와 끝난 쓰기를 구분할
  수 없었다. **태그 이름 바꾸기 모달이 아무것도 안 쓰고도 성공한 듯 닫혔다** — 되돌릴 수 없는
  작업이 실패하는 가장 나쁜 방식이다. 됐다고 믿게 만든다. 이제 결과를 반환하고, 실패하면 모달이
  이유와 함께 열린 채로 남으며, 노트 rename 경로는 사람이 읽을 요약을 남긴다.

  트랜잭션 자체도 Svelte store 밖 `$lib/safeWrite`로 옮기고 IO를 주입받게 했다. 소비자가 하나였다가
  둘이 되었고(#202가 export 했다) 셋이 되려던 참이었다. **되돌릴 수 없는 쓰기의 규칙은 갈리면
  안 된다** — 갈리면 고침이 한쪽에만 들어간다.

---

## [1.16.0] — 2026-08-26

### Added
- **커맨드라인 도구** ([#210]). MCP가 에이전트에게 열어주는 그 인덱스를 이제 터미널에서도 쓴다.
  앱이 떠 있지 않아도 된다 — `search` · `backlinks` · `list` · `links --broken` · `status`.
  모든 명령이 `--json`을 받고 `lapis_query`가 돌려주는 것과 **같은 모양**을 낸다. 스크립트나
  에이전트가 형식을 두 번 배우지 않아도 된다.

  랭킹을 다시 짜지 않고 `lapisQuery()`를 그대로 부른다 — 랭킹 하나에 소비자 둘이다. 명령 표면
  (이름·옵션·도움말)은 배열 하나에 있고 `--help`·인자 검증·짝 맞춤 테스트가 **모두 그걸 읽는다.**
  도움말이 실제로 받는 옵션과 어긋날 수 없다. 모르는 옵션은 무시하지 않고 `2`로 죽는다 —
  `--limt 5`를 조용히 버리면 기본 limit으로 돈 결과가 요청한 결과처럼 보인다.

  계약·종료 코드, 그리고 **의도적으로 아직 안 만든 것**(헤드리스 인덱싱·쓰기·실행 중인 앱 조작)의
  층별 계획은 [`cli/README.md`](cli/README.md)에 있다. 심링크된 노트 트리가 아니라 **저장소 안**에
  두어 클론과 함께 따라오게 했다.

### Fixed
- **v1.15.0으로 올리면 MCP 질의가 전부 실패했을 것** ([#209]). 앱은 캐시 v8로 갔는데([#201])
  MCP 서버가 기대하는 버전은 7에 머물러, **멀쩡한 캐시를 `version_skew`로 거부**한다 — 인덱스는
  정상인데 도구가 통째로 죽는다.

  테스트는 **구조적으로** 이걸 못 잡는다. `mcp/fixture.ts`가 TypeScript 상수로 캐시를 쓰고 서버도
  같은 상수로 읽으니, 그 값이 앱에서 얼마나 멀어지든 **둘은 늘 일치한다.** 이제 가드가 Rust 소스를
  직접 읽어 대조한다 — 두 진실이 만나는 유일한 자리다. `version: 7`을 리터럴로 박아둔 테스트 4곳도
  (그 값이 마침 상수와 같았을 뿐이다) 상수를 쓰게 바꿔, 원래 검증하려던 것을 계속 검증한다.
- **프런트엔드 래퍼가 더 이상 없는 Tauri 커맨드를 부르고 있던 것** ([#208]). `writeSearchCache`는
  샤딩 이전(캐시 v3)의 저장 함수였다. Rust 커맨드는 v4에서 사라졌는데 TypeScript 래퍼만 남아,
  **부르면 "command not found"로 죽는 함수가 API처럼** 놓여 있었다. 커맨드 이름은 **문자열**이라
  타입 검사가 닿지 않는다 — `tsc`도 `svelte-check`도 `cargo`도 통과했고, 누가 실제로 부르는
  순간에만 터진다. 이제 `invoke("x")`가 `generate_handler!`에 없으면 테스트가 실패하고 해당
  파일이 찍힌다. 쓰이지 않던 `gitHasChanges` 래퍼도 함께 지웠다.

  두 번째 가드로 `ko.json`과 `en.json`의 키 집합이 같은지 본다. 한쪽에만 있는 키도 에러가 아니다 —
  paraglide가 baseLocale로 폴백하므로 한국어 화면에 영어 문장 하나가 섞일 뿐 아무도 항의하지 않는다.
- **테이블 뷰의 hover·선택 스타일이 아무 일도 안 하던 것** ([#206]). `TableView.svelte`가
  `--surface-hover`(4곳) · `--accent-soft`(1곳) · `--text-tertiary`(6곳)를 참조하는데 셋 다
  `app.css`에 없었다. 정의되지 않은 커스텀 프로퍼티는 **에러가 아니라** 선언이 통째로 무시되는
  것이라, 빌드도 `svelte-check`도 통과했고 행 hover·칩 선택·흐린 텍스트가 조용히 죽어 있었다.
  의도했던 토큰(`--surface-raised` · `--accent-bg-subtle` · `--text-muted`)으로 맞췄다. 이제
  소스의 `var(--x)`가 `app.css`에 없으면 테스트가 실패하고, 실패 메시지에 해당 파일이 찍힌다.
- **검색 캐시 파일 이름이 불안정한 해시에서 나오던 것** ([#207]). `vault_key`는 캐시 파일 이름을
  vault 경로에서 뽑는데 `DefaultHasher`를 썼다. std가 그 값의 안정성을 **명시적으로 보장하지 않는다.**
  값이 한 번 달라지면 앱은 존재하지 않는 파일 이름을 찾게 되고 — 조용한 전체 재빌드에, 이전 캐시는
  아무도 읽지도 지우지도 않는 고아로 남는다. v1.15.0에서 고친 fingerprint와 같은 뿌리인데, 증상이
  오답이 아니라 느려짐인 자리다. 이제 `crate::hash`의 명세된 FNV-1a를 쓴다 — fingerprint와 공유해서
  관습 두 개가 아니라 **문서화된 계약 하나**가 되게 했다.

  기존 캐시는 **다시 만들지 않고 이름만 옮긴다.** 처음 읽기가 빗나가면 옛 이름을 찾아 파일을
  rename한다. 버전 bump도, 두 번째 재인덱싱도 없다. 옛 이름을 재현하지 못하면 결과는 지금과 정확히
  같다 — 재빌드다. 그래서 이 이주는 **아무것도 안 하는 것보다 나빠질 수 없다.**

---

## [1.15.0] — 2026-08-26

### Added
- **vault 전체 태그 이름 바꾸기·병합** ([#202]). 태그는 `/`로 계층을 이루고 사이드바가 접두 트리로
  그리는데, 오타 하나를 고치려면 그 태그가 든 노트를 **전부 손으로 열어야 했다.** 노트 rename은
  오래전부터 인용 링크를 자동으로 고쳐왔는데 태그에는 그 짝이 없었다. 하위 태그도 부모를 따라간다 —
  `tech`를 `stack`으로 바꾸면 `tech/svelte5`는 `stack/svelte5`가 된다. 경계는 **`/`에서만** 인정해
  `technical`은 건드리지 않는다. 이미 있는 태그로 바꾸면 둘이 합쳐지고, 적용 전에 그 사실을 알린다.

  적용은 노트 rename의 트랜잭션을 **그대로** 탄다: dry-run 미리보기 → 백업 → 순차 쓰기 → 실패 시
  롤백. 그리고 `related:` 갱신과 마찬가지로 YAML을 **파싱하지 않고 줄 단위로** 고친다 — #184에서
  파싱 실패가 노트의 frontmatter를 통째로 날린 그 교훈이다. 본문의 `#tag`는 의도적으로 그대로 둔다.
  인덱서가 그걸 무시하는 데는 이유가 있기 때문이다.
- **vault 전체 리터럴·정규식 검색** (`⌘⇧G`) ([#200]). 문서 **내** 검색(`⌘F`)에는 regex·대소문자·단어
  단위가 오래전부터 있었는데, vault **전체**(`⌘⇧F`)는 BM25 토큰 매칭뿐이었다. 이 공백이 문제인 이유는
  **BM25와 grep이 반대 방향으로 실패**하기 때문이다 — 이 vault 실측에서 `_memories`의 4문항에 grep은
  전부 0건이었고(기록은 "창", 질의는 "윈도우") BM25는 같은 트리에서 상위를 익사당했다.
  `mcp/README.md`가 "둘 다 쓰는 게 맞다"고 결론냈는데 앱에는 한 팔만 있었다. 매칭은 Rust에서,
  `read_vault_bundle`이 이미 쓰는 rayon 병렬 walk 위에서 돈다. 결과를 클릭하면 같은 패턴으로 문서 내
  검색이 켜져 노트 맨 위가 아니라 찾은 자리에서 시작한다.

  ⚠️ 매치 오프셋은 **Rust가** UTF-16 코드 단위로 돌려준다. 프런트에서 다시 계산하면 두 번 틀린다 —
  Rust `regex`에는 역참조·lookaround가 없어 JS `RegExp`가 다른 곳을 매치할 수 있고, 바이트 오프셋을
  쓰면 한글이 든 줄에서 하이라이트가 통째로 어긋난다.
- **끊긴 링크 감사** ([#199]). 프리뷰가 미해소 위키링크에 클래스를 붙이긴 하지만 **그 노트를
  열었을 때만** 보인다. 19,000 노트에서 눈으로 훑어 찾을 수 있는 게 아니다. README가 밝히듯
  **vault를 쓰는 게 Lapis가 아니라 바깥 도구들**이라 이게 문제가 된다 — 앱 안에서의 rename은
  가리키는 링크를 따라가 고치지만, 밖에서 파일이 지워지거나 이름이 바뀌면 조용히 끊기고 그
  경로엔 아무 신호가 없었다. 새 명령이 미해소 본문 링크를 전부 열거하되 **대상별로 묶고 참조
  수 내림차순으로** 낸다 — 고칠 단위가 "링크 하나"가 아니라 "없는 노트 하나"라, 목록 위쪽이
  곧 가장 싸게 고치는 순서다. 인덱스 빌드가 아니라 **요청 시에만** 계산해 기동 경로는 그대로다.

  ⚠️ frontmatter 상호참조는 의도적으로 제외한다. `relations.ts`는 "노트로 resolve되면 관계"를
  정의로 삼기 때문에, 그 필드를 감사하면 `status: welcome` · `priority: high` 같은 평범한
  스칼라가 전부 끊긴 링크로 잡힌다. 본문 링크는 문법 자체가 링크 선언이라 그 모호함이 없다.
- **질의를 가로질러 비교되는 상대 점수 `rel`** ([#198]). raw BM25 점수는 질의 간 비교가 안 됐다 —
  같은 코퍼스가 `"멀티 윈도우"`에 63점, 영문 혼합에 1,494점을 냈다(다른 표본은 848 vs 73).
  IDF가 질의 term 구성에 따라 통째로 달라지고 shard-local이기까지 해서다. 그래서 **절대 임계값을
  세울 수 없었고**, 특히 `OR` 폴백에서 아팠다 — 일부러 넓게 긁는 단계인데 꼬리를 자를 기준이
  없었다. 이제 랭킹 결과마다 그 질의의 top-1을 `1.0`으로 둔 `rel`이 실린다. MCP 도구는 `min_rel`로
  꼬리를 자르고, 자른 건수를 `used[].dropped_by_min_rel`로 보고한다 — 걸러진 결과가 조용히
  사라지지 않는다. **랭킹 순서는 그대로다.** 기존 정렬 뒤에 얹는 단조 변환이라 계측 하네스의
  R@1·R@10·MRR이 변하지 않는다.

### Fixed
- **stale 판정이 추정에서 정확 판정으로** ([#201]). `mcp/README.md`가 남은 한계로 적어둔 항목이다 —
  캐시 fingerprint가 Rust `DefaultHasher`에서 나왔는데 std가 그 값의 안정성을 **명시적으로 보장하지
  않아** MCP 서버가 재현할 수 없었고, 그래서 mtime 비교로 물러섰다. 그 프록시는 **mtime이 움직이지
  않은 채 내용만 바뀐 파일을 놓친다** — 색인이 낡았는데 "최신"이라고 답하는 셈이라 "모르겠다"고
  답하는 것보다 나쁘다. 이제 해시가 명세된 FNV-1a 구성이고, 양쪽이 같은 문서화된 계약에서
  구현하며, `vault.rs`와 `mcp/fingerprint.test.ts`가 **동일한 벡터**로 고정한다. 응답에는
  `stale.changed`(정확한 판정)와 `stale.fingerprint`가 실린다.

  fingerprint 입력의 경로도 `/`로 정규화한다. 두 번째 문제가 함께 닫힌다 — 같은 vault가
  **macOS와 Windows에서 다른 fingerprint**를 내서, 양쪽에서 열면 매번 전체 재빌드였다.

  ⚠️ **캐시 버전 7 → 8.** 올린 뒤 첫 기동에서 인덱스를 한 번 전부 다시 만든다(19,000 노트 기준
  약 1분). 한 번뿐이다.

---

## [1.14.0] — 2026-08-26

### Added
- **Windows (x64) 지원** ([#196]). macOS와 나란히 Windows 10+에서 빌드·실행된다. CI의 Rust 검사·테스트도
  이제 **양쪽**에서 돈다. 세 가지를 고쳐야 했다. **경로** — Rust는 프런트에 `\` 구분자 문자열을 넘기는데
  프런트는 20여 곳에서 `/`로 쪼갠다. 게다가 Windows `canonicalize()`는 확장 길이 경로(`\\?\C:\...`)를
  낸다. 경계 헬퍼 하나(`uipath::to_ui`)가 둘 다 정규화하고, MCP 서버도 같은 계약(`normPath`)을 갖는다.
  **이미지** — 정적 asset 프로토콜 scope가 macOS 배치를 전제해서, vault가 사용자 폴더 밖(`D:\notes`)이면
  이미지가 **하나도** 안 떴다. 이제 열린 vault를 런타임에 등록한다 — 정적 scope보다 오히려 *좁다*.
  **단축키** — 존재하지도 않는 `⌘K`를 팔레트가 그대로 보여주고 있었다. 라벨과 샘플 노트를 표시 시점에
  `Ctrl+K`로 바꾼다.

### Fixed
- **지식 질의 MCP 서버가 Windows에서 캐시를 영영 찾지 못하던 것** ([#196]). 앱 데이터 경로가
  `~/Library/Application Support`로 박혀 있어, 인덱스가 멀쩡해도 모든 질의가 `cache_absent`로 답했다.

---

## [1.13.0] — 2026-08-24

### Changed
- **노트 하나를 고쳐도 기동 때 vault 전량을 다시 읽던 것** ([#192]). 캐시 키가 모든 파일의
  `(경로, mtime, 크기)`를 한 덩어리로 해싱한 값이라, 노트 한 개가 바뀌면 전부가 무효였다.
  작성자 vault 기준으로 그건 본문 **52.6 MB** 재읽기 + **약 5.3 s** 재색인인데, 실제 방아쇠는
  19,364개 중 **38개(0.2%)** 변경이다. vault를 쓰는 게 Lapis가 아니라 바깥 도구들이라 평상시
  기동의 대다수가 그 경로였다 — 최근 30일 중 **19일**에 변경이 있었다. 이제 기동 시 파일별 stat
  스냅샷을 이전 것과 대조해, 움직인 파일이 적으면 그것만 고친다(파일 watcher가 이미 쓰던 증분
  기계장치 그대로). 변경이 많거나, shard 수가 바뀌거나, 스냅샷이 없거나 어긋나면 예전처럼 전체를
  다시 만든다. 스냅샷은 매 기동 읽히는 메타데이터가 아니라 **별도 파일**에 둔다(gzip 231 KB,
  캐시의 +1.6%) — 바뀐 게 없는 vault는 추가 비용이 0이다.

### Fixed
- **검색 결과 스니펫이 매치된 본문 대신 노트의 YAML 머리말을 보여줄 수 있었다** ([#191]).
  랭킹은 한국어를 글자 bigram으로 찾는데 스니펫 추출기는 입력한 질의 그대로만 찾았다. 조사가
  다르면 — 질의 `인덱스로`, 본문 `인덱스를` — 매치가 0이 되고 폴백이 본문 앞 120자를 냈다.
  규약상 그 자리는 frontmatter 블록이다. 검색이 찾아낸 문서를 두고 왜 걸렸는지 못 보여준 셈이다.
  이제 어절 그대로 → 랭킹과 같은 bigram 순으로 내려가고, frontmatter에서는 시작하지 않는다.
- **vault를 바꾸면 이전 vault의 노트가 검색 결과로 새어 나올 수 있었다** ([#191]). vault를 열 때
  링크 인덱스는 비웠지만 풀텍스트 워커는 그대로 남았다. shard는 새 vault가 쓰는 것만 초기화되고
  shard 수는 노트 수를 따르므로, 큰 vault(8 shard) → 작은 vault(1 shard) 전환이면 shard 1–7이
  메모리에 남는다. 그리고 준비된 shard는 전부 질의에 답한다.
- **코드펜스 5종이 하이라이트 없이 그려졌다** ([#193]). vault 전량 실측: `svelte`(18개) ·
  `dart`(15) · `ruby`(7) · `http`(4) · `objective-c`(2). 앞 셋은 언어가 등록돼 있지 않았고,
  뒤 둘은 등록된 alias가 커버하지 않는 표기였다. `svelte`는 XML 문법에 연결했다 — 마크업과
  `<script>`/`<style>` 안쪽은 그대로 칠해진다.

## [1.12.2] — 2026-08-24

### Fixed
- **표가 가로로 터지고, 정작 줄이면 들어갈 열이 한 줄에 한 글자씩 접혔다** ([#189]). 인라인 코드에
  줄바꿈 선언이 없어 공백 없는 긴 경로(`dontalk/…/Foo.swift:1234`, brace 축약 `A/{B,C,D}`)가 끊을 곳
  없는 한 덩어리로 남았다. 문단에서는 페인 전체를 가로로 밀고, 표에서는 그 열의 최소 폭을 키워
  자동 레이아웃이 남은 폭을 다른 열에서 짜냈다 — 한글은 어느 글자에서나 끊기므로 그 압박이
  **한 줄에 한 글자**로, 높이 545px짜리 셀로 나타났다. 이제 인라인 코드에 `overflow-wrap: anywhere`가
  걸린다. 최소 폭까지 낮추는 값은 이것뿐이다(`break-word`는 낮추지 않아 표에는 듣지 않는다).
  코드블록은 배제해 그대로 가로 스크롤하고, 내보낸 HTML에도 같은 규칙이 실린다. 허브 실측: md
  1,301개 중 **321개(24.7%)**가 공백 없는 60자 초과 인라인 코드 토큰을 갖고 있다.

## [1.12.1] — 2026-08-21

### Fixed
- **다이어그램 하나가 깨지면 "Syntax error in text" 그림이 화면에 박혀 있었다** ([#187]). mermaid는
  자기 에러 그림을 `document.body`에 붙인 임시 `div`에 그리는데, 파싱이 실패하면 그 노드를 지우지
  않고 throw 한다. 그래서 폭탄 아이콘이 창 하단에 떠서 노트를 바꿔도 안 없어지고 앱을 재실행할
  때까지 남았다 — 화면에 열린 노트에 다이어그램이 없어도 그랬다. 잔해가 이전에 열었던 다른 노트에서
  온 것이기 때문이다. 이제 mermaid의 에러 렌더링을 끄고 임시 노드를 정리한다. 실패는 깨진 블록이
  있는 자리에 인라인으로 표시된다.

## [1.12.0] — 2026-08-21

### Fixed
- **속성 하나를 고치면 노트의 frontmatter가 통째로 날아갈 수 있었다** ([#184]). YAML 파싱이
  실패하면 앱이 "속성이 아예 없는 노트"로 읽고, 방금 고친 키만 남긴 블록을 새로 썼다 —
  나머지는 사라졌다. 이제는 쓰기를 거부하고 에디터에서 YAML을 먼저 고치라고 알린다.
  실측: 작성자 vault의 19,213개 노트 중 1개가 지금 정확히 그 상태다.
- **속성을 고칠 때마다 날짜가 다시 쓰였다** ([#184]). `created: 2026-08-13`이
  `2026-08-13T00:00:00.000Z`로 돌아왔다 — js-yaml 기본 스키마가 timestamp를 `Date` 객체로
  만들기 때문이다. 읽기·쓰기 모두 CORE 스키마를 쓰도록 바꿔 날짜가 문자열로 남는다.
  같은 결함이 UI에도 샜다: 속성 패널이 `Thu Aug 20 2026 09:00:00 GMT+0900`을 보여줬다.
- **frontmatter와 본문 사이의 빈 줄이 속성 편집 때마다 사라졌다** ([#184]) — 구분자 패턴이
  개행까지 삼켰다. 자동 링크 갱신도 같은 분리기를 써서 같은 결함을 갖고 있었다.

### Added
- **검색 계측 하네스가 품질뿐 아니라 비용도 게이트한다** ([#182]). `lapis-eval`이 R@1/R@10/MRR
  옆에 p50/p95/max 지연을 함께 내고, 긴 질의 프로브(16·32어절 — 품질 케이스로는 구성상
  만들어지지 않는다)를 더했으며, 지연 예산을 넘기면 종료 코드가 0이 아니다. 직전 변경이
  품질 지표를 전부 통과하면서 4배 느렸고, 그건 손측정으로 잡았다.
- **`lapis-bench`** ([#182]) — 인덱스 빌드 비용: 1,000노트당 밀리초, n/2→n 증가 배율(초선형
  회귀 감지), **노트당 JSON 바이트**. 크기가 주 게이트다 — 결정론적이라 15% 여유면 충분한
  반면, 바쁜 머신에서 오검출을 피할 만큼 느슨한 벽시계 예산은 토크나이저 회귀를 아예 못 잡는다.
- **frontmatter 파서 테스트 42건** ([#184], 그전엔 0건) + 헬퍼 호출을 통한 effect 의존성
  등록 테스트 1건 ([#185]).

### Changed
- **프리뷰 후처리 effect를 다시 썼다** ([#185]). 다섯 개의 `$effect`가 쓰지도 않는 변수에
  대입하는 방식으로 의존성을 선언하고 있었고(`const _html = parsed.html`), 이 관용구는 가드가
  앞으로 오면 조용히 깨진다. 이제 이름이 있는 `trackPreviewHtml()`을 부르고, 반복되던
  "널 체크 → `tick()` → 다시 널 체크 → 후처리" 절차는 헬퍼 하나로 모았다. 동작 변화는 없다 —
  위키링크 색·mermaid·테마 전환·`⌘F`를 손으로 확인했다.

## [1.11.0] — 2026-08-20

### Changed
- **풀텍스트 결합이 2단계에서 4단계로 늘었다.** 종전은 AND 다음 OR이었다 — 질의 단어 하나가
  정답 문서에 없으면 AND가 0건이 되고 통째로 OR로 떨어져 **평균 10,346건**(코퍼스의 53%)을
  긁어왔다. 절반쯤 기억하는 제목을 치는 건 예외가 아니라 기본 사용 패턴인데 그 경로가 가장
  나빴다. 중간에 두 단계를 넣었다 — `AND-1`(단어 하나를 빼고 AND)과 `OR-min`(OR 결과를 매칭
  term 수로 거름). 19,292 노트 · 360 케이스 실측에서 무관한 단어가 하나 섞인 질의의 매칭이
  **10,346건 → 220건(−98%)**, R@1은 **67.2% → 68.9%**. 깨끗한 질의는 완전히 그대로다 —
  새 단계는 AND가 0건일 때만 도달한다. `AND-1`은 O(n²)이라 **8어절 상한**을 뒀다 — 상한 없이는
  R@1이 1.1pt 높은 대신 **4배 느리다**(평균 118ms, 기존 29ms · 32어절 질의 860ms).
- MCP 응답 필드 `used[].combine`에 `AND-1`·`OR-min` 두 값이 추가됐다. `mcp/README.md` 참조.
- 설치 안내를 Releases 다운로드에서 소스 빌드로 교체. 바이너리 배포를 중단했다.
- **발행 문서의 기본 언어를 영어로 바꿨다.** `README.md`·`CHANGELOG.md`가 영어판이고
  한국어는 `README.ko.md`·`CHANGELOG.ko.md`다. 종전 `README.en.md`는 `README.md`로 옮겨졌다.

### Added
- **CI** — `.github/workflows/ci.yml`. PR과 `main` push에서 `svelte-check` · MCP 타입 체크 ·
  vitest · vite 빌드(ubuntu)와 `cargo fmt --check` · `cargo clippy`(경고를 오류로) ·
  `cargo check` · `cargo test`(macOS)를 돌린다.
- **`CHANGELOG.md`** — 이 문서. GitHub Releases를 쓰지 않으므로 릴리즈 노트를 대신한다.
- **DOM·반응성 테스트 프로젝트 신설**(`*.dom.test.ts`). 기존 node 프로젝트와 나란히 둔다.
  프리뷰 DOM 후처리와 Svelte effect 발화 시점을 **컴포넌트를 띄우지 않고** 잴 수 있다.
  카나리아를 함께 넣었다 — `resolve.conditions: ["browser"]`가 없으면 vitest가 SSR로 컴파일해
  `$effect`가 no-op이 되고 반응성 테스트가 전부 공허하게 통과한다. 카나리아가 그때 실패한다.
- **`⌘⇧F` 풀텍스트 검색이 구조 팔도 함께 낸다** — 걸리는 태그와 `doc_kind`/`topic` facet이 본문
  결과 아래에 붙는다. 종전엔 `⌘K`에만 있었다.
  짧은 질의를 도우려던 것인데 **실측 결과 그 효과는 없다**: 구조 팔의 어휘가 영문이다(고유 태그
  4,643개 중 한글 4개 · `topic` 299개 중 5개 · `doc_kind` 23개 중 0개). 점수가 나쁜 질의는
  한국어라 **둘은 만날 수 없다.** 실제로 돕는 건 어휘와 같은 언어로 친 질의다.
  짧은 한국어 질의는 여전히 미해결이고, 답은 여기가 아니다.
- **창 위치·크기가 재시작 너머로 유지된다.** 창마다 라벨 기준으로 자기 위치를 기억하므로,
  두 번째 창도 직전에 닫은 그 자리에 열린다. 그 창이 있던 모니터가 사라졌으면 화면 밖으로
  복원하지 않고 기본 위치로 떨어진다.

### Fixed
- `mcp/lapis-eval`이 표본 수 인자를 조용히 무시하던 것 — 래퍼가 `"$@"`를 안 넘겨 항상 기본값으로
  돌았다. 케이스 수가 다른 두 측정을 비교하고 나서야 드러났다.
- README의 릴리즈 배지가 죽어 있던 것 — 릴리즈를 전건 삭제해 `github/v/release`가 아무것도
  못 찾았다. 태그 기준 `github/v/tag`로 교체.

---

## [1.10.0] — 2026-08-19

UI 로컬라이제이션과 MCP 질의 토글. 저장소를 개인 계정으로 이전하면서 공개 저장소용 정리도 함께 했다.

### Added
- **UI 로컬라이제이션 (ko/en)** — Paraglide JS 2 기반, 원본 언어는 영어이고 한국어는 번역본이다.
  기본은 OS 언어 추종이며 설정에서 직접 고를 수도 있다. 메시지 309개 ([#171])
- **MCP 질의 토글** — 앱 설정에서 `lapis_query` 도구를 켜고 끈다. **기본은 차단**이고, 게이트는
  `tools/list`가 아니라 `tools/call`에 있어 재시작 없이 즉시 반영된다 ([#170])
- MIT LICENSE ([#166]), 영어 README와 언어 스위처 ([#168])

### Fixed
- 속성 자동완성 드롭다운이 컨텍스트 패널 바깥으로 나가지 못하고 잘리던 문제 ([#169])
- MCP 래퍼가 `PATH`에 의존해 Claude Desktop에서 서버가 뜨지 않던 문제 ([#163])
- 설정 부분 쓰기 — 일부 필드만 담아 저장하면 나머지 필드가 기본값으로 되돌아가던 문제.
  필드가 하나뿐이던 동안은 증상이 없었다 ([#170])

### Docs
- README 전면 재작성 + 저장소 URL 갱신 ([#165]), 개인 도구 면책 문구 ([#167])
- 검색 스택 기술 오류 정정 — README가 오랫동안 "tantivy + lindera"라고 적고 있었지만
  그 스택은 v1.3.0에서 제거됐다 ([#161], [#164])
- MCP 사용 가이드 추가 ([#162])

---

## [1.9.0] — 2026-08-13

지식 vault를 Claude Code에서 질의할 수 있게 열고, 그 과정에서 조용히 잘못 동작하던 캐시 결함들을 걷어냈다.

### Added
- **`lapis_query` MCP 서버** — 앱이 만든 인덱스 위에서 풀텍스트와 구조 질의(백링크·토픽·태그·doc_kind)를
  함께 낸다. 도구는 하나뿐이다 ([#156])
- 읽던 위치에서 편집 시작 — 읽기↔편집 전환 시 섹션 앵커를 이월한다 ([#154])

### Changed
- 풀텍스트 결합을 AND 우선 + OR 폴백으로 전환. 계측 결과 병목은 토크나이저가 아니라 결합 방식이었다 ([#159])

### Fixed
- 캐시 meta와 shard가 어긋나던 결함 6건 ([#155])
- 편집해도 디스크 캐시가 갱신되지 않던 문제 — 호출부 중복 게이트 ([#158])
- 개발 빌드와 릴리즈 빌드가 앱 데이터 디렉터리를 공유하던 것을 분리 ([#157])

---

## [1.8.0] — 2026-08-11

창 단위 멀티 vault와 읽기↔편집 단일 토글. 비활성 상태로 남아 있던 그래프 코드를 걷어냈다.

### Added
- **창마다 다른 vault** — watcher refcount + 창별 이벤트 라우팅
- ⌘T 새 탭 / ⌘P는 활성 탭 교체로 정리
- topbar 경로 라벨 클릭으로 절대 경로 복사

### Changed
- **Editor/Preview 분할을 제거하고 읽기↔편집 단일 토글로 전환**
- Editor 지연 로드 — 시작 payload 1089 KB → 543 KB
- 단축키 매칭을 `keymap.ts`로 분리 (테스트 27건), welcome 문서를 `welcomeDoc.ts`로 분리
- 비활성 그래프 기능 제거 (3,135줄)

### Fixed
- 새 창이 이전 vault를 그대로 열던 문제 — 창별 키를 지연 평가로 전환

---

## [1.7.0] — 2026-08-06

### Added
- 읽기 타이포그래피 교정 — 본문 폭 조절, 헤딩 위계, 문단 간격 ([#144])
- 디버그 빌드 표식 — 창 제목과 topbar 배지 ([#146])

### Fixed
- 페인 툴바 ⋯ 메뉴가 한 자씩 줄바꿈되던 문제 (v1.6.0 회귀) ([#145])

---

## [1.6.0] — 2026-08-05

UI 전면 개편. 채팅 앱(Discord)의 셸 레이아웃을 참조해 좌측 아이콘 레일 + 우측 컨텍스트 패널 구조로 바꿨다.

### Added
- 디자인 토큰 — 표면 3계층, radius 상향, 밀도 2단 ([#130])
- 좌측 아이콘 레일 상시화와 활성 섹션 표시 ([#132]), 우측 컨텍스트 패널 신설 ([#133])
- vault 헤더 드롭다운 + 하단 상태 줄 ([#139]), 레일 툴팁 ([#138])
- **안 본 사이 바뀐 노트 표시** — unread 은유 ([#140])
- 오버레이 등장/퇴장 모션 ([#137]), 탭 칩·카테고리 접기 모션 ([#141])

### Changed
- 하드보더 제거 + 표면 3계층 적용 ([#131]), 액센트 색 전환 ([#135])
- 신규 설치 기본 레이아웃을 Editor 접힘으로, topbar 슬림화 ([#134])
- 아이템 칩화 · 레일 pill 모프 · 카테고리 대문자 ([#136])

### Fixed
- 목차 항목 하단 잘림 + 섹션 배지 상한 9999+ ([#142])

---

## [1.5.0] — 2026-08-03

### Added
- Finder에서 보기 — 파일 트리·탭·Editor/Preview 전부에서 ([#127])
- 프리뷰 내용을 자립형 HTML로 내보내기 ([#128])
- 페인 툴바 ⋯ 오버플로 메뉴 ([#126])

---

## [1.4.0] — 2026-06-22

검색 품질과 응답성.

### Added
- **Quick Switcher 초성 검색** — 한국어 자음 매칭 ([#119])
- **풀텍스트 한국어 bigram 토크나이저** — 합성어·어미 변형 recall ([#120])
- 설정에 인덱스 강제 재구축 — 캐시 무시, 워커 초기화, 전체 재빌드 ([#123])

### Changed
- 팔레트 검색 디바운스 + 스니펫 지연 생성 ([#121])
- Quick Switcher 정규화 캐시 + 점진 필터링 ([#122])
- git 자동 커밋을 변경 경로만 `add` — 전체 워킹트리 스캔 회피 ([#118])

---

## [1.3.0] — 2026-06-18

### Removed
- **claude-mem 통합 기능 전체 제거** ([#117]). 이때 함께 쓰이던 **tantivy + lindera 형태소 검색 엔진도
  같이 빠졌다** — 이후 검색은 MiniSearch 단일 스택이다. README가 한동안 이 사실을 반영하지 못했고,
  v1.10.0에서야 정정됐다.

---

## [1.2.2] — 2026-06-18

### Changed
- 그래프 기능 일시 비활성화 — 3D 재설계 검토 ([#114])
- watcher 재인덱싱을 백그라운드로 — 실시간 변경 시 사이드바 blocking 제거 ([#113])

### Fixed
- mirror sync 표시가 영구 "진행중"으로 stuck 되던 문제 — 리스너 재등록 race ([#115])

---

## [1.2.1] — 2026-06-18

### Fixed
- **`strip_md_extension`의 UTF-8 문자 경계 panic** — 릴리즈 직후 즉시 크래시. 바이트 슬라이스 비교로 교체 ([#110])
- 인덱스 빌드 스피너 freeze, watcher가 검색 인덱스를 놓치던 문제 ([#111], [#112])

### Added
- 증분 인덱싱, `.mmd` watcher ([#112])

---

## [1.2.0] — 2026-06-15

vault를 git으로 버전 관리.

### Added
- vault git 버전관리 백엔드 — shell-out 명령 ([#104])
- 자동 커밋 + 버전관리 시작 배너 ([#105])
- git 이력 뷰어 — Neighborhood "History" 구획 ([#106])
- 설정에 Git 버전관리 진입점 — 배너를 "나중에"로 닫은 뒤 재접근 ([#107])

### Fixed
- git 명령이 IPC 스레드를 블로킹해 UI가 30초 freeze 되던 문제 — async + `spawn_blocking` ([#108])

---

## [1.1.0] — 2026-06-11

### Added
- betweenness 온디맨드 "다리 노트" 토글 ([#97]), 무방향 환산 + 가중 토글 ([#101])
- disparity filter 백본 모드 ([#98])
- 그래프 Global type(`doc_kind`) 필터 ([#100])
- MOC 자동제안 진단 패널 — MOC 후보·다리 노트·고아 노트 ([#102])
- 사이드바 펼친 섹션 드래그 리사이즈 ([#99])

---

## [1.0.0] — 2026-06-08

문서 단위 지식 그래프와 사이드바 재구성.

### Added
- frontmatter 타입 관계 인덱스 + Neighborhood 패널 ([#92])
- 필드 렌즈 그룹핑 — Files 탭 ([#93])
- 문서 단위 지식 그래프 — 데이터 모델과 Local/ego 모드 ([#94]), Global 모드와 인코딩 ([#95])
- 세로 아코디언 사이드바 네비 + 아이콘 레일 ([#96])

> 그래프 기능은 v1.2.2에서 비활성화되고 v1.8.0에서 코드까지 제거됐다. 현재 앱에는 없다.

---

## [0.12.1] — 2026-06-05

### Fixed
- 탭 우클릭 "다른 탭 닫기" 등이 동작하지 않던 버그 ([#90])

---

## [0.12.0] — 2026-06-04

### Added
- 노트 탭 영속화 — vault별 복원 ([#85])
- 탭 드래그 재정렬 + 우클릭 다른 탭 닫기 ([#86])
- 프리뷰 글꼴 크기(줌) 조절 ([#87])

### Fixed
- 앱 내 rename/delete/move 시 백링크 캐시 즉시 무효화 ([#88])

---

## [0.11.0] — 2026-06-04

노트 네비게이션 묶음.

### Added
- **위키링크 자동완성(`[[`)** — CodeMirror 자동완성 드롭다운 ([#79])
- 노트 뒤로/앞으로 가기 ([#80])와 히스토리 목록 드롭다운 ([#81])
- **노트 탭 (멀티 오픈)** ([#82])
- 즐겨찾기/핀 사이드바 탭 + 최근 노트 ([#83])

---

## [0.10.0] — 2026-06-02

### Added
- 문서 아웃라인(TOC) 패널 — 에디터·프리뷰 양방향 동기 ([#73])
- 노트 워드/글자수·읽기시간 topbar 표시 ([#72])
- 빈 frontmatter에서도 Properties를 추가할 수 있는 진입점 ([#77])

### Changed
- 간격 토큰화 — 정확일치 px를 `--sp-*`로 ([#74])
- 모달 셸 프리미티브 `ModalShell` + z-index 토큰 정규화 ([#75])
- 자동 링크 갱신의 코드영역 식별을 정규식에서 AST(markdown-it)로 ([#76])
- 아이콘 버튼을 `.btn--icon`으로 통합 ([#77])

---

## [0.9.1] — 2026-06-01

### Fixed
- Mermaid PNG 내보내기 — WKWebView canvas taint 회피 (blob → data URL) ([#71])

---

## [0.9.0] — 2026-06-01

### Added
- 프리뷰 코드블록 구문 하이라이팅 — highlight.js를 `--cm-*` 토큰에 연결 ([#68])

### Changed
- **디자인 시스템 전면 개편** — 토큰 도입 + 3-way 테마(라이트/다크/시스템) ([#64])
- 크기·형태 정규화 + 버튼 프리미티브 ([#67])

### Fixed
- 라이트 모드에서 Mermaid 다이어그램 가독성 — 테마 적응 렌더 ([#66])

---

## [0.8.0] — 2026-05-29

### Added
- Mermaid 다이어그램 PNG 내보내기 — hover 버튼에서 atomic 저장 ([#62])

---

## [0.7.0] — 2026-05-27

### Added
- 사이드바 접기/펼치기 — ⌘B, 헤더 버튼, 접힌 strip ([#60])

---

## [0.6.0] — 2026-05-21

큰 vault 대비 성능 묶음.

### Changed
- 검색 cold init 제거 + LRU 캐시 + release profile ([#49])
- vault cold-start 묶음 — `read_vault_bundle`(rayon) ([#50])
- MiniSearch 디스크 캐시 ([#51]), 캐시 gzip + `fullTextIndex` lazy load ([#52])
- 캐시 메타/JSON IPC 분리 + MiniSearch Web Worker ([#53])
- 파일 트리 가상 스크롤 ([#55])
- 검색 인덱스 sharded progressive load — 첫 shard 1.8초 뒤 부분 검색 가능 ([#56]),
  shard 수를 vault 크기로 동적 조정 ([#57])

### Added
- 현재 노트 절대 경로 복사 + 사이드바 트리 검색 필터 ([#54])

---

## [0.5.1] — 2026-05-19

### Added
- 백업 `max_keep`을 설정에서 노출 (1–100) ([#47])

### Removed
- 그래프 뷰 기능 완전 제거 — ADR-001 ([#45])

---

## [0.5.0] — 2026-05-18

### Added
- 링크 자동 갱신 dry-run 미리보기 + `.lapis` 스냅샷 백업 ([#42])
- 백업 prune(최대 20개) + write 실패 시 자동 롤백 ([#43])
- in-doc 검색 옵션 — regex / case / whole-word ([#39])
- vitest 도입 + linkRewrite 골든 테스트 ([#41])

---

## [0.4.0] — 2026-05-14

### Added
- F2 rename / ⌘⌫ 삭제, Properties 추가, 백링크 무효화 ([#34])
- Command Palette에 F2 보조 항목 + Mac 매직 키보드 안내 ([#35])
- `.mmd` 단일 파일 지원 ([#36])

---

## [0.3.0] — 2026-05-14

### Added
- claude-mem 세션 요약을 vault로 export, 메모리 FTS 검색과 관련 메모리 패널
- `lapis-mem.db` mirror + 실시간 sync 엔진
- tantivy + lindera 한국어 형태소 검색 엔진
- claude-mem 통합을 옵션화 (기본 OFF) ([#30])

> 이 문단의 기능은 **v1.3.0에서 전부 제거**됐다.

---

## [0.2.0] — 2026-05-12

첫 태그. Phase 0–5.0의 기반 기능이 모두 여기 들어 있다.

### Added
- Tauri 2 + SvelteKit + TypeScript 스캐폴딩, CodeMirror 6 + markdown-it + frontmatter PoC
- vault 열기, 파일 트리, 스크롤 동기화
- Wikilink + 백링크 패널 + 안전한 링크 navigation
- Quick Switcher(⌘P), 풀텍스트 검색(⌘⇧F), 태그 인덱스와 Files/Tags 탭
- 편집·저장(⌘S + autosave) + **atomic write**
- frontmatter 4키 스키마 인식, nested tags 계층 색인, `doc_kind`/`topic` facet 필터
- file watcher — 외부 변경 자동 감지 + 충돌 처리
- vault 조작(생성·삭제·rename·이동) + 자동 링크 갱신
- ⌘K 통합 명령 팔레트, 백링크 칩 펼침과 컨텍스트 스니펫
- frontmatter 인라인 편집, Mermaid 코드블록 인라인 렌더, 이미지 상대 경로와 발행물 갤러리
- 인-도큐먼트 검색 — Editor·Preview 각각 ⌘F

<!-- 링크 참조 -->

[Unreleased]: https://github.com/eren0315/lapis/compare/v3.11.0...main
[3.11.0]: https://github.com/eren0315/lapis/compare/v3.10.0...v3.11.0
[3.10.0]: https://github.com/eren0315/lapis/compare/v3.9.0...v3.10.0
[3.9.0]: https://github.com/eren0315/lapis/compare/v3.8.0...v3.9.0
[3.8.0]: https://github.com/eren0315/lapis/compare/v3.7.2...v3.8.0
[3.7.2]: https://github.com/eren0315/lapis/compare/v3.7.1...v3.7.2
[3.7.1]: https://github.com/eren0315/lapis/compare/v3.7.0...v3.7.1
[3.7.0]: https://github.com/eren0315/lapis/compare/v3.6.0...v3.7.0
[3.6.0]: https://github.com/eren0315/lapis/compare/v3.5.2...v3.6.0
[3.5.2]: https://github.com/eren0315/lapis/compare/v3.5.1...v3.5.2
[3.5.1]: https://github.com/eren0315/lapis/compare/v3.5.0...v3.5.1
[3.5.0]: https://github.com/eren0315/lapis/compare/v3.4.0...v3.5.0
[3.4.0]: https://github.com/eren0315/lapis/compare/v3.3.0...v3.4.0
[3.3.0]: https://github.com/eren0315/lapis/compare/v3.2.0...v3.3.0
[3.2.0]: https://github.com/eren0315/lapis/compare/v3.1.2...v3.2.0
[3.1.2]: https://github.com/eren0315/lapis/compare/v3.1.1...v3.1.2
[3.1.1]: https://github.com/eren0315/lapis/compare/v3.1.0...v3.1.1
[3.1.0]: https://github.com/eren0315/lapis/compare/v3.0.2...v3.1.0
[3.0.2]: https://github.com/eren0315/lapis/compare/v3.0.1...v3.0.2
[3.0.1]: https://github.com/eren0315/lapis/compare/v3.0.0...v3.0.1
[3.0.0]: https://github.com/eren0315/lapis/compare/v3.0.0-beta...v3.0.0
[3.0.0-beta]: https://github.com/eren0315/lapis/compare/v2.4.1...v3.0.0-beta
[2.4.1]: https://github.com/eren0315/lapis/compare/v2.4.0...v2.4.1
[2.4.0]: https://github.com/eren0315/lapis/compare/v2.3.1...v2.4.0
[2.3.1]: https://github.com/eren0315/lapis/compare/v2.3.0...v2.3.1
[2.3.0]: https://github.com/eren0315/lapis/compare/v2.2.0...v2.3.0
[2.2.0]: https://github.com/eren0315/lapis/compare/v2.1.0...v2.2.0
[2.1.0]: https://github.com/eren0315/lapis/compare/v2.0.0...v2.1.0
[2.0.0]: https://github.com/eren0315/lapis/compare/v1.20.0...v2.0.0
[1.20.0]: https://github.com/eren0315/lapis/compare/v1.19.0...v1.20.0
[1.19.0]: https://github.com/eren0315/lapis/compare/v1.18.0...v1.19.0
[1.18.0]: https://github.com/eren0315/lapis/compare/v1.17.0...v1.18.0
[1.17.0]: https://github.com/eren0315/lapis/compare/v1.16.0...v1.17.0
[1.16.0]: https://github.com/eren0315/lapis/compare/v1.15.0...v1.16.0
[1.15.0]: https://github.com/eren0315/lapis/compare/v1.14.0...v1.15.0
[1.14.0]: https://github.com/eren0315/lapis/compare/v1.13.0...v1.14.0
[1.13.0]: https://github.com/eren0315/lapis/compare/v1.12.2...v1.13.0
[1.12.2]: https://github.com/eren0315/lapis/compare/v1.12.1...v1.12.2
[1.12.1]: https://github.com/eren0315/lapis/compare/v1.12.0...v1.12.1
[1.12.0]: https://github.com/eren0315/lapis/compare/v1.11.0...v1.12.0
[1.11.0]: https://github.com/eren0315/lapis/compare/v1.10.0...v1.11.0
[1.10.0]: https://github.com/eren0315/lapis/compare/v1.9.0...v1.10.0
[1.9.0]: https://github.com/eren0315/lapis/compare/v1.8.0...v1.9.0
[1.8.0]: https://github.com/eren0315/lapis/compare/v1.7.0...v1.8.0
[1.7.0]: https://github.com/eren0315/lapis/compare/v1.6.0...v1.7.0
[1.6.0]: https://github.com/eren0315/lapis/compare/v1.5.0...v1.6.0
[1.5.0]: https://github.com/eren0315/lapis/compare/v1.4.0...v1.5.0
[1.4.0]: https://github.com/eren0315/lapis/compare/v1.3.0...v1.4.0
[1.3.0]: https://github.com/eren0315/lapis/compare/v1.2.2...v1.3.0
[1.2.2]: https://github.com/eren0315/lapis/compare/v1.2.1...v1.2.2
[1.2.1]: https://github.com/eren0315/lapis/compare/v1.2.0...v1.2.1
[1.2.0]: https://github.com/eren0315/lapis/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/eren0315/lapis/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/eren0315/lapis/compare/v0.12.1...v1.0.0
[0.12.1]: https://github.com/eren0315/lapis/compare/v0.12.0...v0.12.1
[0.12.0]: https://github.com/eren0315/lapis/compare/v0.11.0...v0.12.0
[0.11.0]: https://github.com/eren0315/lapis/compare/v0.10.0...v0.11.0
[0.10.0]: https://github.com/eren0315/lapis/compare/v0.9.1...v0.10.0
[0.9.1]: https://github.com/eren0315/lapis/compare/v0.9.0...v0.9.1
[0.9.0]: https://github.com/eren0315/lapis/compare/v0.8.0...v0.9.0
[0.8.0]: https://github.com/eren0315/lapis/compare/v0.7.0...v0.8.0
[0.7.0]: https://github.com/eren0315/lapis/compare/v0.6.0...v0.7.0
[0.6.0]: https://github.com/eren0315/lapis/compare/v0.5.1...v0.6.0
[0.5.1]: https://github.com/eren0315/lapis/compare/v0.5.0...v0.5.1
[0.5.0]: https://github.com/eren0315/lapis/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/eren0315/lapis/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/eren0315/lapis/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/eren0315/lapis/releases/tag/v0.2.0

[#278]: https://github.com/eren0315/lapis/pull/278
[#277]: https://github.com/eren0315/lapis/pull/277
[#276]: https://github.com/eren0315/lapis/pull/276
[#275]: https://github.com/eren0315/lapis/pull/275
[#274]: https://github.com/eren0315/lapis/pull/274
[#273]: https://github.com/eren0315/lapis/pull/273
[#272]: https://github.com/eren0315/lapis/pull/272
[#271]: https://github.com/eren0315/lapis/pull/271
[#270]: https://github.com/eren0315/lapis/pull/270
[#269]: https://github.com/eren0315/lapis/pull/269
[#268]: https://github.com/eren0315/lapis/pull/268
[#267]: https://github.com/eren0315/lapis/pull/267
[#265]: https://github.com/eren0315/lapis/pull/265
[#264]: https://github.com/eren0315/lapis/pull/264
[#263]: https://github.com/eren0315/lapis/pull/263
[#261]: https://github.com/eren0315/lapis/pull/261
[#259]: https://github.com/eren0315/lapis/pull/259
[#258]: https://github.com/eren0315/lapis/pull/258
[#256]: https://github.com/eren0315/lapis/pull/256
[#254]: https://github.com/eren0315/lapis/pull/254
[#253]: https://github.com/eren0315/lapis/pull/253
[#252]: https://github.com/eren0315/lapis/pull/252
[#251]: https://github.com/eren0315/lapis/pull/251
[#250]: https://github.com/eren0315/lapis/pull/250
[#249]: https://github.com/eren0315/lapis/pull/249
[#247]: https://github.com/eren0315/lapis/pull/247
[#246]: https://github.com/eren0315/lapis/pull/246
[#245]: https://github.com/eren0315/lapis/pull/245
[#243]: https://github.com/eren0315/lapis/pull/243
[#240]: https://github.com/eren0315/lapis/pull/240
[#239]: https://github.com/eren0315/lapis/pull/239
[#238]: https://github.com/eren0315/lapis/pull/238
[#237]: https://github.com/eren0315/lapis/pull/237
[#236]: https://github.com/eren0315/lapis/pull/236
[#235]: https://github.com/eren0315/lapis/pull/235
[#233]: https://github.com/eren0315/lapis/pull/233
[#232]: https://github.com/eren0315/lapis/pull/232
[#230]: https://github.com/eren0315/lapis/pull/230
[#229]: https://github.com/eren0315/lapis/pull/229
[#228]: https://github.com/eren0315/lapis/pull/228
[#227]: https://github.com/eren0315/lapis/pull/227
[#226]: https://github.com/eren0315/lapis/pull/226
[#224]: https://github.com/eren0315/lapis/pull/224
[#223]: https://github.com/eren0315/lapis/pull/223
[#221]: https://github.com/eren0315/lapis/pull/221
[#220]: https://github.com/eren0315/lapis/pull/220
[#219]: https://github.com/eren0315/lapis/pull/219
[#217]: https://github.com/eren0315/lapis/pull/217
[#216]: https://github.com/eren0315/lapis/pull/216
[#215]: https://github.com/eren0315/lapis/pull/215
[#214]: https://github.com/eren0315/lapis/pull/214
[#213]: https://github.com/eren0315/lapis/pull/213
[#212]: https://github.com/eren0315/lapis/pull/212
[#210]: https://github.com/eren0315/lapis/pull/210
[#209]: https://github.com/eren0315/lapis/pull/209
[#208]: https://github.com/eren0315/lapis/pull/208
[#207]: https://github.com/eren0315/lapis/pull/207
[#206]: https://github.com/eren0315/lapis/pull/206
[#202]: https://github.com/eren0315/lapis/pull/202
[#201]: https://github.com/eren0315/lapis/pull/201
[#200]: https://github.com/eren0315/lapis/pull/200
[#199]: https://github.com/eren0315/lapis/pull/199
[#198]: https://github.com/eren0315/lapis/pull/198
[#196]: https://github.com/eren0315/lapis/pull/196
[#193]: https://github.com/eren0315/lapis/pull/193
[#192]: https://github.com/eren0315/lapis/pull/192
[#191]: https://github.com/eren0315/lapis/pull/191
[#189]: https://github.com/eren0315/lapis/pull/189
[#187]: https://github.com/eren0315/lapis/pull/187
[#185]: https://github.com/eren0315/lapis/pull/185
[#184]: https://github.com/eren0315/lapis/pull/184
[#182]: https://github.com/eren0315/lapis/pull/182
[#171]: https://github.com/eren0315/lapis/pull/171
[#170]: https://github.com/eren0315/lapis/pull/170
[#169]: https://github.com/eren0315/lapis/pull/169
[#168]: https://github.com/eren0315/lapis/pull/168
[#167]: https://github.com/eren0315/lapis/pull/167
[#166]: https://github.com/eren0315/lapis/pull/166
[#165]: https://github.com/eren0315/lapis/pull/165
[#164]: https://github.com/eren0315/lapis/pull/164
[#163]: https://github.com/eren0315/lapis/pull/163
[#162]: https://github.com/eren0315/lapis/pull/162
[#161]: https://github.com/eren0315/lapis/pull/161
[#159]: https://github.com/eren0315/lapis/pull/159
[#158]: https://github.com/eren0315/lapis/pull/158
[#157]: https://github.com/eren0315/lapis/pull/157
[#156]: https://github.com/eren0315/lapis/pull/156
[#155]: https://github.com/eren0315/lapis/pull/155
[#154]: https://github.com/eren0315/lapis/pull/154
[#146]: https://github.com/eren0315/lapis/pull/146
[#145]: https://github.com/eren0315/lapis/pull/145
[#144]: https://github.com/eren0315/lapis/pull/144
[#142]: https://github.com/eren0315/lapis/pull/142
[#141]: https://github.com/eren0315/lapis/pull/141
[#140]: https://github.com/eren0315/lapis/pull/140
[#139]: https://github.com/eren0315/lapis/pull/139
[#138]: https://github.com/eren0315/lapis/pull/138
[#137]: https://github.com/eren0315/lapis/pull/137
[#136]: https://github.com/eren0315/lapis/pull/136
[#135]: https://github.com/eren0315/lapis/pull/135
[#134]: https://github.com/eren0315/lapis/pull/134
[#133]: https://github.com/eren0315/lapis/pull/133
[#132]: https://github.com/eren0315/lapis/pull/132
[#131]: https://github.com/eren0315/lapis/pull/131
[#130]: https://github.com/eren0315/lapis/pull/130
[#128]: https://github.com/eren0315/lapis/pull/128
[#127]: https://github.com/eren0315/lapis/pull/127
[#126]: https://github.com/eren0315/lapis/pull/126
[#123]: https://github.com/eren0315/lapis/pull/123
[#122]: https://github.com/eren0315/lapis/pull/122
[#121]: https://github.com/eren0315/lapis/pull/121
[#120]: https://github.com/eren0315/lapis/pull/120
[#119]: https://github.com/eren0315/lapis/pull/119
[#118]: https://github.com/eren0315/lapis/pull/118
[#117]: https://github.com/eren0315/lapis/pull/117
[#115]: https://github.com/eren0315/lapis/pull/115
[#114]: https://github.com/eren0315/lapis/pull/114
[#113]: https://github.com/eren0315/lapis/pull/113
[#112]: https://github.com/eren0315/lapis/pull/112
[#111]: https://github.com/eren0315/lapis/pull/111
[#110]: https://github.com/eren0315/lapis/pull/110
[#108]: https://github.com/eren0315/lapis/pull/108
[#107]: https://github.com/eren0315/lapis/pull/107
[#106]: https://github.com/eren0315/lapis/pull/106
[#105]: https://github.com/eren0315/lapis/pull/105
[#104]: https://github.com/eren0315/lapis/pull/104
[#102]: https://github.com/eren0315/lapis/pull/102
[#101]: https://github.com/eren0315/lapis/pull/101
[#100]: https://github.com/eren0315/lapis/pull/100
[#99]: https://github.com/eren0315/lapis/pull/99
[#98]: https://github.com/eren0315/lapis/pull/98
[#97]: https://github.com/eren0315/lapis/pull/97
[#96]: https://github.com/eren0315/lapis/pull/96
[#95]: https://github.com/eren0315/lapis/pull/95
[#94]: https://github.com/eren0315/lapis/pull/94
[#93]: https://github.com/eren0315/lapis/pull/93
[#92]: https://github.com/eren0315/lapis/pull/92
[#90]: https://github.com/eren0315/lapis/pull/90
[#88]: https://github.com/eren0315/lapis/pull/88
[#87]: https://github.com/eren0315/lapis/pull/87
[#86]: https://github.com/eren0315/lapis/pull/86
[#85]: https://github.com/eren0315/lapis/pull/85
[#83]: https://github.com/eren0315/lapis/pull/83
[#82]: https://github.com/eren0315/lapis/pull/82
[#81]: https://github.com/eren0315/lapis/pull/81
[#80]: https://github.com/eren0315/lapis/pull/80
[#79]: https://github.com/eren0315/lapis/pull/79
[#77]: https://github.com/eren0315/lapis/pull/77
[#76]: https://github.com/eren0315/lapis/pull/76
[#75]: https://github.com/eren0315/lapis/pull/75
[#74]: https://github.com/eren0315/lapis/pull/74
[#73]: https://github.com/eren0315/lapis/pull/73
[#72]: https://github.com/eren0315/lapis/pull/72
[#71]: https://github.com/eren0315/lapis/pull/71
[#68]: https://github.com/eren0315/lapis/pull/68
[#67]: https://github.com/eren0315/lapis/pull/67
[#66]: https://github.com/eren0315/lapis/pull/66
[#64]: https://github.com/eren0315/lapis/pull/64
[#62]: https://github.com/eren0315/lapis/pull/62
[#60]: https://github.com/eren0315/lapis/pull/60
[#57]: https://github.com/eren0315/lapis/pull/57
[#56]: https://github.com/eren0315/lapis/pull/56
[#55]: https://github.com/eren0315/lapis/pull/55
[#54]: https://github.com/eren0315/lapis/pull/54
[#53]: https://github.com/eren0315/lapis/pull/53
[#52]: https://github.com/eren0315/lapis/pull/52
[#51]: https://github.com/eren0315/lapis/pull/51
[#50]: https://github.com/eren0315/lapis/pull/50
[#49]: https://github.com/eren0315/lapis/pull/49
[#47]: https://github.com/eren0315/lapis/pull/47
[#45]: https://github.com/eren0315/lapis/pull/45
[#43]: https://github.com/eren0315/lapis/pull/43
[#42]: https://github.com/eren0315/lapis/pull/42
[#41]: https://github.com/eren0315/lapis/pull/41
[#39]: https://github.com/eren0315/lapis/pull/39
[#36]: https://github.com/eren0315/lapis/pull/36
[#35]: https://github.com/eren0315/lapis/pull/35
[#34]: https://github.com/eren0315/lapis/pull/34
[#30]: https://github.com/eren0315/lapis/pull/30

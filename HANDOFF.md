# 이어받기 — 머신을 넘는 작업 인계

**이 파일은 커밋된다.** `CLAUDE.md` 와 `docs/` 는 `.gitignore` 대상이고 SharedDocs 허브는
원격이 없다(`git remote` 0건) — 커밋된 파일만 다른 머신의 나에게 도달한다. 규칙은
[PRACTICES.md](PRACTICES.md), 변경 이력은 [CHANGELOG.ko.md](CHANGELOG.ko.md), **한쪽 머신에서
관측한 것을 다른 쪽에서 이어받는 것**은 여기다.

🔴 **public 리포다.** vault 실경로·회사 문서명·티켓 번호는 적지 않는다. 계측은 **종류와 수**로만
적는다(원본은 각 머신의 사용 로그에 남아 있다).

최신 항목을 위에 쌓는다. 해소된 항목은 CHANGELOG 로 승격하고 여기서 지운다.

---

## 2026-09-11 — macOS 실사용 4일 (v3.11.0)

윈도우에서 v1.13 → v3.11 을 올린 뒤, macOS 에서 **처음으로 계속 쓰기만 했다**(4일).
**화면에 뜬 버그는 없었다.** 그런데 사용 로그에는 warn 31 건이 있었고, 넷은 원인까지 나왔다.
"조용히 틀리던 것"을 찾는 방식이 또 맞았다 — 이번에는 만든 로그가 스스로를 잡았다.

### 계측 스냅샷

| 항목 | 값 |
|---|---|
| 사용 로그 | 15 세션 · 400 이벤트 · 2026-09-08 ~ 09-11 · 평균 세션 66분 |
| 오류 | **err 0 · warn 31** (전부 화면에 안 뜨는 경로) |
| 검증 | svelte-check **0/0** · cargo fmt·clippy 통과 · cargo test **107 passed** |
| 유닛 테스트 | vitest **2586 passed / 65 failed** ← ① 참조 (CI 는 초록) |
| 인덱싱 | 전량 빌드 6회 **평균 15.5s / 최대 18.2s** · 캐시히트 6회 488ms · 델타 6회 656ms |
| 질의 | quick 179건 · 0건 반환 7건 |

측정 방법: 앱 사용 로그(`lapis_usage` MCP 또는 앱의 분석 문서)를 그대로 읽었다. 추정한 값은
하나도 안 섞었다 — 아래 항목마다 "근거"가 로그의 실제 개수다.

---

### ① `.mmd` 가 증분 인덱싱에서만 거부된다 — 전량 빌드는 받는다 · P1

**증상은 없다.** 사용자에게는 아무 일도 안 일어난다. 대신 `.mmd` 를 고칠 때마다 로그에 warn 이
쌓이고 기동이 **0.66s 에서 15.5s 로** 바뀐다.

- **원인**: `scan_link_single` 이 `.md` 만 허용한다 — `src-tauri/src/vault.rs:564`
  (`"only .md files"`). 같은 파일의 워커 셋은 전부 `is_supported_note_ext`(`.md` + `.mmd`,
  `vault.rs:436`)를 쓰고, 그걸 통일해 두었다는 **테스트도 있다**(`vault.rs:1619`
  `walkers_count_md_and_mmd_consistently`). 증분 경로 하나만 그 통일에서 빠져 있다.
- **결과 둘**:
  1. `src/lib/stores/vault.ts:350` — 델타가 실패해 **전량 재빌드로 넘어간다**(로그 3건).
     전량 빌드가 결과를 고쳐 놓기 때문에 증상이 안 보인다. 15초를 그 대가로 낸다.
  2. `src/lib/stores/vault.ts:925` — 재색인에서 그 `.mmd` 의 링크가 **갱신되지 않는다**(로그 18건).
     전량 빌드는 `.mmd` 링크를 넣으므로, **전량과 증분이 서로 다른 인덱스를 만든다.**
- **근거**: warn 21 건 전수의 확장자가 `.mmd`(18 + 3). 해당 파일들은 지금도 vault 에 있다 —
  삭제 경쟁이 아니다.
- **고칠 자리**: `vault.rs:564` 의 확장자 게이트를 `is_supported_note_ext` 로 교체.
- **회귀 장치**: `.mmd` 를 넣은 `scan_link_single` 테스트. 더 나은 것은 **"증분과 전량이 같은
  인덱스를 낸다"**를 한 번 보는 테스트다 — 이 결함은 그것이 없어서 살아남았다.
- ⚠️ **로그가 원인을 못 말해 준다.** `logWarn(..., path, e)` 인데 기록에는 `path` 만 남고
  **에러 객체가 버려진다**. 어느 파일인지는 알고 왜인지는 모른다 — 이것도 같이 고칠 값이 있다.

### ② macOS 에서 `dom` 테스트 65건이 실패한다 — node 26 이 happy-dom 을 가린다 · P1

- **원인**: **Node 26 이 `globalThis.localStorage` 를 own getter 로 정의한다**(값은 `undefined`,
  `--localstorage-file` 없으면 경고만 낸다). vitest 의 happy-dom 환경이 그 자리를 못 덮어
  `window.localStorage` 까지 `undefined` 가 된다(`window === globalThis` 확인). 테스트는 맨
  `localStorage` 를 쓰므로 **파일 단위로 전멸**한다.
- **근거**: 실측 — 이 맥의 node `v26.7.0`. `dom` 프로젝트 5파일 65건 실패, 전부
  `Cannot read properties of undefined (reading 'clear')`. 프로브로 확인:
  `globalLS: undefined` · `windowLS: undefined` · `windowIsGlobal: true`.
  실패 파일: `usageBuffer` · `inDocSearch` · `layout` · `cliRenderWait` · `motionPref`(`*.dom.test.ts`).
- **왜 CI 가 못 잡나**: `.github/workflows/ci.yml` 이 **node 24** 를 고정한다. `.nvmrc` 도
  `engines` 도 없어서 **로컬은 아무 버전이나 쓴다.** 한쪽 머신에서만 빨간 상태를 막자고 만든
  규칙(#280)이 정작 **런타임 버전에는 안 걸려 있었다.**
- **고칠 자리**: ⓐ `.nvmrc`(+ `package.json` `engines`)로 24 고정 — CI 와 같은 것을 로컬이 쓴다.
  ⓑ 그것만으로는 node 26 에서 조용히 죽는 성질이 남는다. `dom` 프로젝트 setup 에서
  `localStorage` 를 **명시적으로 심고**, 심지 못하면 **즉시 실패**하게 한다.
- ⚠️ **확인 못 한 것**: 이 맥에 node 24 가 없어서(homebrew 에 `node@26` 만) **24 로 재실행해
  초록인지는 못 봤다.** 윈도우에서 먼저 볼 수 있다 — 거기서 초록이면 원인 확정이다.
- ⚠️ 그러니 **"맥에서 테스트가 깨졌다"를 코드 회귀로 읽지 말 것.** 같은 커밋이 CI 에서 초록이다.

### ③ CLI 로 열면 창이 앞으로 나오지 않는다 — ACL 누락 · P2

- **원인**: `plugin:window|set_focus not allowed by ACL`.
  `src-tauri/capabilities/default.json` 에 `core:window` 권한이 다섯 개 있는데
  **`allow-set-focus` 가 없다**(전체 12건 중).
- **근거**: warn 2건. 같은 기간 CLI 로 문서를 연 것이 **정확히 2번** — **두 번 다 실패했다**.
- **고칠 자리**: 권한 한 줄 추가. ⚠️ 권한 누락은 런타임 silent fail 이라 **테스트로는 안 잡힌다**
  (`CLAUDE.md` §3). 다른 머신에서 CLI 로 열어 창이 뜨는지 **손으로 본다.**

### ④ auto-commit 이 없는 경로 하나에 배치 전체를 잃는다 · P2

- **원인**: `commit_paths_inner`(`src-tauri/src/git.rs:170`)가 바뀐 경로 전부를 **한 번의
  `git add -- <paths…>`** 로 넘긴다. git 은 **하나라도 매치가 없으면 전체를 fatal 로 끝낸다**
  (`pathspec … did not match any files`) → 스테이징 0 → 커밋 0. 프론트(`stores/git.ts:208`)는
  실패분을 되돌려 넣으므로 **그 경로가 사라지지 않는 한 계속 같은 실패를 반복한다.**
- **근거**: warn 7건, 전부 같은 한 파일 이름. 문서를 만들자마자 규약대로 날짜를 붙여
  이름을 바꾼 자리로 보인다 — **추적도 안 되고 디스크에도 없는 이름**이 남는다.
- **결과**: 그 세션의 vault 스냅샷이 **안 남는다.** 재시작하면 전체 스윕으로 복구된다(그래서
  허브 히스토리에는 빈 자리가 안 보인다).
- **고칠 자리**: add 직전에 경로를 **거른다** — 디스크에 있거나 추적 중인 것만(삭제된 추적
  파일은 `git add --` 로 정상 처리된다). 거르고도 실패하면 전체 스윕으로 내려간다.
- ⚠️ 이건 **배너가 뜬다**(`alert_commit_failed`). "버그 안 보였다"에 안 걸린 유일한 이유는
  배너를 닫으면 사라지기 때문이다 — 반복 실패를 한 번은 **세어서** 보여줄 값이 있다.

### ⑤ 0건 질의 7건이 두 가지를 말한다 — IME 역방향은 이제 근거가 있다 · P2

- **한글을 영문 자판으로 친 것 3건** (`flqb` → `flqbe` → `flqbeo`, 두벌식으로 "리뷰…").
  v3.11.0 은 **반대 방향만**(영문을 한글 자판으로) 넣고 이 방향을 보류했는데, 그 근거가
  **"로그에 사례가 없다"** 였다. **이제 있다.** 보류 근거가 뒤집혔으니 다시 판단할 자리다.
  ⚠️ 보류의 다른 근거인 **조합 오토마타 필요**는 그대로 유효하다 — 낱자를 음절로 합쳐야 한다.
- **하이픈을 뺀 표기 4건** (영문 파일명을 하이픈 없이 붙여 쓴 것 3건 + 오타 1건). 찾던 파일은 **vault 에 있었고**
  이름이 하이픈으로 끊긴 형태다. 토크나이저가 하이픈으로 쪼개므로 **붙여 쓴 질의는 못 만난다.**
  0건이 "그런 노트 없음"으로 읽힌 자리다(실제로는 있다).
- ⚠️ 이 둘은 **검색을 건드리는 변경**이다 — 지연을 게이트에 넣는다(#175 에서 4배 느려질 뻔했다).

### ⑥ package-lock 버전이 1.20.0 에 멈춰 있다 · P3

- 앱은 **3.11.0**, 커밋된 `package-lock.json` 은 **1.20.0**. 20 버전을 지나며 6군데 범프 규칙에서
  빠졌다. 맥에서 `npm install` 한 번에 버전 2곳 + optional 패키지의 `dev` 플래그가 바뀌어
  **작업트리가 더러워진다**(지금 이 리포의 유일한 미커밋 변경).
- 고칠 자리: 한쪽 머신에서 한 번 재생성해 커밋. 재발 방지는 **버전 범프 대상에 lock 을
  포함했는지 보는 검사** — 릴리스마다 손으로 여섯 군데를 세는 규칙은 이미 두 번 놓쳤다.

---

### 안 쓰인 기능 — 판단 자료 (고칠 것 아님)

4일간 **호출 0회인 명령이 24개**다. 쓰기 계열(`new-note`·`rename`·`delete`)은 사용자가
읽기 전용이라 당연하다. 눈에 걸리는 것은 **`audit-*` 8종 전부와 `table-view`** — v3.x 에서
공들여 만들었는데 한 번도 안 불렸다. 반대로 `view:settings` 를 17회 열었다(팔레트 26회 다음).

기능이 없어서가 아니라 **닿지 않아서**일 수 있다. 다음 라운드에서 "새로 만들기" 전에 이 표를
먼저 볼 것 — 근거 없이 기능을 더하면 안 쓰이는 것이 25개가 된다.

### 머지 안 된 채 남은 것

- **`origin/fix/bundle-targets-and-practices`** (efb4913 · 2026-09-02 · PR 없음) — 번들 타깃 배열화
  + `PRACTICES.md` 신설 + `scripts/bundleTargets.*`. main 에 안 올라와 있다.
  ⚠️ 이 파일(`HANDOFF.md`)이 그 브랜치의 `PRACTICES.md` 를 참조한다 — **그 브랜치를 먼저 올리면**
  링크가 맞는다.

### macOS 쪽에서 확인된 것 (윈도우에서 안 봐도 되는 것)

- 4일 실사용 중 **화면에 뜬 결함 0건.** 표·다이어그램·인쇄·팔레트·탭 모두 정상.
- 캐시히트 기동 **488ms** · 델타 **656ms** — #192 의 증분 기계장치가 실제로 작동한다.
  (전량 15.5s 는 ① 때문에 불려 나온 것이다.)
- 한글 파일명·한글 디렉터리명 문서가 정상으로 열린다.
- `cargo fmt` · `clippy -D warnings` · `cargo test 107` 전부 통과.
